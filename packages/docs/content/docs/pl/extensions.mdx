---
title: Rozszerzenia
description: Zainstaluj rozszerzenia innych autorów w OpenChamber, zdecyduj, co mogą robić, połącz ich konta i usuń je, gdy nie są potrzebne.
---

# Rozszerzenia

Rozszerzenie może oferować stronę pełnoekranową, na przykład tablicę zadań. Otworzysz ją w menu Strony rozszerzeń nad listą sesji. Menu pojawia się tylko dla włączonych i zatwierdzonych stron. Wybór czatu, przeładowanie, wstrzymanie, usunięcie lub zmiana serwera zamyka stronę. Rozszerzenie nie może samo jej otworzyć.

Własne dane rozszerzenia są przechowywane na połączonym serwerze i usuwane wraz z rozszerzeniem. Tablica może śledzić sesje wielu projektów i worktree bez tworzenia plików w Twoim projekcie.

W **Ustawienia → Rozszerzenia** dodasz do OpenChamber panele zrobione przez inne osoby. Rozszerzenie to mała strona na prawym pasku, obok wbudowanych paneli. Może pokazywać dane z innej usługi, dołączać zadanie do czatu, a jeśli na to pozwolisz, uruchamiać sesje, wysyłać prompty lub czytać pliki.

Rozszerzenia działają w OpenChamber w wersji web i desktop. VS Code i aplikacja mobilna jeszcze ich nie ładują. To nie to samo co wtyczki OpenCode.

## Zainstaluj rozszerzenie

Rozszerzenia oznaczone **Wbudowane** są dostarczane z wybraną instancją OpenChamber. Początkowo są włączone i otrzymują zadeklarowane uprawnienia bez dodatkowego okna. Użyj **Wyłącz** lub **Włącz** na ich karcie. Nie można ich usuwać ani aktualizować osobno od aplikacji. Wyłączenie zachowuje dane i połączone konta, także po aktualizacji aplikacji.

Ich konta znajdziesz w **Ustawienia → Integracje → Wbudowane integracje**. Dostęp do konta w danej usłudze może nadal wymagać tokenu API lub logowania.

1. Otwórz **Ustawienia → Rozszerzenia**.
2. Wklej jedną z tych rzeczy w pole **Folder, ZIP lub URL** i wybierz **Dodaj**:
   - pełną ścieżkę do folderu na tym komputerze
   - ścieżkę do pliku `.zip`
   - link https do repozytorium git lub pliku zip
   - adres Git SSH, np. `git@github.com:owner/extension.git`
3. Jeśli rozszerzenie prosi o uprawnienia, okno dialogowe je wymienia. Wybierz **Zezwól i włącz** albo **Usuń**, żeby je wycofać.

Możesz też upuścić `.zip` na pole albo wybrać go przyciskiem archiwum obok; archiwum jest wysyłane na serwer OpenChamber, więc działa to także dla zdalnej instancji. Na desktopie przycisk folderu otwiera systemowe okno wyboru, a upuszczony folder instaluje się z jego ścieżki.

Ikona rozszerzenia pojawia się na pasku. Jeśli jej nie ma, zajrzyj do [Rozwiązywania problemów](/troubleshooting/).

Instalacja z folderu działa bezpośrednio z tego folderu, więc ktoś, kto tworzy rozszerzenie, może edytować i przeładowywać. Zip lub URL jest najpierw kopiowany do folderu danych OpenChamber; oryginalny plik zostaje nietknięty.

## Prywatne repozytoria i tożsamości Git

Instalacja SSH obsługuje `git@github.com:owner/extension.git` i `ssh://git@github.com/owner/extension.git`, opcjonalnie z `#branch-or-tag`. W menu odcisku palca obok pola wybierz tożsamość globalną lub zapisany profil Git. Wybór jest zachowany do sprawdzania i instalowania aktualizacji; zmiana klucza działa od następnej operacji.

Tożsamość globalna używa konfiguracji Git/SSH i agenta SSH użytkownika uruchamiającego wybrany serwer OpenChamber. Dla instancji zdalnej klucz musi być dostępny na tym serwerze. Klucze chronione hasłem odblokuj najpierw w agencie SSH procesu serwera. HTTPS używa poświadczeń HTTPS Git i nie przełącza się automatycznie na SSH. Hosty Git muszą wskazywać publiczne adresy; lokalne aliasy SSH i cele w sieciach prywatnych nie są obsługiwane.

## Co rozszerzenie może robić

Pokazywanie panelu i odczyt, który projekt i sesja są otwarte, nie wymaga uprawnień. Wszystko, co działa w Twoim imieniu, wymaga, a całą listę widzisz raz, przy instalacji:

- **Wysyłanie wiadomości w Twoich sesjach**: pisze i wysyła prompty do modelu, co kosztuje tokeny i może uruchamiać narzędzia.
- **Dostęp do projektów i sesji**: listy projektów, worktree i stanów sesji oraz tworzenie i otwieranie sesji w zarejestrowanych projektach. Treść rozmów pozostaje prywatna.
- **Odczyt i zapis plików projektu**: dowolny plik w otwartym projekcie.
- **Odczyt i zapis plików poza projektem**: tylko lokalizacje wymienione w oknie dialogowym.
- **Używanie twojego Small Model**: jednorazowe generowanie tekstu poza Twoimi sesjami, z modelem z Ustawienia → Sesje → Small Model. Kosztuje tokeny.
- **Łączenie z usługą zewnętrzną**: z kontem, które połączysz w Ustawienia → Integracje.
- **Czytanie rozmowy sesji**: tylko gdy uruchamiasz akcję rozszerzenia na tej sesji.
- **Uruchamianie lokalnej usługi**: osobny program z pełnymi uprawnieniami Twojego konta. Zezwalaj tylko, jeśli ufasz autorowi.

Gdy nowsza wersja prosi o więcej, niż pozwoliłeś, karta pokazuje **Wymaga zgody** i przycisk **Sprawdź uprawnienia**. Dopóki nie zezwolisz na nową listę, to rozszerzenie pozostaje wyłączone.

## Gdzie się pojawia

Poza panelem na pasku bocznym rozszerzenie może dodawać pozycje do menu, z których już korzystasz: akcje na wiadomości w czacie, menu sesji, menu + obok pola czatu oraz polecenia z ukośnikiem, jak `/task ABC-12`, które dołączają coś bez otwierania czegokolwiek. Może też nadać własnym narzędziom porządną ikonę, tytuł i układ wyniku w czacie, tak jak mają wbudowane narzędzia OpenChamber. Rozszerzenie samo decyduje, które z nich oferuje; wszystkie są wyłączone, dopóki nie zezwolisz na rozszerzenie.

Niektóre rozszerzenia pokazują obraz na żywo zamiast własnego panelu: to, w czym agent pracuje na serwerze, na przykład przeglądarkę albo symulator. Kliknij w obraz lub wpisz coś, aby przejąć kontrolę; wiersz stanu pokaże **Masz kontrolę**, a działania agenta w przeglądarce będą odrzucane, dopóki nie naciśniesz **Oddaj agentowi**. Kopiowanie i wklejanie działają jak zwykle. Inne urządzenie połączone w tym samym czasie widzi, kto ma kontrolę, i nie może jej przejąć. Takie rozszerzenie może dodać własne kontrolki obok obrazu, na przykład pasek adresu nad nim albo kolumnę narzędzi z boku.

## Połącz jego konto

Rozszerzenie, które rozmawia z usługą, dostaje kartę w **Ustawienia → Integracje** w sekcji **Konta rozszerzeń**. Zależnie od usługi karta prosi o token API, nazwę użytkownika i token albo client id do logowania. Wybierz **Połącz**; **Rozłącz** zapomina zapisany token. Dodatkowe pola zadeklarowane przez rozszerzenie zapisują się same, gdy je opuszczasz.

Token zostaje na serwerze OpenChamber. Strona rozszerzenia nigdy go nie widzi.

## Aktualizuj

Rozszerzenia zainstalowane z adresu URL git mogą aktualizować się same. Otwarcie **Ustawienia → Rozszerzenia** sprawdza każde z nich najwyżej raz na godzinę; **Sprawdź aktualizacje** sprawdza od razu. Karta z nowszą wersją pokazuje **Dostępna aktualizacja** i przycisk **Aktualizuj**. Aktualizacja zachowuje Twoje uprawnienia, konta i ustawienia; jeśli nowa wersja prosi o więcej, okno dialogowe zgody pojawia się ponownie. Instalacje z folderu i z zip nie aktualizują się: zastąp je ręcznie.

Dodaj `#v1.2.0` albo `#stable` do adresu URL git przy instalacji, żeby śledzić tag lub gałąź zamiast gałęzi domyślnej.

## Wstrzymaj lub usuń

Każda karta ma przełącznik **Włączone**. Wyłączony zostawia rozszerzenie zainstalowane, ale ukrywa jego panel i blokuje jego wywołania. **Usuń** kasuje instalację z zip lub URL z folderu danych OpenChamber; przy instalacji z folderu tylko zapomina ścieżkę, Twój folder zostaje.

## Zbuduj własne

Wszystko, co rozszerzenie potrafi, i jak je zrobić, znajdziesz w [Zbuduj rozszerzenie](/sdk/).
