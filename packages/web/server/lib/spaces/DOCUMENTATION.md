# Spaces module

Design, words, and product decisions are in [docs/isolated-spaces/DESIGN.md](../../../../../docs/isolated-spaces/DESIGN.md). Read it first. Stages are in `STAGES.md` next to it, test rules in `TESTING.md`.

This is stage 4a. The server imports the module behind one switch, `isolatedSpacesEnabled` in `settings.json`, read once at start: with the switch on it forwards requests under `/api/spaces/<id>/` to the server inside that space, see "Dispatcher"; with it off nothing of the feature exists, no place, no manager, no route, no `docker`. There is no UI yet. A space has one way out, its own gatekeeper, and nothing the host has not allowed passes through it. Since 3a the module can also bring a project's code into a space, see "Code in". Since 3b it can bring the space's work out again and apply it to the user's repository, see "Code out".

## What the module owns

Creating, finding, stopping, starting, and removing isolated spaces on a place, and proving that a created space has the restrictions it was asked for. Since stage 1b a space runs an OpenChamber server and OpenCode, started from a tools volume that is mounted read-only. Since stage 2 every space has a gatekeeper container of its own: the corridor a space reaches the internet through, the window that adds a secret the space never sees, and the journal of what was allowed and refused.

- `manager.js`: `createSpaceManager({ registry, now })`. Generates the space id, builds the spec with the default memory limit of 4 GiB, calls the place. Keeps no state file and no cache. Every answer is read from the place at call time.
- `layout.js`: what a space looks like from the inside, for every place. User, HOME, work path, tools path, the port and the token file of the server inside, the space environment, and the space command. Since 3a also the project path `/spaces/<id>/<name>` and the path of the history side repository.
- `space-server.js`: `createSpaceServerChannel({ exec, wait, now })`. Everything the host does to the server inside, written once on top of `exec`: store and read the token, link the OpenCode plugin, send an HTTP request, wait until the server is ready.
- `gatekeeper-channel.js`: `createGatekeeperChannel({ exec, wait, now })`. The same for the gatekeeper: write its program, wait until its corridor accepts, set the network mode and the allowlist, add a grant, read the journal. It also exports `GATEKEEPER_PROGRAM`, the text of the program file.
- `gatekeeper-program.cjs`: the program that runs inside a gatekeeper. One self-contained CommonJS script on Node's standard library, run by the container and by its own unit test with the same arguments.
- `exec-http.js`: the pieces the two channels share, and the only place where the dangerous details live: the curl argv, a request as a curl config, and the parser for an answer that came out of a container.
- `code-in.js`: `createCodeIn({ git, place, temporaryDirectory, removeDirectory })`. The snapshot on the host, the list of files that travel, the first-release limits, the transfer in, the history after it, and the removal of the host refs. See "Code in".
- `code-out.js`: `createCodeOut({ git, place, temporaryDirectory, removeDirectory })`. The snapshot inside the space, the quarantine fetch with its caps, the promote into the user's repository, and the two ways to apply. See "Code out".
- `code-transfer.js`: what code in and code out share: the host git session that starts nothing of the user's, the one git version floor, the work tree check, the `ext::` URL builder, and the time limits of a transfer.
- `host-git.js`: `createHostGit({ runCommand, gitPath, environment })`. Every git command the host runs against the user's repository goes through it, with the environment rule in one place.
- `tools.js`: tools sources and the content key. `createRegistryToolsSource`, `createPackedToolsSource`, `readHostToolVersions`, `toolsContentKey`.
- `tools-filler.js`: the fixed program of the filler container and the framing of its stdin.
- `tools-pack.js`: `packLocalTools` packs the local `web` and `sdk` packages of a checkout for a development build. Nothing calls it yet except tests. Deciding when a host is a development build is wiring for a later stage.
- `dispatcher.js`: `createSpaceDispatcher({ transport })`. The thin layer in front of the host's routes that forwards a request addressed to a space, and the two guards. See "Dispatcher".
- `host.js`: `createSpacesHost({ dataDir, dockerPath, gitPath, hostEnvironment, listProjectDirectories })`. What `server/index.js` builds when the switch is on: the owner id, the Docker place, the registry, the manager, the dispatcher, the gatekeeper channel, code in and code out on one host git, the records and the journey, with the hooks the rest of the server takes from it.
- `journey.js`: `createSpaceJourney({ manager, place, gatekeeper, codeIn, codeOut, records, listProjectDirectories, announce, onSpacesChanged })`. The journey of a space as the host drives it, written once on top of those: create in the background with progress, list with state, start with the network said again, stop, remove with the refs and the record, stop all for the switch, the journal, the apply preview and the apply. See "The journey".
- `routes.js`: `registerSpaceRoutes(app, { getJourney, getPlaces, readSwitch, setSwitch })`, the HTTP of the journey under `/api/openchamber/spaces`, which translates and nothing else; and `createSwitchController({ getHost, setHost, buildHost, startHost, persist })`, the switch as `index.js` runs it, one change at a time.
- `space-records.js`: `createSpaceRecords({ dataDir })`. What the host remembers about a space beside the labels: the network choice, the repository, the space path, the base of the history and whether the history arrived. One file per space under `spaces/records/`, no secret in it.
- `places/registry.js`: place registry, same pattern as the tunnel provider registry. Sealed after boot.
- `places/docker.js`: the local Docker place. `createDockerPlace({ runCommand, dockerPath, owner, toolsSource, wait, now })`. `wait` is the pause used by rollbacks and by the readiness wait. `now` stamps the tools labels and is the clock of the readiness wait. Both are injectable so tests do not sleep.
- `places/docker-tools.js`: the tools volume of the Docker place. Ensure, fill, fill rollback, pruning.
- `places/docker-engine.js`: how both of them call the docker CLI. Run, inspect, find by label, remove.
- `hardening.js`: the `docker create` argv for a space and for its gatekeeper, the argv for both networks and for the three one-shot containers, and the pure checkers that compare `docker inspect` output with them.
- `labels.js`: label keys, resource names, space ids, tools keys, label parsing.
- `run-command.js`: the only file that starts a process. Since 4a also `openCommandStream`, a command's stdin and stdout as one duplex stream that an `http.Agent` uses as a socket: the transport of `connect`.
- `errors.js`: `SpaceError` with a `code`.
- Test support, never imported by product code: `places/contract-suite.js`, `places/escape-suite.js`, `places/memory-place.js`, `places/fake-docker.js`, `places/docker-live-support.js`, and since 3a `code-in-bait.js`, the bait repository and the host state that code in must leave alone. Since 3b `code-in-bait.js` also holds the stand-in for a space in a local folder that the unit tests of both directions use, and knows what code out may change in the user's repository.

## Place contract

A place has a non-empty `id` and these operations. The registry rejects a place that lacks one.

| Operation | Contract |
|---|---|
| `check()` | Never rejects for an expected problem. Resolves `{ available: true, version, os, arch, hostIsolation }` or `{ available: false, code, message }`. The message names what the user must fix. `hostIsolation: false` means a space on this place could reach services that listen on the place's host. It is a capability, and the manager decides later what to do with it. |
| `create(spec)` | `spec` is `{ id, name, project, created, memoryBytes }`. Resolves when the gatekeeper runs with its corridor accepting connections, the space runs, both verify clean, and the server inside the space reports OpenCode ready. Rejects with code `space_name_taken` when the id is in use. A rejected create has rolled back. `details.rollbackFailures` lists what it could not remove, and `details.uncertain: true` means a step was interrupted and the place may still finish it, so the caller should show the spaces list. |
| `list()` | Resolves `[{ id, name, project, created, state, orphans, damaged, missing }]`. `state` is the state of the space container: `running`, `exited`, or `missing`, because that is what `start`, `stop` and `remove` act on. `orphans` lists `{ kind, name }` resources whose space container is gone. `damaged: true` means the container exists and `missing` names what the space needs and does not have right now: a lost network or volume, or a gatekeeper that is absent, or one that is not running while the space is. `list` only reads and repairs nothing. A place failure rejects. It never resolves an empty list instead. |
| `exec(spaceId, argv, { stdin, timeoutMs, target })` | Runs `argv` as the space user, in the space or, with `target: 'gatekeeper'`, in its gatekeeper. Resolves `{ code, stdout, stderr }` for any exit code of the command. Any other target is rejected with `invalid_exec_target`. |
| `connect(spaceId)` | Since 4a. Resolves a duplex stream that carries bytes to the loopback port of the server inside the space and back, after the same ownership and running checks as `execArgv`, so a missing, foreign or stopped space is refused with the same codes. The space's network never sees it. For Docker it is the bridge of `layout.js` run over the `execArgv` argv, see "Dispatcher". |
| `execArgv(spaceId)` | Since 3a. Resolves the argv that runs a command in the space, as the space user, with stdin and stdout attached, for a caller that has to start the process itself: git starts it for a push over `ext::`. The space only, never the gatekeeper. The same ownership check as `exec` comes first, so a missing container or one this installation did not create is refused, and a container that is not running is refused with `space_not_running`, since git would get nothing but the runtime's own words from it. The argv carries no secret, no empty argument and no control character. For Docker it is `<docker> exec --interactive --user 1000:1000 <space container>`. |
| `stop(spaceId)`, `start(spaceId)` | Keep files. `stop` stops the space and then its gatekeeper; `start` starts the gatekeeper, waits for its corridor, and only then the space. A running space is left alone. `start` resolves when the server inside is ready again. A stopped space moves to the current tools first, see "New tools at the next start". `start` is the only operation that repairs a half-done move. |
| `remove(spaceId)` | Resolves `{ removed, failed }`. Missing resources count as removed. Removing an unknown space resolves with both lists empty. |
| `verify(spaceId)` | Resolves the list of `{ check, message }` violations of the space and of its gatekeeper. A gatekeeper that is absent is the violation `gatekeeper_missing`, and every check of the gatekeeper's own is named `gatekeeper_*`. Empty means verified. |

Stage 2 talks to the server inside and to the gatekeeper through `exec` only, see "The exec channel" and "The gatekeeper". `connect` arrived in 4a for the dispatcher; the channels of stage 2 keep using `exec`.

The contract suite is fixed against being rewritten per place, not against a new stage. Stage 2 added three assertions to it and changed none: the gatekeeper target runs in a container of its own, which a hostname of its own shows and the absence of the space's work directory confirms; `exec` takes those two targets and rejects any other with `invalid_exec_target`; and the gatekeeper is gone after `remove`, which is checked against the container and not against the space record. From here on a place cannot host a space without a gatekeeper, so every later place is held to that too. `places/memory-place.js` has two containers for this reason, and the three assertions were each confirmed to fail against a place that fakes one.

Stage 4a added three assertions and changed none: `connect` gives a stream on which one HTTP request reaches the server inside this space and is answered, and that ends cleanly when the caller destroys it; a stopped space gets no stream, with `space_not_running`; and a space that is gone gets none. The memory place answers them with a loopback listener per space, and `connect` is in the registry's list of required operations.

Stage 3a added three more and changed none. The argv from `execArgv` runs a command in the very container `exec` reaches, and not in the gatekeeper, which a hostname shows. It does not check the user: a Docker space already runs as uid 1000 by default, so no check through the argv could fail, and the exact-argv test in `places/docker.test.js` is what proves `--user` (removing the flag turns it red). A stopped space gets no argv, with `space_not_running`. And a space that is gone gets no argv. All three are marked in the file. Each was confirmed to fail against a memory place that fakes it: one that built the argv from the id alone failed the first and the last, one whose argv reached the gatekeeper failed the first, and one without the running check failed the second. `execArgv` is in the registry's list of required operations, so a place without it is rejected at registration.

A new place is done when `runPlaceContractSuite` and `runEscapeSuite` pass against it.

## Labels

Labels are the only record of a space. Every container, network, and volume carries all of them. Since stage 2 that includes the gatekeeper container and the outer network.

| Label | Value |
|---|---|
| `openchamber.space` | `true`. The marker. |
| `openchamber.space.id` | 12 lowercase hex characters from `crypto.randomBytes`. |
| `openchamber.space.role` | `space`, `gatekeeper`, `setup`, `network`, `outer-network`, or `volume`. |
| `openchamber.space.owner` | Installation id, passed in by the caller. Two installs that share a Docker daemon do not see or touch each other's spaces. |
| `openchamber.space.project` | First 16 hex characters of the sha256 of the project directory. |
| `openchamber.space.name` | Display name. |
| `openchamber.space.created` | ISO time. |

Resource names are `openchamber-space-<id>-<role>[-<suffix>]`.

Tools resources belong to an owner, not to a space. They carry the marker, the owner, a role of `tools`, `tools-fill`, or `tools-check`, the created time, and two labels of their own:

| Label | Value |
|---|---|
| `openchamber.space.tools.key` | The content key, 16 lowercase hex characters. |
| `openchamber.space.tools.description` | For people, such as `web 1.24.2, opencode 1.18.31` or `development build`. |

They have no space id. `parseSpaceLabels` therefore returns null for them, and `list` and `remove` of spaces pass them by. `parseToolsLabels` reads them.

Rules the Docker place keeps:

- Labels are read from `docker inspect` JSON. The `Labels` column of `docker ps` is one comma-joined string, and a display name may contain commas.
- A name proves nothing. `remove` and `list` find resources by label filter, then check marker, owner, and id again on the inspect result. `exec`, `stop`, `start`, and `verify` inspect the container first and refuse one without this owner's labels.
- `create` refuses when any of its names exists, because `docker volume create` succeeds silently on an existing volume and would adopt a stranger's data.

## Hardening

`buildSpaceCreateArgs`, `buildSpaceNetworkArgs`, and `findHardeningViolations` in `hardening.js` belong together, and so do `buildGatekeeperCreateArgs`, `buildGatekeeperNetworkArgs`, and `findGatekeeperHardeningViolations`. Change a pair together, and add an escape test for every new restriction. The environment and the command of both containers come from `layout.js`.

| Flag | Why |
|---|---|
| `--init` | Reaps zombies and forwards signals, so the container stops cleanly. |
| `--user 1000:1000` | The agent is never root. |
| `--read-only` | The agent cannot change the image's files. |
| `--tmpfs /tmp:rw,exec,nosuid,size=256m` | A writable scratch area with a size limit. `exec` stays on because tools unpack and run binaries there. |
| `--security-opt no-new-privileges` | setuid binaries such as `su` cannot raise privileges. It is the only security option, so the engine's seccomp and AppArmor defaults stay on. |
| `--cap-drop ALL` | No kernel capabilities, and none are added back. |
| `--pids-limit 512` | A fork bomb stays inside the space. |
| `--memory N --memory-swap N` | Equal values, so swap adds nothing. A process that takes more is killed and the space keeps running. |
| `--shm-size 64m` | `/dev/shm` is memory too. |
| `--ipc private --cgroupns private` | Asked for by name. The checker requires both, and a daemon configured with `default-cgroupns-mode: host` or `default-ipc-mode: shareable` would otherwise fail every create with nothing the user can act on. |
| `--log-driver local --log-opt max-size=10m --log-opt max-file=1 --log-opt compress=false` | The space's output lands in a file on the Docker host. Without a cap the agent can fill that disk. The `local` driver refuses `max-file=1` while compression is on. |
| `--network <space network>` | The only network is the space's own. Its only other member is the gatekeeper. |
| `--mount type=volume` twice | Work volume at `/spaces/<id>`, home volume at `/home/space` with `HOME` set. `type=volume` cannot turn into a bind mount, unlike `-v`. |
| `--mount type=volume,...,readonly` once | The tools volume at `/opt/openchamber-tools`. Read-only, because every space of this owner runs its programs from it. An agent that could write there would change what the next space runs. |
| `--env` twelve times | See "The server inside a space". Never the server password, never a provider key, and never a grant's secret. The checker allows these names and the base image's own, `PATH`, `NODE_VERSION` and `YARN_VERSION`, and nothing else. |

The inner network is created with `--internal`, `--ipv6=false`, and `--opt com.docker.network.bridge.gateway_mode_ipv4=isolated`.

`--internal` alone is not enough. Measured on Docker 29.2.1: from a space on a plain internal network, a TCP connect to the network's gateway address reached a listener on the Docker host. With the isolated gateway mode the bridge has no address on the host, `docker network inspect` shows no `Gateway`, and the same connect fails. The option needs Docker Engine 28. The checker looks at the option and at the missing gateway address, so an engine that ignores the option fails verification and `create` rolls back. `check()` reports such an engine with `hostIsolation: false`.

Never present: bind mounts, `--volumes-from`, the runtime socket, `--privileged`, extra security options, shared namespaces, sysctls, another runtime than `runc`, devices, published ports, a second tmpfs, `OPENCHAMBER_UI_PASSWORD` in the container environment. The checker reports each of these. The environment is an allowlist: every variable must be one the create args set or one the pinned base image sets itself, read from the image's config and pinned in `hardening.js`. Any other variable fails verification, so it fails closed at every create and every start. That matters because OpenCode 2 turns variables into logins: a provider key under any of the 218 names its catalog lists for 226 integrations, such as `OPENAI_API_KEY`, or a key inside `OPENCODE_CONFIG_CONTENT`. OpenCode 1's `OPENCODE_AUTH_CONTENT` appears nowhere in the 2.0.15 binary. The server password is still refused by name, with a message of its own. The allowlist does not judge values, so a key added to the space environment in `layout.js` itself would pass it; the escape suite's connection check catches a catalog provider's key there, see "Tests". For the namespace modes it accepts `''` and `'private'`, which is what this engine reports for a correct container: `''` for pid, uts, and userns, `'private'` for ipc and cgroupns.

The checker demands that every mount is a volume whose name starts with this space's prefix. The mount at `/opt/openchamber-tools` is the one exception, and it has rules of its own:

- There is exactly one mount at that path. Check `tools_mount`.
- It is a volume named `openchamber-tools-<this owner>-<key>`. Check `tools_mount`.
- It is read-only, `RW === false` in the inspect result. Check `tools_read_only`.
- The inspected volume carries the marker, this owner, the role `tools`, and the key that its name holds. Check `tools_labels`. A volume with a matching name and no labels fails here.

The exception goes by destination. A tools volume mounted anywhere else, read-only or not, is reported by `mounts` like any foreign volume. `verify` does not ask for the current key, because a running space keeps the volume it started with.

`create` runs `docker create`, then `verify`, then `docker start`. A container that fails the check never runs. Confirmed on Docker 29.2.1: `docker inspect` of a created container that never started already has every field the checker reads, including `NetworkSettings.Networks` and `Mounts`. The Docker place therefore has nothing left to check after the start.

Every create makes the space, the `setup` one-shot, and the `tools-check` one-shot. A new tools volume adds the filler. The `setup` one-shot runs as root with `--network none`, `--cap-drop ALL --cap-add CHOWN`, a 128 MiB memory limit, the same log cap, and the fixed argv `/bin/chown 1000:1000 <mount points>`, then removes itself. Fresh volumes belong to root, and the space user cannot fix that itself.

Do not add `--workdir /spaces/<id>`. Docker then resets the ownership of that empty volume to root, and the space user cannot write to its own work directory. The escape suite's positive control catches this.

Known limit: a named volume of the default driver has no size limit. A space can fill the disk of the Docker host through its work or home volume. Nothing here prevents that.

## Tools volume

A space downloads nothing to start. Its programs come from a tools volume: one volume per owner and per tools content, filled once by a trusted one-shot container that can reach the npm registry, then mounted read-only into every space of that owner.

The volume is a plain npm project at `/opt/openchamber-tools`, with `package.json`, `node_modules`, and the binaries in `node_modules/.bin`. It is not a global install. A development build needs the local `@openchamber/sdk` tarball to win over the published package with the same version number. npm `overrides` in a project `package.json` does that. A `-g` install would pull the published sdk as a dependency of `web`.

Contents: `@openchamber/web`, `@opencode/cli`, and `@opencode/plugin` at the version of `@opencode/cli`.

### Sources and the key

The Docker place gets its source at construction, `createDockerPlace({ ..., toolsSource })`.

- `createRegistryToolsSource({ webVersion, openCodeVersion })` asks the registry for exact versions of the three packages. `readHostToolVersions()` reads them from `packages/web/package.json`: the version of `web`, and the version of its `@opencode/client` dependency for OpenCode. A range or a URL is rejected.
- `createPackedToolsSource({ webTarballPath, sdkTarballPath, openCodeVersion })` is a development build. Its `package.json` uses `file:` dependencies for both tarballs and `"overrides": { "@openchamber/sdk": "$@openchamber/sdk" }`, so every sdk in the tree is the local one. The tarballs are read and hashed once, when the source is made.
- Both take an optional `revision`. It changes the key and nothing that gets installed, for the day the same packages must be filled again.

The place accepts only an object that one of the two functions made.

The key is the first 16 hex characters of a sha256 over the source's canonical description, the base image reference, and the sha256 of the filler program. For the registry the description holds the exact versions, for a packed source the sha256 of each tarball plus the OpenCode versions.

The volume is named `openchamber-tools-<owner>-<key>`. Docker cannot change the labels of a volume after it made it, so "the fill finished" cannot be a label. The filler writes `/opt/openchamber-tools/.filled`, holding the key, as its last step.

A filled volume is never changed again. New content means a new key and a new volume.

Known limit: only the three named packages are pinned. Their own dependencies are still ranges, and no lock file travels with the source. Two fills of the same key, on two machines or on two days, can differ in a transitive package.

### ensure

`create` and `start` call `ensure` of `places/docker-tools.js`:

1. Inspect the volume name. If it exists, it must carry the marker, this owner, the role `tools`, and this key. Otherwise the call rejects with `tools_volume_not_ours`. `docker volume create` succeeds silently on an existing name, so a stranger's volume would otherwise be adopted.
2. Remove one-shot containers of this key that a killed CLI left behind. They would block the container name.
3. If the volume exists, read the marker with the `tools-check` one-shot: no network, the volume read-only, the space user, the hardening of the `setup` one-shot. Its fixed command is `/bin/sh -c '[ -f "$1" ] || exit 42; /bin/cat "$1"' sh <marker>`. Exit code 42, and only 42, means there is no marker: the fill was interrupted, the volume is ours by label, so it is removed and filled again. Exit code 0 with this key means filled. Exit code 0 with anything else, an empty marker included, means the content cannot be trusted: the volume is removed and filled again. Docker refuses the removal while any container mounts the volume. Then the call rejects with `tools_marker_mismatch`, and the message says in plain words what frees it: apply or discard the work in the spaces that were made with these tools and remove them. It names no docker command, because the user never needs a terminal. A restart of those spaces would not help, because a stopped container still holds its volumes. Every other exit code rejects. Measured with CLI 29.3.0: a docker CLI that cannot reach the daemon exits with 1, the same code `cat` uses for a missing file. With a plain `cat` a daemon hiccup would have removed a filled volume.
4. If the volume is absent: create it with labels, run the filler, read the marker once more as the space user.
5. After a new fill, try to remove this owner's other tools volumes. Docker refuses a volume that a container still mounts. That refusal means a space still runs on it, and it stays. Pruning never fails the call.

Two creates in one process share one `ensure` run, so one key is never filled twice at once. A failed fill does not stick: the next call tries again.

Known limit: two OpenChamber processes with the same owner are not coordinated. Step 2 of one would remove the running filler of the other.

### The filler

| Flag | Why |
|---|---|
| `--rm --interactive` | It removes itself, and its input arrives on stdin. |
| `--init`, `--read-only`, `--cap-drop ALL`, `--security-opt no-new-privileges`, `--pids-limit 512`, `--ipc private --cgroupns private`, the log cap | The same as a space. |
| `--user 0:0` | Root owns the fresh volume, so no chown and no capability is needed. Measured: `npm install` works as root with every capability dropped. npm writes files with mode 644 and 755, so uid 1000 can read and run them. |
| `--network bridge` | The one container here with a way out. It needs the npm registry. |
| `--tmpfs /tmp:rw,exec,nosuid,size=1g` | The npm cache and the tarballs live here and vanish with the container. Measured: the cache of one fill is 444 MB. Kept out of the volume, that halves its size. |
| `--memory 2g --memory-swap 2g` | A tmpfs counts as memory. |
| `--env HOME=/tmp` | npm keeps its logs under HOME, and the root filesystem is read-only. |

The command is `/usr/local/bin/node -e <program> /opt/openchamber-tools /tmp/openchamber-fill`. The program in `tools-filler.js` is fixed. Only its stdin varies: one JSON header line with the key and the name and byte length of each file, then the bytes. There is no tar library here. The program accepts plain file names only, puts `package.json` into the tools directory and every other file into the staging directory, runs `npm install --ignore-scripts --no-audit --no-fund --cache <staging>/npm-cache`, checks that `openchamber`, `opencode`, and the plugin are there, runs `/bin/sync`, and only then writes the marker through a temporary name and a rename. It flushes the file and its directory to disk before the rename and the directory again after it. The order matters. The marker vouches for everything npm wrote, and npm flushes nothing, so a marker that reached the disk before the packages did would be a broken volume that nothing repairs. A Docker machine that crashes right after a fill can still lose the marker or leave it empty. `ensure` treats both the same way, it removes the volume and fills again. Whether the order survives a real crash of the Docker machine was not tested.

The program is one line. 1a verified many special characters in an argument on Docker Desktop for Windows, but never a newline.

`--ignore-scripts` was measured with `web` 1.24.2 and OpenCode 1.18.31, whose package was then called `opencode-ai`. Three packages in that tree had an install script: `node-pty`, `msgpackr-extract`, and OpenCode's. `node-pty` ships prebuilt binaries for Linux arm64 and x64, and the terminal works without its script. `msgpackr-extract` is optional. OpenCode's package did need its script: its launcher `bin/opencode.exe` was a stub that printed "postinstall script was not run" until `postinstall.mjs` linked the OpenCode binary for this CPU and C library into its place. Since OpenCode 2 that package is `@opencode/cli`, and the filler runs `node postinstall.mjs` in `node_modules/@opencode/cli`, and no other script. It makes a hard link and needs no network. Checked again with OpenCode 2.0.15 only in part: in a filled volume the launcher is a second hard link to the binary of `@opencode/cli-linux-arm64`, which is what the script makes, and the space starts from it. The stub and the list of packages with scripts were not looked at again.

A fill has its own timeout of 30 minutes and its own output cap of 16 MB.

### Fill rollback

A failed fill leaves nothing. It removes the one-shots of this key and the volume, and rejects with `tools_fill_failed` or the code of the failed step, with `details.rollbackFailures`. After an interrupted step it waits two seconds and sweeps again, like the space rollback, and the error carries `details.uncertain: true`. The volume goes by name in both sweeps, labelled or not, because a late `docker run` makes the missing `src=` volume again without labels. This is safe for the same reason as in the space rollback: the same call found the name free, or found it ours.

A failed space create never removes a tools volume that carries labels. The space rollback removes by space id and by the five names of the space, and a tools volume has neither.

There are two by-name removals here, next to the one in "Rollback". The fill rollback removes the tools volume of this key by name, labelled or not: the same call found the name free, or found it ours, moments earlier. The second one has the same justification. Another process of the same owner with another key can prune this key's volume between this call's `ensure` and its `docker create`. `docker create` then makes the missing `src=` volume again without labels. Verification fails on `tools_labels`, and without a repair every later call would refuse that volume as a stranger's, for good. So the failure path of `create`, and of a move to new tools, looks at the tools volume that `ensure` handed to this same call as ours and filled. If it now exists with no labels at all, it is removed by name, after the failed container is gone. A volume with any label, ours or anyone's, is never touched there.

Known limit: the wedge is still reachable in two ways. The process can die between that `docker create` and the rollback. Or the rollback cannot remove the failed container, and Docker then refuses the volume too. In both cases an unlabelled volume with the tools name stays, every later call rejects with `tools_volume_not_ours`, and somebody has to remove that volume by hand.

## The server inside a space

The container command is a fixed `/bin/sh -c` line from `layout.js`:

```
while [ ! -s <token file> ]; do /bin/sleep 0.2; done; OPENCHAMBER_UI_PASSWORD="$(/bin/cat <token file>)"; export OPENCHAMBER_UI_PASSWORD; exec openchamber serve --foreground --api-only --host 127.0.0.1 --port 27600
```

`--init` stays, and with `--foreground` the container stops cleanly. The server binds to loopback inside the space. In this stage the host reaches it only through `exec`, so no listener faces the space network. An escape test reads `/proc/net/tcp` to prove that. The port is an unusual one because the agent's own dev servers share that loopback.

Environment:

| Variable | Why |
|---|---|
| `HOME=/home/space` | The home volume. |
| `PATH` with `/opt/openchamber-tools/node_modules/.bin` last | The server refuses to start without `opencode` on PATH. The image has no `openchamber` and no `opencode`, so both are found there. The image's own directories come first, so a transitive npm package that ships a bin named `node` or `sh` never shadows the image's program. |
| `HTTPS_PROXY`, `https_proxy`, `HTTP_PROXY`, `http_proxy`, all four `http://gatekeeper:3128` | The corridor. curl 7.88.1, the version in this image, ignores an uppercase `HTTP_PROXY` on purpose, the httpoxy protection, and honours the lowercase one. Without the lowercase spelling, plain-HTTP traffic from curl and from everything that uses libcurl never reaches the corridor. |
| `NO_PROXY`, `no_proxy`, both `gatekeeper,localhost,127.0.0.1` | Window traffic skips the corridor, and so does the space's own loopback. Measured: with `http_proxy` set, curl sends even a request to 127.0.0.1 to the corridor, and Node with `NODE_USE_ENV_PROXY=1` proxies loopback too. curl reads both spellings. |
| `NODE_USE_ENV_PROXY=1` | Node 22 ignores proxy variables without it. Measured: `EAI_AGAIN` for every fetch, and with it Node honours the uppercase spelling. |
| `OPENCODE_DISABLE_MODELS_FETCH=1`, `OPENCODE_DISABLE_AUTOUPDATE=1` | The catalog and the update check would spend corridor attempts on downloads a space does not need. Measured with OpenCode 2.0.15: without `OPENCODE_DISABLE_MODELS_FETCH` the journal shows `models.opencode.ai:443 deny:not-on-allowlist`, and with it the journal stays empty. The 2.0.15 binary does read `OPENCODE_DISABLE_AUTOUPDATE`, `1` or `true`, and then logs that it skipped the update check. What it changes in a space was not seen: `opencode serve` made no update attempt with it or without it. It stays because the binary honours it and it costs nothing. |
| `OPENCHAMBER_RELAY_HOST=off` | The server inside a space must never host the relay passively. It has a way out now, and paired devices must not land on a space. `server/index.js` reads this variable when it computes `allowPassiveHost`. |

`npm_config_fetch_retries=0` is gone. Stage 1b set it because a space had no route at all, and OpenCode 1.18.31's background install of its plugin package, then `@opencode-ai/plugin`, held its tool registry for a long time: with no network, the first request for a project with `.opencode/tool/*.ts` took 131 seconds. Measured again in stage 2 with `npm view opencode-ai version` from a space-like container: with no route, 70 seconds; through a corridor that answers an immediate 403, 0.3 seconds with npm's own retry settings, because npm does not retry a 403. That measurement is about npm, not OpenCode. So the corridor's refusal does the work the variable did, and the agent's own `npm install` gets its retries back. OpenCode 2.0.15 has no such install for a local plugin at all: it imports the file and either finds the package or fails at once.

The plugin must also resolve from project files. OpenCode 2 loads a project's plugins from `.opencode/plugin/` and `.opencode/plugins/`, `.ts` or `.js`, and imports each file directly. Measured with OpenCode 2.0.15 and no link: such a plugin reports `failed`, and its log says `Cannot find package '@opencode/plugin'`, with no download attempted. After the start, `create` makes `/spaces/<id>/node_modules/@opencode/plugin` a symlink to the package in the tools volume. Module resolution walks up from the project, so one link above every project is enough. Only the plugin is linked, so project code does not quietly resolve our other packages. With the link the plugin reports `active` and the plugin's own dependencies resolve from the link's real path. With OpenCode 1.18.31 a tool that used `tool.schema` was listed in 141 ms. The agent can delete the link. That hurts only itself and is not a boundary.

### Token

`create` makes 32 random bytes with `crypto.randomBytes`. After `docker start` it writes them over the stdin of an `exec` into `/home/space/.openchamber-space/token`: directory mode 0700, file mode 0600, through a temporary name and `mv`, so the waiting server never reads half a token. The token is in no argument on the host, not in the container environment, not in a label, and not in anything `docker inspect` shows. An escape test checks the full inspect output.

The honest limit: the agent runs as the same user. It can read the token file and the environment of the server process. The token is not a secret from the agent. It keeps the server closed to anything else, and it stays out of container metadata.

The host keeps no copy. `readToken` reads it back over `exec`. `stop` and `start` keep it, so the server comes up by itself. **Decided in 4a: the host does not issue a fresh token at its own start.** The token is read back over `exec` at the first request to a space and kept in the host's memory for the life of the process, and read again only when the server inside refuses a session made from it. A fresh token would need a restart of the server inside, which ends whatever the agent is doing there, and buys nothing: the token protects the server inside from others and never from the agent, who can read the file, and the file and the host's copy both live where only the user reaches them. A rotation belongs with the repair actions of a later stage, where restarting the server inside is what the user asked for.

### The exec channel

`createSpaceServerChannel({ exec, wait, now })` in `space-server.js` works for every place, because it needs `exec` only. `wait` and `now` are the pause and the clock of the readiness wait.

- `request(spaceId, { method, path, headers, body })` runs `/usr/bin/curl --disable ... --config -` inside the space against loopback and resolves `{ status, headers, body }`. The whole request is a curl config on stdin. A cookie or a token in a header therefore shows up in no argument list, on the host or inside. `curl` is part of the base image.
- `waitUntilReady(spaceId)` polls `/health` until it answers 200 with `isOpenCodeReady: true`. Each attempt has a curl time limit of 3 seconds, attempts are half a second apart, and no attempt begins later than 120 seconds after the wait began, by the injected clock. The wait can therefore overrun 120 seconds by the length of one attempt, at most 13 seconds. Then it rejects with `space_server_not_ready` and names the seconds that really passed. When `exec` itself fails, for example because the container is gone, it rejects at once. `docker exec` reports that with exit code 1, 125, 126, or 127. curl uses exit code 1 too. Measured with curl 7.88.1: a listener that answers `hello` gives exit code 1 and `curl: (1) Received HTTP/0.9 when not allowed`. curl starts its error lines with `curl:`, so one of those codes counts as "exec failed" only when stderr does not start like that. In `create` that failure rolls the space back and keeps the tools volume.

Everything that comes out of a space is untrusted data. When a stopped space starts, agent code can run before the server does and can own the port. The channel is written for that:

- `--disable` is the first curl argument, so curl never reads the `~/.curlrc` of the space user. Measured: a `write-out` line there injected text into the output the host parses. An escape test repeats that.
- The body travels as `data-raw`. `data-binary` reads a body that starts with `@` as a file name.
- Header names go into an object without a prototype, so `__proto__` and `constructor` are names like any other. The host parses at most 64 KiB and 200 lines of headers and answers `space_server_answer_unreadable` beyond that. Values are pushed into one array per name. The first draft copied the array for every line, and 160,000 repeated lines blocked the host's event loop for 38 seconds.
- A request whose `exec` timed out or was killed becomes `space_server_unreachable`, which the readiness wait counts as "not ready yet". No Docker step was interrupted there, so `create` must not answer "Docker may still finish the interrupted step".
- `readToken` has a time limit of 10 seconds and accepts 32 to 128 characters of base64url only. The agent owns the token file and can make it huge, empty, or a FIFO. All of that becomes `space_token_unreadable`.
- The host names every program it runs in a container by its absolute path in the pinned image: `/bin/sh`, `/bin/cat`, `/bin/chown`, `/usr/bin/curl`, `/usr/local/bin/node`. The constants are in `layout.js`, and the paths were checked in the image with `command -v`. Every fixed script that the host sends through `exec` starts with `PATH=` set to the image's own directories. The space command and the marker check use shell builtins and absolute paths instead, with one exception: the space command ends in `exec openchamber`, which the space PATH finds in the tools volume, the only place that has it. The filler's own `npm` call keeps the bare name: the filler's PATH is the image default and does not include the volume it fills.
- A body that is `null`, not JSON, or anything else than an object with `isOpenCodeReady: true` counts as "not ready yet". So does an answer that is not HTTP, any curl error, and more output than the runner accepts. That last one becomes `space_server_answer_unreadable`. It is never `command_output_too_large`, which the rollback would read as "Docker may still finish the step".
- A listener that accepts and never answers costs 3 seconds per attempt, and the deadline still ends the wait. With 20 seconds per attempt and a count of 240 attempts it would have held `start` for 82 minutes.
- Error messages carry at most 2,000 characters of text from inside.
- No answer from inside makes the channel throw anything but a `SpaceError`.

Measured: from the `create` call to a healthy server takes 2.3 seconds when the tools volume is filled, and 38 to 46 seconds with a first fill.

## The gatekeeper

Every space has one, and it is the only way out. The space keeps exactly one network, its inner one, and the gatekeeper is the other member of that network. The gatekeeper is also on an outer network of this space alone, an ordinary bridge, and that is where its own traffic goes. Nothing else joins either network.

```
inner network (internal, no gateway)      outer network (ordinary bridge)
   space  ──────────►  gatekeeper  ──────────►  the internet
                       corridor 3128
                       window   8080
                       control  127.0.0.1:9099
```

Measured on Docker 29.2.1, Colima, linux/arm64, with the pinned image:

- A container on both networks works as the design wants. The space keeps one route, its own subnet, with no gateway, and still gets `ENETUNREACH` for a public address.
- Docker's embedded DNS resolves the gatekeeper's network alias from inside the space, and public names still do not resolve there (`EAI_AGAIN`). So on Docker the gatekeeper runs no DNS server: the space resolves container aliases and nothing else, and the gatekeeper resolves public names itself when it opens a tunnel. A place where alias resolution does not work, such as Apple `container` on a host-only network, has to bring its own answer.
- A listener on the gatekeeper's `127.0.0.1` is refused from the space (`ECONNREFUSED`), while the same listener on `0.0.0.0` answers. That is why the control channel is a loopback listener, reached over `exec`, and invisible to the space.
- The gatekeeper can reach the Docker host: a TCP connect to the outer bridge's gateway address and to `host.docker.internal` both succeed from it. This is the whole reason the corridor refuses by address. A corridor that let private, loopback, link-local and metadata addresses through would hand the space everything stage 1a shut.
- On an isolated network Docker gives the first container the `.1` address. The gatekeeper is created first, so `.1` on the space's subnet is the gatekeeper and never the Docker host. An escape test must not read a connection to it as an escape.

### The gatekeeper container

`buildGatekeeperCreateArgs` and `findGatekeeperHardeningViolations` belong together, exactly as the space's pair does. The gatekeeper gets everything the space gets: `--init`, `--user 1000:1000`, `--read-only`, a size-limited `nosuid` tmpfs, `--security-opt no-new-privileges`, `--cap-drop ALL`, `--pids-limit 512`, equal `--memory` and `--memory-swap`, `--shm-size 64m`, `--ipc private --cgroupns private`, the same log cap, no bind mount, no `--volumes-from`, no runtime socket, no sysctl, no device, no published port, and none of the forbidden environment variables. Three things differ, and the checker demands each of them:

| Difference | Why |
|---|---|
| Two networks, this space's inner one and this space's outer one, by name, and no others. The checker also demands the alias `gatekeeper` on the inner one. | The inner network is where the space reaches it, under that name and no other. The outer one is its way out. Any third network would be a way around this pair. |
| No mount at all. The checker demands zero mounts. | Its only writable place is its tmpfs, which is gone when the container stops. It holds no volume, so nothing it holds survives it, and there is no shared volume for an agent to read. |
| `--memory 256m --memory-swap 256m`, and the checker demands that exact value. | Measured: an idle gatekeeper with its three listeners holds about 24 MiB. 256 MiB is ten times that. The tunnel cap is 64 and two sockets with their buffers cost well under a megabyte each, so a gatekeeper at its busiest stays far below the limit, and a leak is still capped. A space's 4 GiB would be for an agent, and this is not one. |
| `--tmpfs /tmp:rw,noexec,nosuid,size=16m` where a space gets `rw,exec,nosuid,size=256m`. | One file lives there, the program, and Node reads it. Nothing runs from it. |

Its environment is `HOME=/tmp` and nothing else: the root filesystem is read-only, and a program that resolves HOME must land somewhere writable. No secret is ever in the environment, in an argument, in a label, or on disk. A grant arrives on the stdin of an `exec` and lives in the program's memory.

The outer network is created with `--driver bridge --ipv6=false` and the labels of the space. It is never `--internal`: that is the corridor's way out, and the checker reports an internal one.

Docker refuses `--network-alias` together with a second `--network`, so `create` makes the container on the inner network with the alias and then runs `docker network connect` for the outer one. Both happen before the verification, and the verification before the start.

### The program

`gatekeeper-program.cjs` is one self-contained CommonJS script on Node's standard library. It is far larger than the tools filler, and stage 1a only ever verified a 2,641 character `node -e` argument on Windows, so it does not travel as an argument. The container's command waits for the file in its tmpfs and then execs Node on it, the same shape as the space's wait for its token file:

```
while [ ! -s /tmp/openchamber-gatekeeper/gatekeeper.cjs ]; do /bin/sleep 0.2; done; exec /usr/local/bin/node /tmp/openchamber-gatekeeper/gatekeeper.cjs 0.0.0.0 3128 8080 9099 300000 128 64 8
```

The host writes the file over `exec` on stdin, through a temporary name, so the waiting container never runs half a file. `docker cp` is not an option: measured, it fails with "container rootfs is marked read-only" against a read-only container, even for a tmpfs destination.

Its size crosses no boundary, and the question is worth answering once rather than every time it grows. It travels on the stdin of `docker exec --interactive` and never as an argument, `run-command.js` writes it and ends that stdin with the pipe's own backpressure, so no single write of ours can block or truncate, and it is in any case under the 65,536-byte default pipe buffer on Windows. The 2,641 characters stage 1a verified were the limit of a `node -e` argument, which is exactly what this path avoids.

The bind address, the three ports, the window's deadline and the three connection caps are arguments, so the container and the unit test run the very same program. There is no switch in it that weakens a rule: every argument is a length or a place, the container command carries the production numbers, and a test asserts that command whole.

The caps are arguments because a test has to exceed one, and exceeding a cap is not something a test can do reliably at 128. A drop happens when a connection is *accepted* past the cap, and a machine under load accepts slowly while the kernel's backlog keeps completing handshakes. Measured: with the program stopped while 300 clients connected, all of them reported themselves connected, 128 were queued in the backlog, none was accepted past the cap, and no note was ever written — the flood was invisible to the journal by construction. At a cap of 4 that cannot happen.

`gatekeeper-channel.js` reads the file with `readFileSync(new URL('./gatekeeper-program.cjs', import.meta.url), 'utf8')` and hands the text to the container. `packages/web` ships `server/` as plain files with no build step, so this works in the published package as it does in a checkout.

Everything the space sends is hostile input. A malformed request line, an absurd header block, a huge body, a slow client, a reset in the middle of the handshake: none of it may end the process, because a crash of the gatekeeper is a denial of service the agent can trigger whenever it likes. Every socket has an error handler, the HTTP servers answer a `clientError` on that one socket and destroy it, the header block is capped at 16 KiB, and headers must arrive within 10 seconds. After the three listeners are up, an uncaught exception is written to stderr and the process keeps running. That line holds the error's name, its code when it has one, and the frame where it happened, and no message text: a message can carry what the space sent, a header value or a piece of a body among it, and the container log is not a place for that. Before the listeners are up, one that cannot bind still ends the process with a message.

### Corridor

An HTTP CONNECT proxy on the space-facing interface. Stage 0 proved that OpenCode, curl, npm and git all use CONNECT for https, so a plain HTTP proxy request is answered 403 and journalled, with the destination host and without the path.

- **The corridor takes names and never addresses, in both modes.** A literal address walks past every rule that is about a name, and that is not a theory: with the address rules alone, `auth.openai.com` was refused and its address was not, and a TLS handshake through that tunnel reached the real service. The space has no public resolver of its own, but in open mode it can ask a public DNS-over-HTTPS resolver through this same corridor and then connect to what it learns, which is two CONNECTs and nothing the base image lacks. Every CONNECT a space makes on purpose is already by name, so nothing it needs is lost. Comparing the resolved addresses of the refused names instead would be unreliable in both directions, because a CDN's answers rotate.
- Two modes, set by the host and changed live with no restart: `allowlist` and `open`. In `allowlist` mode a name passes only by an exact match, read in any case and with a trailing dot stripped. No wildcards, no suffix matching: `sub.example.com` does not match `example.com`. The list holds names, and it is checked with the same predicate a target passes, so the host can never put an entry on the list that the corridor would then always refuse. In `open` mode any name passes the name check.
- Port 443 only, in both modes, for two reasons. A space that could reach any port could attack a third party from the user's address: a port scan, an SSH brute-force, mail abuse, with the user as the apparent origin. Data can leave over 443 whatever anyone does here, but attacking a service that does not listen on 443 cannot, so this is a real restriction and not a speed bump. And it costs almost nothing: services on other ports are overwhelmingly private ones, internal registries and internal git, which the private-address block already refuses in both modes at every port, while public HTTPS is essentially all 443. When a port is genuinely needed, the intended route is an explicit `address:port` the user opens in the grant dialog, which belongs to the UI stage. Do not solve it again by loosening a mode.
- `auth.openai.com` is refused in both modes, whatever the allowlist says, and the window refuses it as an upstream too. What that is worth differs by mode, and the honest statement is three lines:
  - The guarantee is that the user's long-lived refresh token never enters the space. That is what protects their login, and it holds in both modes.
  - In allowlist mode `auth.openai.com` is unreachable.
  - In open mode reaching it is only made harder, not prevented: the space reaches every public host on 443, so it can go through a third-party intermediary, a public proxy, a relay or a mirror, and the corridor sees only that intermediary's name. No rule about names or addresses closes that.
- **In both modes, the refusal is by resolved address.** The corridor resolves the name itself and refuses when any answer is loopback, private, link-local, carrier-grade NAT, multicast, reserved or a cloud metadata address: `0.0.0.0/8`, `10.0.0.0/8`, `100.64.0.0/10`, `127.0.0.0/8`, `169.254.0.0/16`, `172.16.0.0/12`, `192.0.0.0/24`, `192.168.0.0/16`, `198.18.0.0/15`, `224.0.0.0/4`, `240.0.0.0/4`, and for IPv6 `::`, `::1`, `fc00::/7`, `fe80::/10`, `ff00::/8`, plus the IPv4 inside `::ffff:0:0/96`, `64:ff9b::/96` (NAT64) and `2002::/16` (6to4). A target that reads as an address never gets this far: the corridor takes names only, and "a name" means a domain name whose last label is neither a number nor hex. That is what closes the spellings `getaddrinfo` takes and four dotted octets do not describe, `1746020849`, `0x01010101`, `1.1.257`, `0x1.0x1.0x1.0x1` and `0177.0.0.1` among them. Leaning on the address check for those would have been wrong: it only refuses private ranges, and a public address in one of those spellings went straight out, which is how a reviewer reached a refused host through this corridor with a real TLS session.
- **The connection goes to the address that was checked.** The name is never resolved a second time. That is the rebinding defence, and it is one line in the code with the check right above it.
- Among the checked answers, IPv4 comes first, whatever order the resolver gave them. The outer network is created with `--ipv6=false`, so an IPv6 address there is a connection that cannot be made, and a resolver that answered AAAA first would turn a perfectly allowed host into a 502 that names nothing. An IPv6-only host is still attempted and fails with a message that says so. This changes which checked address is used and never whether it was checked.
- A refusal and a connection that did not happen are different answers, and both say which they are, and no more than that. The corridor refuses with `x-gatekeeper-reason: refused`, under a status that says what kind of refusal it was: 403 for a target it will not take, 400 for a request line it cannot read, 503 when the tunnel cap is full; a host it allowed and could not reach answers 502 with `x-gatekeeper-reason: unreachable`, and that attempt is journalled as `failed:<error code>` next to the `allow` that came before it. Without the difference, an unreachable host reads as a refused one in the user's view.
- Why it refused stays in the journal and is never told to the space. Sorting `deny:blocked-address` from `deny:unresolved` would let an agent walk a list of names and map the user's internal DNS without reaching any of it, and telling `deny:not-on-allowlist` from `deny:not-a-name` would tell it which mode it is in.
- Sockets are capped as well as tunnels: 128 connections on the corridor, 64 on the window, 8 on the control channel, each an argument of the program with those numbers in the container command, and the window's own sockets out are capped at 64 by an agent of its own. `MAX_TUNNELS` counts established tunnels only, so without a socket cap a space could hold thousands of connections in the ten-second header window and have the process killed for its memory. Egress fails closed when that happens, but the journal lives in that memory, so the space would erase the record of everything it tried. The corridor's cap is twice the tunnel cap, which leaves room for every handshake a working space makes. A connection refused by the cap, and a request the HTTP parser threw out before any rule saw it, are journalled once per listener and per kind per minute: five hundred notes would erase everything else the record holds, and no record at all would let a space probe invisibly with targets the parser rejects.

  That limit has a cost worth naming, because the journal is the user's only view: a second flood inside the same minute leaves no mark of its own. Two answers were considered and neither was taken. Putting something in the limiter's key that separates one flood from another gives the space a way back to unbounded notes, which is the denial of service the limiter exists to stop. Protecting notes from eviction by ordinary records answers a different problem: measured, the journal held 190 records when a flood went unrecorded in CI, so nothing had been evicted at all. What would actually close the gap is a count carried by the note — one note a minute saying how many connections it stands for — and that changes the shape of a record the host reads back, so it belongs with the stage that puts the journal in front of a user, where a count is what they would read.
- A tunnel that is established is torn down the way TCP means it: a close on one side is passed on to the other as a close, once what is buffered has gone out. Destroying both sockets instead is how a proxy truncates a download. It takes both directions at once to see it: with the space still uploading, the socket to it has unread bytes, a destroy becomes a reset, and the kernel throws away the send queue with it. Measured with the old teardown, four runs of a four-megabyte answer to a reader taking 16 KiB every 15 ms lost between 1,114,112 and 1,572,864 bytes; with this one, four runs delivered all 4,194,304 bytes with equal hashes. One direction alone does not show it, which is why the first test written for this defect passed against the defect.
- A death is not a close, and `pipe` does not carry one. When one socket is destroyed, pipe unpipes its peer, the peer goes paused, and a paused socket never learns that the other end is gone: the second close never arrives and the place stays taken until the idle timeout, five minutes later. So each side tells the other to go, with `destroySoon`, which writes out what it holds first. Measured before that: 20 tunnels whose clients were destroyed left 20 places taken with nothing alive at either end, and rounds of it reach the cap.
- The connection out is made with `allowHalfOpen`, so a close travels in each direction on its own. Without it Node ends our side to the upstream as soon as the upstream sends FIN, and measured against the real program, an upstream that half-closed and kept reading never received what the space sent next. What it costs is that a genuinely half-open tunnel holds its place until one side closes or the idle timeout ends it, and the tunnel cap bounds how many of those a space can hold.
- A refused client's socket is let go, not merely answered. An `http.Server` socket allows half-open, the request timeouts stop applying once a CONNECT has been handed to us, and the idle timeout belongs to a tunnel that in this case never exists. Measured before that: 140 clients that read their refusal and held on filled the connection cap at 128, and the corridor then refused everything for as long as they held, with nothing open anywhere.
- Letting it go means draining it and waiting for the client to finish, not closing on the spot. Closing a socket with unread bytes still in its receive queue sends a reset, and a reset throws away what the client has received and not yet read: measured against a build that closed at once, a client that wrote a megabyte after its CONNECT lost its 403 in three runs out of five, and at four megabytes it was a coin toss. The refusal stands either way, and nothing gets through; what is lost is the one line that tells the space why. So the socket is resumed and closed when the client ends, and at most 16 refused clients are ever waited on at once, against a cap of 128: past that the oldest is closed, which costs it a readable reason and costs the corridor nothing. A timer instead of that bound would not do, because a space can open refusals as fast as the timer expires.
- An error on the connection out answers 502 only while the socket still belongs to the proxy. Once a tunnel carries bytes, writing `HTTP/1.1 502` into them would put proxy protocol in the middle of somebody's TLS stream, and a reset halfway through a download would reach the space as a record error instead of a reset. The `failed:<code>` record is written either way.
- An error, a timeout, or a client that leaves before the tunnel exists still destroys both sockets at once, which is the right answer there because nothing is in flight.
- A tunnel is closed after 300 seconds with no byte in either direction, and 64 tunnels may be open at once. A 65th is refused and journalled. A model answer streams and a long turn can be quiet for minutes, so the idle limit is generous on purpose; the cap is what the space cannot lift. A client that leaves while its name is being resolved frees its place at once, on `end` as well as on `close`: a socket the corridor has not started reading from stays half-open after the client's FIN, and without that the space could hold every place in the corridor by opening connections and walking away. Measured before the fix: a place stayed taken for the full 15 second connect timeout.

### Window

A reverse proxy on the same interface, for services whose secret must not enter the space. `/model/<grant id>/<rest>` goes to that grant's upstream with `<rest>` appended to the upstream's path unchanged, and with the grant's own header set: `Authorization: Bearer <key>` for OpenAI-style APIs, `x-api-key: <key>` for Anthropic. Whatever the space sent as `authorization`, `x-api-key` or `proxy-authorization` is removed first. An unknown grant id is 403 and journalled. A grant id is never guessable from inside, and it is treated as public anyway: nothing relies on it being secret.

A grant opens a place on a host, not the host: a rest of the path that climbs out of it is refused with `deny:path-walks-out`, because with git and a private registry the path is what says which repository or which package. It is the same host and the same credential either way, so this is a door closed before those windows land rather than an escape today.

Looking for a literal `..` is not enough, and a rule that only did that was measured letting `%2e%2e`, `.%2e`, `..%2f`, `..%5c` and `..;` through to an upstream that echoed what it received. The rest reaches the upstream verbatim, and plenty of servers decode before they resolve a path. So the rest is decoded until a round decodes to itself, and every round is split on `/`, `\`, `;`, `?` and `#` before `..` is compared; a rest that will not decode is refused too. A fixed point rather than a fixed number of rounds, because a bound on the rounds is a hole at the round after it — at three, a four-times-encoded `..` went through verbatim — and the loop ends on its own, since every round that changes anything replaces a three-byte escape with one byte. Refusing every rest that carries a `%` would be simpler and would be wrong: a scoped npm package is fetched as `/@scope%2fname`, and that window is half the reason the rule exists.

The window has a deadline of its own: no byte from the upstream for 300 seconds and the request is over, with a 504 and a `failed:timeout` record. The same number as a tunnel's idle limit and for the same reason, that a model answer streams, so what matters is silence rather than how long an answer takes. Measured before it existed: 64 requests to an upstream that accepted and never answered held every slot the window has, in and out, and nothing recovered. The space cannot choose a grant's upstream, so this is about one provider going quiet. The deadline is the program's fifth argument, 300000 in the container command, so that a test can watch one pass without waiting five minutes; it is a length, not a decision.

The window is written for any upstream and any header name, because git over https and a private npm registry go through the same mechanism later. Only the model-provider path is wired and tested in this stage.

The upstream may be `https`, and stage 0 only ever proved a plain-HTTP one. TLS to the upstream is tested live: a grant that points at a public HTTPS endpoint answers through the window, with no credential of anyone's.

The window applies no private-address rule to a grant's upstream, on purpose. A grant is the user's decision, delivered over the control channel, and a private npm registry on the user's own network is exactly the case the design wants later. The space cannot choose an upstream, only a grant id that the host has already set. The one refusal is the gatekeeper itself: any of its own addresses, not only loopback, on one of its three ports. The corridor and the window bind every interface, so the address a space reaches them on is the one on the inner network, and a grant pointing there would feed the window into itself until its 64 sockets out were gone. The consequence is a rule the host must keep: never build a grant out of anything a space said.

### Control

An HTTP listener on the gatekeeper's own loopback, which the space cannot reach. It takes, from the host only:

| Request | Meaning |
|---|---|
| `GET /health` | Answers 200 once all three listeners are up. It is the last to bind, so an answer means the corridor accepts connections. |
| `POST /network` | `{ mode, domains }`. Replaces both. |
| `POST /grants` | `{ id, upstream, header, secret }`. Adds or replaces one grant. |
| `GET /journal` | The records and how many were dropped. |

Anything else is 404, and a body that is not an object is 400. Grants and the allowlist live in the program's memory only.

`createGatekeeperChannel({ exec, wait, now })` in `gatekeeper-channel.js` is the host's side, built on `exec` alone so it works for every place, and written with the same discipline as `space-server.js`, which it shares `exec-http.js` with: `--disable` first so curl never reads a `~/.curlrc` from inside, `--noproxy '*'` on every request, the whole request as a curl config on stdin so no secret reaches an argument list, capped header parsing into a prototype-less object, bounded error text, a wall-clock readiness deadline of 60 seconds, and no answer from inside able to make it throw anything but a `SpaceError`.

The journal comes back as data: known fields only, at most 1,000 records of at most 256 characters each, and a body over 1 MiB, a body that is not JSON, or one without a record list is an error and never an empty journal.

### Journal

A ring buffer of 500 records in memory. One record per attempt: the time, which listener, the destination host and port, and the decision. The host is cut to 256 characters and the decision to 64, the same caps the host channel reads back with, so a full ring always fits in one answer. Without the cut on the host a space could make its own journal unreadable: a plain proxy request carries its target in the request line, and the request line has to fit the 16 KiB header cap, which leaves room for a host of 16,370 characters. Measured with the cut removed: 63 such requests took the journal to 1,054,537 characters, past the 1,048,576 the channel reads. With the cut in place, a full ring of 500 of those records is 180,527 characters, 17 percent of the read cap. It would then hold it that way by refilling the ring, and the journal is the user's only view of what the agent tried. Never a path, never a query, never a body, never a header value, never a secret. When the buffer is full the oldest record goes and `dropped` counts it, and the host reads that count with the records, so a caller can say that it is not looking at the whole story.

The decisions are `allow`, `failed:<error code>` for a host that was allowed and could not be reached, `deny:not-a-name`, `deny:not-on-allowlist`, `deny:port`, `deny:always-refused`, `deny:blocked-address`, `deny:unresolved:<reason>`, `deny:too-many-tunnels`, `deny:too-many-connections`, `deny:malformed`, `deny:unreadable-request` for a request the HTTP parser threw out, and `deny:not-connect` for the corridor, and `allow`, `failed:<error code>`, `deny:no-grant`, `deny:path-walks-out`, `deny:own-listener`, `deny:always-refused`, `deny:unresolved:<reason>`, `deny:too-many-connections` and `deny:unreadable-request` for the window.

### Lifecycle

- `create` makes both networks, the volumes, the ownership one-shot, then the gatekeeper: create, connect the outer network, verify, start, write the program, wait for the corridor. Only then does it make the space container. The space's proxy therefore works from its first moment, and the gatekeeper is the first container on the inner network, which is why it holds `.1` there.
- A new gatekeeper allows nothing. Mode `allowlist` with an empty list is where it starts, so a space never reaches further than the host has said, including in the window between a start and the host's first `setNetwork`.
- `stop` stops the space and then the gatekeeper. `start` starts the gatekeeper, waits for its corridor, and only then starts the space, whether the space starts as it is or moves to new tools first. A space is never running while its way out is not under the host's control, and a failed stop of the gatekeeper leaves the space stopped, which is the safe side of that order.
- A `start` that races a `stop` can leave the space running with its gatekeeper stopped, because `stop` waits for a start of the same space and then stops the space first. That direction is the safe one: the space has no way out at all while its gatekeeper is down, `list` reports it as damaged, and the next `start` brings the gatekeeper back. It is written here so that nobody has to find it twice.
- `start` looks after the gatekeeper even when the space container is already running, and that is the one thing it does to a running space. "A running space is never touched" is about the space's own container and its tools. A gatekeeper can die on its own, from its memory limit among other things, and `list` reports such a space as damaged; without this, the one action a user would take would skip the only step that repairs it, and only a stop and a start would help.
- The program lives in a tmpfs, so it is gone after every stop, and `start` writes it again. Grants and the allowlist are gone with it and are not restored here: the host says again what this space may reach. DESIGN.md already asks for that, as "needs access" after a machine restart. No container of ours has a restart policy, so a Docker machine that comes back up leaves both containers stopped and the next `start` brings them up in this order.
- The move to new tools does not touch the gatekeeper. It mounts no tools volume, so a new tools version changes nothing about it, and the move remakes the space container only.
- `remove` removes both containers, both networks and the volumes, by label, containers first. `create`'s name check covers the two new names, and so does the second by-name sweep after an interrupted step.
- A space whose gatekeeper container is gone does not start, and neither does one that is already running: `start` rejects with `gatekeeper_missing` and changes nothing. Known limit: the gatekeeper is not made again, although it holds no state, so such a space has to be applied or discarded and made again. Making it again would be a repair path with a rollback of its own, and stage 2 does not build one.
- `verify` covers both containers and both networks, and the manager's rule that a space which cannot be verified is never handed out therefore covers the gatekeeper: an absent gatekeeper is the violation `gatekeeper_missing`, and the manager removes the space.

### Known limits

- The corridor and the window listen on every interface of the gatekeeper, because the space's network is the point of them. **On Linux that includes the user's own machine.** A Linux Docker host routes into the bridge subnet, so any local process can reach both listeners without a published port. Measured on Debian 13, amd64, Engine 29.8.1: from the host, `curl http://<the gatekeeper's outer address>:8080/` connected, and reading the socket returned the window's answer. On macOS and Windows a virtual machine stands between the host and the bridge, which is why a Mac-only run could never have shown this.

  The space cannot use it: it still cannot reach the host, and that is what the escape tests prove. What it means is that an unprivileged local process on a Linux desktop could spend the user's model key once a real one sits behind the window. Nothing in this stage puts one there, and the stage that first does has to close it. Two ways are on the table, neither chosen here: a secret the host hands the window together with the grant, which every request must carry, or binding the window to the space's inner network address alone. The second needs no change to the create order, whatever an earlier draft of this paragraph said: the bind address is an argument of the program, and the program arrives over `exec` after the container is running, when Docker has already given it both addresses. `STAGES.md` carries this as an obligation on that stage.
- Anything else attached to the space's outer network could use the two listeners as well. The place attaches nothing else, and the escape suite's stand-in upstream is the only exception, in tests.
- A tunnel the upstream half-closed stays open, because a close travels in each direction on its own. Measured: 64 of those with live clients means the 65th is refused, and all 64 places come back as the clients let go. That is the tunnel cap doing its job on genuinely open tunnels rather than a leak, and the idle limit ends one that goes quiet.
- Port 443 only. A tool that needs another port cannot be reached until the grant dialog can open one.
- The corridor takes names, so a tool that only knows an address, and that the user cannot give a name, cannot be reached either.
- A single-label host, `localhost` among them, is refused. Closing the integer and hex spellings meant demanding a name with a real last label, and a name with no dot at all cannot have one. Nothing in a space needs it: its own loopback does not go through the corridor.
- A host with an underscore in it is refused, for the same reason: the name rule is the allowlist's, and that one takes letters, digits and hyphens. Such names are not valid hostnames, and the ones that exist are service records rather than things a space connects to.
- A grant's upstream is not checked against the private-address block, on purpose, because the user's own registry is a legitimate upstream. See "Window" for the one refusal that does apply and the rule it puts on the host.
- In open mode, refusing `auth.openai.com` is a lock and not a guarantee. See the corridor's rules.
- The gatekeeper is not made again when it is gone.
- The journal is memory only. A restart of the gatekeeper loses it, and so does a host that never reads it between two starts.
- Grants and the allowlist have to be set again after every start of a space.

## Dispatcher

The design is in DESIGN.md, "Dispatcher, sessions, events". 4a built HTTP and SSE forwarding under `/api/spaces/<id>/`, the two guards, and the route rules; 4b added the WebSocket forwarding, see "Sockets" below, and the merged session list with the event connection of every space, see "Sessions and events". 4c made the UI address a space through the prefix; the list the sidebar reads per project is the host's alone, and the global list, without a directory, carries the spaces.

### The switch

`isolatedSpacesEnabled` in `settings.json`, a boolean, false by default, in the settings registry as an instance field with no UI binding. `server/index.js` reads it once at start: when it is true it makes the host of `host.js`. The place of the dispatcher in the middleware chain, after the `/api` auth gate and before every feature route and the OpenCode proxy, and the `upgrade` handler of the server are each a slot mounted once, which reads the host at call time and passes on when there is none; the body parsers and the directory gate read their hooks from the host at call time as well. So while the switch is off none of the feature exists: the module is imported and nothing of it runs, no `docker` is started, no file is written, and a request under `/api/spaces/` falls through to the host's own routes and the OpenCode proxy, which know nothing about it. A request that names a space's directory without the prefix runs on the host as any other while the switch is off: the guards belong to the feature, and the feature is absent.

Since 5a the switch changes live, through `GET` and `PUT /api/openchamber/spaces/switch` in `routes.js`, the one route of the feature that exists while it is off, because turning it on is what makes the feature exist (decision 19). `GET` answers `{ enabled, spaces }`, the spaces with their id, name and state while it is on and an empty list while it is off, so the banner of decision 18 can say what turning it off would stop; while off it runs nothing. When the place cannot be asked, Docker being down among the reasons, `spaces` is null with a `failure` beside it, so the switch stays reachable and the screen can say that the list is unknown rather than showing an error where the switch was. `PUT { enabled: false }` stops every running space, one by one, writes the setting, closes the host and takes it out of the slots; the answer carries `stopped` and `stillRunning`, a space that could not be stopped named with its code and never counted as stopped. A creation under way refuses the turn-off with `space_preparing`, and from the first stop on the journey takes no new creation and no start, `isolated_spaces_off`, so nothing slips in between the stops and the moment the feature is gone; a setting that cannot be written leaves the feature on and the journey open again. `PUT { enabled: true }` writes the setting, makes the host and starts its event connections, exactly what the next start would do; when the host cannot be made, the setting is written all the same and the answer is the failure, as a start with the switch on logs it. The controller runs one change at a time: a second request waits for the first and sees what it left, so a double click builds one host and closes one host, never two. The settings toggle of 4c still writes the setting directly and says that a restart is needed; the settings screen that calls this route is a later part of stage 5.

### What is forwarded

A request goes to a space when its path starts with `/api/spaces/<id>/` and the id is one of this host's spaces, read from the runtime's labels through the manager's list. The list is read again after two seconds, and at once for an id it does not hold, at most twice a second, so a space made a moment ago is found and a wrong id costs no more than one `docker ps` per half second. The dispatcher reads the path and nothing else to decide where a request goes: no directory, no body, no session id, no terminal id.

The prefix comes off, so `/api/spaces/<id>/session` is `/api/session` inside. The user's `cookie`, `authorization` and `proxy-authorization` headers come off, and so do `oc_url_token` and `oc_client_token` from the query; `host`, `origin`, `referer`, the hop-by-hop headers and the `x-forwarded-*` family come off as well. The request gets the space's own session cookie, `Host: 127.0.0.1:27600`, and `accept-encoding: identity`. Everything else, method, path, query, headers and body, travels as it is, and the body streams: nothing on the host reads it first. The host's `express.urlencoded` is global, so `registerCommonRequestMiddleware` takes a `skipBodyParsing(req)` predicate and leaves a prefixed request's body alone. A body a parser did read anyway is answered with 500 and `space_request_body_consumed` rather than waited for.

The answer streams back the same way. An event stream is written with `writeSseChunkWithBackpressure` of the host's own proxy, headers flushed at once, `Cache-Control: no-cache`, `X-Accel-Buffering: no`, and a client that leaves ends the request inside. Anything else is piped. Errors on the way are one JSON answer with a stable `code` when nothing has been sent yet, also while the client's body was still on its way in, and a closed connection after that. Route rules read the path without regard to case, as Express inside does.

### The session inside

The space's token is the password of the server inside, and the UI auth takes a password only through `POST /auth/session`, never as a bearer. So the dispatcher logs in once per space, with the token read back over `exec` and kept in memory, keeps the cookie, and renews it at ninety percent of the `Max-Age` the server inside gave, twelve hours by default. A login that the server inside refuses with the remembered token is repeated once with the token read again, because the agent can rewrite the file and the server inside can restart on the new one; a second refusal is the failure. One login runs at a time per space; requests that arrive during it wait for it. When the server inside answers 401 all the same, the session is forgotten, the token is read again, because the space may have been made again, and the login repeats: a request without a body is then sent again, once; a request with a body cannot be, its body went with the first attempt, so it is answered with 503 and `space_session_expired`, and the next one goes through on the renewed session. A login that the server inside refuses is 502 with `space_login_failed`. The token is in no answer, no error text and no log line: the dispatcher logs the code of a failure and the space id, nothing else.

### The transport

`connect` of the place contract gives the dispatcher its channel. For Docker it is `docker exec --interactive --user 1000:1000 <space container> /usr/local/bin/node -e <bridge> 127.0.0.1 27600`: the bridge, `CONNECT_BRIDGE_PROGRAM` in `layout.js`, joins the exec's stdin and stdout to the loopback port of the server inside and ends when either side ends. It runs on the image's Node by absolute path, uses `net`, which no proxy variable of the space touches, and reads nothing from the space but its two arguments. It leaves only after its last write to stdout has gone out; measured, an exit on the socket's close dropped what was still buffered, and a plain file stream on the descriptor failed with a system error once the pipe was full, so the two standard streams are Node's own.

On the host `openCommandStream` in `run-command.js` wraps that process as a `CommandStream`, a duplex with everything an `http` client calls on a socket: `setNoDelay`, `setKeepAlive`, `setTimeout`, `ref`, `unref` and `destroy`, which kills the command. It ends when the command's stdout ends, and a command that exits with a code other than zero, or before it wrote anything, ends it with `command_stream_failed` and the tail of the command's stderr, so a bridge that found nothing on the port says `ECONNREFUSED`. Backpressure runs both ways through the pipes; a four-megabyte answer while the other side is still sending arrives whole, sha256 compared, in the unit test.

One `http.Agent` per space holds these streams with keep-alive, at most eight, and reuses them: measured on the host, seven requests through one bridge cost one process, and a request on a warm stream took two milliseconds against seventy for the first. A free stream is closed after four seconds, before the server inside closes its side at five, Node's default keep-alive timeout, so a request never lands on a stream the server is closing. A stream that is closed kills its `docker exec`, and the bridge inside ends with it: the live file counts the bridge's processes inside after `close` and finds none. The agent asks `connect` for every new stream, so a space that stopped in between is refused with the place's own code. A stream from the pool can still die under a request, when the space stopped or the server inside closed it a moment earlier: a request without a body is then sent once more on a fresh stream, and a request with a body is answered 502, because its body went with the first attempt. The live file shows the first: a stopped space with a stream still in the pool answers 503 with `space_not_running`.

### The two guards

Both only reject. Neither routes anything.

- A prefixed request whose `directory` or `location[directory]` query, or `x-opencode-directory` header, decoded when the encoding header says `uri`, names a directory outside `/spaces/<id>` is refused with 400 and `directory_outside_space`. Directories are normalised as POSIX paths first, so `/spaces/<id>/../<other>` is outside.
- An unprefixed request under `/api/` that names a directory under `/spaces/` the same way is refused with 400 and `space_directory_needs_prefix`, before any host route and before the last-directory fallback. The directory gate of the host, `validateDirectoryPath`, takes a `refuseDirectory(candidate)` hook and refuses such a directory before it looks at the disk, which covers the OpenChamber routes that pass a body's directory through the gate, `markdown-image-grants` and the OpenChamber session routes among them; those answer with the gate's own 400 and its message. A body the OpenCode proxy forwards untouched, `location.directory` of `POST /api/session` among them, is read by nobody on the host: such a session lands wherever the host's OpenCode puts a directory that does not exist there.

The dispatcher reads no body, so a prefixed request whose body names a directory outside the space is not caught by the first guard. The server inside then works in a directory that does not exist there, which is the space's own business. Neither guard is a defence against the agent; both catch a client that addresses the wrong side.

### Route rules

Refused with 403 and `host_only_route`, whatever the server inside would say: `/api/provider`, `/api/providers`, `/api/integration`, `/api/credential`, `/api/auth`, `/api/config/settings`, `/api/config/themes`, `/api/themes`, `/api/github`, `/api/linear`, `/api/voice`, `/api/tts`, `/api/dictation`, `/api/guests`, `/api/projects`, `/api/client-auth`, `/api/system`, `/api/push`, `/api/notifications` and `/api/openchamber/tunnel`. Refused with 403 and `refused_across_boundary`: `/api/git/worktrees`, `/api/git/worktree-type`, `/api/git/integrate`, `/api/fs/serve`, `/api/preview/proxy` and `/api/session/<id>/move`. A route matches as a whole path segment, so `/api/config/agents` is forwarded while `/api/config/settings` is not. The lists are `HOST_ONLY_ROUTES` and `REFUSED_ACROSS_BOUNDARY_ROUTES` in `dispatcher.js`.

An unknown or malformed space id is 404 with `space_not_found`. A space that is stopped or in the middle of a move is 503 with the place's code. A token that cannot be read, a setup step that failed inside, and a refused login are 502 with their code. Everything else on the way, the bridge included, is 502 with `space_unreachable`.

### What comes back

Everything from a space is untrusted. Only the headers a client needs to read and cache the body come back, an allowlist: the content headers, `cache-control`, `expires`, `pragma`, `etag`, `last-modified`, `vary`, `date`, `x-accel-buffering` and `x-next-cursor`. So `Set-Cookie` never comes back and a space cannot set a cookie on the app's origin, `Clear-Site-Data` cannot wipe it, and `Location` is never followed: a 3xx from a space is answered as 502 with `space_redirected`, because a browser follows a same-origin redirect with the user's credentials and the request's method and body, which would let the agent aim the user's next request at any host route. Every answer carries `X-Content-Type-Options: nosniff`. An answer whose content type a browser would render as a page, `text/html`, `application/xhtml+xml`, `image/svg+xml`, `text/xml`, `application/xml` or `application/pdf`, or that has no content type at all, which a browser sniffs on a navigation, is served as `text/plain`. `/api/fs/raw` is served as it is only for an image a browser merely draws, `png`, `jpeg`, `gif`, `webp`, `avif`, `bmp` and icons, and for audio and video; anything else, SVG and HTML among them, becomes `application/octet-stream` with `Content-Disposition: attachment`. `/api/fs/serve` and `/api/preview/proxy` are refused, see above, so no file of a space renders as a page under the app's origin. Hop-by-hop headers and `www-authenticate` are outside the allowlist as they are outside the host's proxy.

### Sockets

`websocket.js` takes every `upgrade` under the prefix; `host.js` mounts its handler on the HTTP server beside the host's own. Four sockets of the server inside are forwarded, by their inner path: `/api/terminal/ws`, `/api/dev-tunnel`, `/api/event/ws` and `/api/global/event/ws`. Any other upgrade under the prefix is refused: a host-only or refused family with its 403 and code, anything else with 404 and `space_socket_unknown`, so the dictation socket and the guest surfaces stay the host's. The same guard as for HTTP applies first: a `directory` in the query outside the space is 400 with `directory_outside_space`.

The host authenticates the user the way its own socket handlers do, and per socket the same way as that socket's host twin: the terminal and the two event sockets need a session or a URL token and an origin the host trusts; the dev tunnel also takes a client that authenticated with its own token and sends no origin, as the desktop's tunnel client does. Then the user's `cookie`, `authorization`, the `oc_url_token` and the rest of the dispatcher's dropped headers come off, the space's session cookie goes on, and the handshake is written into a fresh stream from the place's `connect`, one `docker exec` per socket, never one from the agent's pool: a pooled stream can die under a request and an upgrade cannot be sent again on the same client socket. The client's `Sec-WebSocket-*` headers travel as they are, so the two ends negotiate the protocol and the extensions between themselves. The `Origin` the server inside gets is the loopback one, `http://127.0.0.1:27600`, the same origin the relay host presents when it dials the host's loopback, and never the client's: the server inside refuses an upgrade without an origin its host header matches, exactly as the host does.

The answer inside is read up to its header block, at most 16 KiB and within 30 seconds. A 401 renews the session once, as for HTTP, and repeats the handshake on a fresh stream. A 101 is answered to the client with the handshake headers only, `upgrade`, `connection`, `sec-websocket-accept`, `-protocol` and `-extensions`, and from then on the two sockets are joined byte for byte with `pipe` in both directions, so backpressure runs both ways and nothing of the frames is read on the host; a 4 MiB frame comes back whole in the unit test, sha256 compared. Any other status is answered on the client's socket as that status, 502 when it makes no sense, with `space_socket_refused`, so the dev tunnel's "that port is not an available dev server" from inside reaches the client as the 403 it is. A place that refuses `connect` answers with the dispatcher's codes, 503 `space_not_running` among them. Every refusal before the upgrade is one HTTP answer with a JSON body and a stable `code`.

After the upgrade, the stream inside ending or failing sends the client a close frame, 1012 `space_closed` or 1011 `space_unreachable`, and ends the socket; a client that leaves, at its `end` already and not only at `close`, since the host's sockets allow a half-open state, kills the stream and with it the `docker exec` and the bridge inside. A client that leaves while the handshake inside is still under way ends that handshake at once, so no stream waits out the 30 seconds for nobody. At most 72 sockets per space are open at once, the handshakes under way counted, 503 with `space_socket_limit` beyond that, because each one is a process on the host and inside from the moment `connect` is called. All of them end at the host's close.

The new paths joined the three allowlists by shape, as the guest surfaces did, in `ALLOWED_WS_PATHS` of `relay/tunnel-host.js`, `isUrlAuthWebSocketPath` of `ui-auth/ui-auth.js` and the Electron realtime proxy's list, and `/api/spaces/<id>/fs/raw` joined `isUrlAuthReadableHttpPath`, so a browser-owned image URL works with a URL token. Each prefixed shape is in exactly the lists its unprefixed twin is in: the realtime proxy carries no dev tunnel today, so it carries none for a space either.

### Sessions and events

The merged list and the rules for it live in `space-sessions.js`, pure; `host.js` reads the spaces and the proxy asks for the merge.

**The merged session list.** `GET /api/session` on the host is the host's list, sanitized and overlaid as before, plus every space's sessions after it, on the first page of the global list only: a request with a `cursor` is a later page of the host's list, and a request with a `directory`, in the query or in the `x-opencode-directory` header, which is what the sidebar reads once per project, is that directory's list; neither carries a space. The host reads its list of spaces, cached for two seconds, and asks every space at the same time for `/api/session` over the dispatcher's own channel, in pages of a hundred, at most ten pages, with a ten-second limit per page; a space's accepted list is served again for two seconds without asking it, so a burst of list requests costs each space one read. The answer carries a mark per space in `spaces`: `complete`, `partial` when the space had more pages than were read, `stale` when the space did not answer and its last accepted list stands in, and `unknown` when it never answered, in which case it has no records. Each mark also names the space: its `name` from the label, the registered host project it was made for as `projectDirectory`, resolved from the label's hash of the project path against the projects `server/index.js` hands the host, and the project's path inside as `directory`, both `null` when the project is not registered here. The UI's sidebar hangs the space's group on that project. A missing answer never means deletion: a stopped or unreachable space keeps the sessions it last listed, and the client learns from the mark that they are old. An empty answer is an answer, and empties the space's list. A host list when no space exists goes back as the very same object, untouched. The order is the place's list, so the merged list reads the same from one call to the next.

**What a space may claim.** Every record of a space is untrusted. A record is dropped, and the drop logged as a code with the space id and never with the record, when it is not an object with an `id`, when its `location.directory` is missing or lies outside `/spaces/<id>`, another space's directory included, when its id is one of the host's, or when another space already listed that id; a duplicate within one list keeps the first. What is kept is passed through the proxy's own field allowlist, so a space cannot add a field the host's list would not carry. The host's ids are learned from every host list that passes through the proxy and from every `session.created` event of the host's, the last fifty thousand of them. The rule is one-way on purpose: the host's id always wins, and a space that lists a host id loses that record, whatever it says about it. Which of two spaces keeps an id they both list is whichever answered first; neither is the host, so nothing of the user's is at stake, and a real collision of two random ids does not happen. Once a space holds an id, listed or created, no other space speaks for it: any event of another space that names that session is dropped as `space_session_claimed`.

**Events.** `space-events.js` opens one event connection per space, over the dispatcher's channel to `/api/event` inside, with the reusable upstream reader and a state of its own: its own `Last-Event-ID`, its own stall timer, and a backoff of its own that doubles from one second to a minute and resets when a connection is made. Every event passes the same rules as the list first: a directory on the event or on a created or moved session outside the space's root drops it, and so does a session id of the host's; an execution event carries no location and passes on its session id alone, as DESIGN.md records. What passes enters the host's global hub through `injectEvent`, marked with the space's id and with the space's directory as its directory: it is numbered, coalesced, replayed and fanned out with the host's events, so a client keeps one cursor for everything. Only subscribers that asked for space events see them, `subscribeEvent(fn, { spaces: true })`: the global WS bridge, so browsers get them; the OpenCode watcher, so live status, unread marks and notifications work for a space's sessions; and the SSE proxy for `/api/global/event`, which writes each one as a block of its own between the upstream's blocks, so a block of the host's is never cut; a stream scoped to one directory, by query or by header, stays the host's. Every other consumer of the hub, the session assist, the goal runtime, the message queue and the permission auto-accept among them, sees the host's events only, because each of them acts on the host's OpenCode by directory and must never act on a space's. When a connection is made the host asks the space for `/api/sessions/status` and enters each busy or idle session as a `session.status` event the same way, so a session that was busy while OpenChamber was closed shows busy from the first moment; and the connection itself is announced to the clients as `openchamber:space-stream` with `connected` or `disconnected`, so a client can re-read that one space after a gap. The host follows its list of spaces every fifteen seconds while the switch is on, so a new space gets its connection and a removed one loses it, with everything the host knew of it.

### Known limits

- A request with a body that meets a refused session is answered with `space_session_expired` once; the client has to send it again. With the renewal at ninety percent of the cookie's life this needs the server inside to have lost its sessions, which a restart of that server does not do, since its signing secret is on its disk.
- The list of spaces is a two-second cache over `docker ps`, so a space removed a moment ago can still be addressed for up to two seconds and is then refused by `connect`.
- The host reads the token once per process. A token file the agent rewrote is noticed when a login with the remembered token is refused, or when the server inside answers 401 to a forwarded request: the token is then read again once and the login repeated once; a token the agent keeps changing costs one extra read per refused login and never more.
- Nothing rotates the token, see "Token".
- One `docker exec` per stream in the agent's pool, at most eight per space, and each costs a process on the host and a `node` inside the space while it is open.
- The switch is read at start only. A host that cannot be made with the switch on, because the data directory cannot be written, starts without the feature and logs why.
- The dispatcher's connections are closed at the server's graceful shutdown; a server killed outright leaves its `docker exec` children to end on their pipes.
- The host's ids are what passed through this process: the lists it served and the sessions it saw created. A host session of a project not read since the server started, an archived one among them, is unknown to the rule, so a space can list it or report for it until the host lists it; from then on the space's record is dropped. A space cannot touch a session the host knows, and the host's own record of a session is never changed by a space's.
- A space's list and events are read whenever the switch is on, one `docker ps` every fifteen seconds and one event connection per space, whether or not a client is looking. That is the cost of live status for sessions nobody has opened yet.
- The relay path of the new sockets, a phone reaching a space's terminal through the private relay, has not been exercised on a device: the three allowlists carry the shapes and the unit tests prove each list, and nothing more.
- A space's event connection that is still being made cannot be aborted: the host's close waits for the server inside to answer or the stream to die. One request per space at most.
- The mark names a space's project only while that project is registered on this host under the very path the space was made for: the label holds a hash of that path, and the host compares it with its registered projects' paths as they are. A project removed or re-added under another spelling leaves the space with no project, and the sidebar shows nothing of it until the spaces page of a later stage lists every space.
- The empty-cwd terminal listing that the sidebar's action badges read is the host's alone; a space's terminals are listed per directory, through the prefix, when the terminal panel of that directory asks.

## The journey

The design is in DESIGN.md, "User journey". Stage 5a built the host's side of it, `journey.js` behind `routes.js`; the screens that call it are the later parts of stage 5, and grants, with the obligations STAGES.md ties to the first real credential behind the window, are stage 5b. Everything here is written once on top of the manager, the gatekeeper channel, code in and code out.

### Routes

All under `/api/openchamber/spaces`, a namespace of the host's beside `/api/openchamber/tunnel`, on the JSON-body allowlist of `core-routes.js`. A request to the server inside a space is `/api/spaces/<id>/...` and belongs to the dispatcher; nothing here has that shape. While the switch is off every route but the switch answers 404 with `isolated_spaces_off`.

| Route | What it does |
|---|---|
| `GET /switch`, `PUT /switch` | The switch, see "The switch" above. |
| `GET /places` | Every place asked `check()` now, because the user is looking: `{ places: [{ id, available, version, os, arch, hostIsolation }] }`, or `{ id, available: false, code, message }`. |
| `GET /` | `{ spaces }`: the place's list with what the host remembers about each, the creations under way in their place, and the failed ones after it. |
| `POST /` | `{ projectDirectory, name, start, network }`. Answers 202 at once with the entry the list carries; the steps follow as events. |
| `POST /<id>/start`, `POST /<id>/stop` | The place's start and stop; start says the network again. Answers the listed entry, start with `networkRestored`. |
| `DELETE /<id>` | Removes the containers, networks and volumes, the service refs in the user's repository and the record; forgets a failed creation. `{ id, removed, refsRemoved, failures }`. |
| `GET /<id>/journal` | The gatekeeper's journal, `{ records, dropped, since }`; 409 `space_not_running` for a stopped space, because the journal is gone with it. |
| `GET /<id>/apply` | Brings the work out and describes what an apply would do, writing nothing of the user's: `bringCodeOut`'s answer and `describeApplyState`'s in one object. |
| `POST /<id>/apply` | `{ as: 'branch', branch, removeAfterwards }` or `{ as: 'changes', removeAfterwards }`. Brings the work out once more and applies it; what is applied is what this call fetched. `{ brought, applied, removal }`, `removal` null unless the space was removed. |

A failure is one JSON answer, `{ code, message, details }`, with the status `routes.js` gives that code: 400 for a bad request, 404 for a space or place that does not exist, 409 for a refusal that the user resolves, `space_preparing`, `space_not_running`, `branch_exists`, every refusal of an apply and `place_cannot_restrict_network` among them, and 502 for a failure of the place or the space; anything that is not a `SpaceError` is 500 with `space_journey_failed`. No stack travels.

### Create

`POST /` answers as soon as the space has an id, and the rest runs in the background, because a first create pulls the image, fills the tools volume and pushes the code, which can take minutes. The steps, each announced on the host's hub as `openchamber:space-progress` with `{ spaceId, step, failure, timestamp }` in `properties`, the way `openchamber:space-stream` is announced: `checking_place`, the place's `check()`, refused with the place's own code when it is unavailable and with `place_cannot_restrict_network` when an allowlist was asked of an engine without host isolation; `creating`, the manager's create, which is the image, the tools volume, both networks, the gatekeeper and the space container, verified; `setting_network`, the record written and the mode and allowlist said to the gatekeeper, which allowed nothing until then; `bringing_code`, the snapshot and the transfer of code in, with the space path and the base kept in the record; `ready`. The history goes after `ready`, in the background, and its outcome is kept in the record as `history`: `sent`, `already_complete`, `host_shallow` or `failed`, `pending` until then. A start of a space whose history is still `pending` sends it again.

A failure after `creating` removes what was made: the containers, the refs in the user's repository and the record, and the answer of the failed step is kept in the list as `state: 'failed'` with `failure: { code, message, details, cleanup }` until the user dismisses it with `DELETE /<id>`, so an error is never silent; when that clean-up itself failed, `cleanup` says so and the dismissal removes what is still there, so the space cannot come back in the list as damaged after the user was told it was gone. A creation under way is listed as `preparing` with its `step`, over the place's view of it, and refuses start, stop, remove and apply with `space_preparing` until it is ready or failed; a failed one refuses them with `space_creation_failed`. One action runs per space at a time: start, stop, remove, the journal, the preview and the apply take a lock on the space's id and a second action meets `space_busy`, so a remove from another client cannot pull the service refs from under an apply that is writing them. A start of a space whose history is `pending` or `failed` sends it again, because a stop right after the creation is the usual way it fails. The project must be one the host has registered, compared as the label's hash and the path, because the sidebar resolves a space to its project the same way. The four choices are parsed at the boundary, each field refusing with a code of its own: `project_not_registered`, `invalid_space_name`, `invalid_snapshot_mode`, `invalid_network`.

### The record

The labels stay the source of truth about which spaces exist. What the host adds is one file per space under `<data dir>/spaces/records/<id>.json`, mode 0600, written through a temporary name: `{ version, network: { mode, domains }, repository, spacePath, base, history }`. It exists so that a gatekeeper that starts with an empty memory is told the network the user chose, and so that the history and the result find the repository and the space path that code in used, which differ from the project directory when the project is a subfolder of its repository. A record the host cannot read is reported as unreadable, never as a fresh one, and is not written over: `start` then leaves the network closed and answers `networkRestored: false`, and the list carries `network: null`, which a screen shows as unknown and never as open. No secret is in a record; that is decision 5, and 5b keeps it when the grants come.

### Known limits

- The list route runs one `docker ps` and one record read per space on every call; there is no cache, because the user is looking and the answer must be the state now.
- A creation is one process's memory: a host that restarts in the middle of one leaves the space where the step got to, listed from the labels with whatever the record holds; a space whose code never arrived has an empty `spacePath` in its record and its apply fails on the project path. Removing such a space is the way out.
- The history transfer is not retried on its own; a start of the space retries it while the record says `pending` or `failed`.
- The lock on a space is one process's memory, like code out's turns: two OpenChamber processes on one Docker daemon are not coordinated.
- Nothing of the setup commands, the chat archive, the idle stop, the grants or the "needs access" state is here; the later parts of stage 5 add each.

## Code in

The project's code travels into a space as git objects over the place's exec channel: `git push` over `ext::`, with `docker exec` as the transport. No network, no ports, no archives, no shared folders. The host drives every step. The command sequences are the ones in `docs/isolated-spaces/stage-0/e4-git-over-exec.md`, with the changes below. Nothing calls this yet, and getting code out, quarantine and apply are stage 3b.

`createCodeIn({ git, place, temporaryDirectory, removeDirectory })` in `code-in.js` returns six functions. `removeDirectory` is how the temporary folder goes, injectable so a test can make it fail.

| Function | What it does |
|---|---|
| `takeSnapshot({ repository, spaceId, mode })` | Two snapshot commits on the host and the host ref that keeps them alive. `mode` is `uncommitted`, or `clean` for a start at HEAD with nothing uncommitted. |
| `listTravellingFiles(repository)` | `{ files, notTravelling, totalBytes }`: the untracked files that travel with uncommitted changes, with sizes, for the create dialog, and the untracked repositories that stay behind. |
| `readTransferLimits(repository)` | Submodule paths and whether Git LFS is in use, for the warnings at creation. |
| `bringCodeIn({ repository, spaceId, mode, timeoutMs, innerMarginSeconds })` | The snapshot, the transfer, and the unfold inside. Resolves `{ spacePath, projectPath, base, staged, start, branch, objectFormat, identityCopied }`. |
| `sendHistory({ repository, spaceId, spacePath, base, timeoutMs, innerMarginSeconds })` | The history behind the base, for the caller to run later, in the background, into the `spacePath` that `bringCodeIn` returned. |
| `removeSpaceRefs({ repository, spaceId })` | Deletes every ref under `refs/openchamber/spaces/<id>/`, in one transaction. The refs live in the user's repository and go with it: on a repository that was moved or deleted it rejects, and there is nothing left to remove there. |

`takeSnapshot` is the first step of `bringCodeIn`, which takes the snapshot itself. It stays exposed because the snapshot tests run through it on every platform, the transfer tests cannot run on Windows; calling it before `bringCodeIn` for the same space gives `space_ref_exists`.

What the caller must do:

- Keep the `spacePath` and `base` that `bringCodeIn` returned, and hand them to `sendHistory`.
- Run `sendHistory` in the background after `bringCodeIn`, one run per space. Calls in this process with the same arguments share a run that is under way, and other arguments are refused; calls from two processes are not coordinated and break each other.
- Treat `history_transfer_failed` as a warning. The space works; its history is shallow.
- Call `removeSpaceRefs` when the space is removed.
- Warn when `identityCopied` is not both true: the agent cannot commit then.
- Give `createHostGit` the environment the docker CLI needs. git starts `docker exec` through `ext::` with that environment, so `DOCKER_HOST`, `DOCKER_CONTEXT`, `DOCKER_CONFIG` and the like ride on it, and the filter keeps them.

What each function rejects with, read from the code:

| Function | Codes |
|---|---|
| `bringCodeIn` | A missing request gives `invalid_space_id`, as it does for `takeSnapshot` and `removeSpaceRefs`. The refusals `invalid_inner_margin`, `invalid_timeout`, `invalid_space_id`, `invalid_snapshot_mode`, `git_version_unreadable`, `git_too_old`, `project_folder_missing`, `not_a_git_work_tree`, `repository_has_no_commit`, `repository_has_unmerged_changes`, `space_ref_exists`, `invalid_branch_name`, `project_folder_does_not_travel`; `git_command_failed` for a host git step; `space_path_in_use`; `code_unfold_failed`; and `code_transfer_failed` for everything else, with `details.step` and `details.cause`, the cause being the place's or the runner's code: `space_not_found`, `space_not_ours`, `space_not_running`, `space_move_unfinished`, `docker_command_failed`, `docker_output_unreadable`, `command_timeout`, `command_output_too_large`, `command_killed`, `command_spawn_failed`, `invalid_ext_argument`, `inside_command_failed`, or an errno for a failure on the host. |
| `sendHistory` | Only `history_transfer_failed`, with `details.step` and `details.cause`, the cause being any code above that applies, or `invalid_space_path`, `invalid_inner_margin`, `invalid_timeout`, `not_a_commit`, `inside_command_failed` or `history_in_progress`. A missing request gives the cause `invalid_space_id`. |
| `takeSnapshot` | The refusals above, `git_command_failed`, a runner code (`command_*`) as it is, and `code_in_failed` for a temporary folder or any failure that is not a `SpaceError`. |
| `listTravellingFiles`, `readTransferLimits` | `project_folder_missing`, `not_a_git_work_tree`, `git_command_failed`, a runner code, `code_in_failed`. |
| `removeSpaceRefs` | `invalid_space_id`, `project_folder_missing`, `not_a_git_work_tree`, `git_command_failed`, a runner code, `code_in_failed`. |

### Host git

Every git command the host runs goes through `createHostGit` in `host-git.js`: `run-command.js`, an argv, no shell, and a git path the caller can inject. The environment is the user's own, so their global config applies, and that is on purpose: their global ignore file must keep its files out of a space. A snapshot run without the global config would carry every file they keep out that way. What is removed is anything inherited that points git somewhere else: `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`, `GIT_NAMESPACE`, `GIT_COMMON_DIR`, the shallow, graft and replace variables, `GIT_CONFIG_PARAMETERS` and the `GIT_CONFIG_COUNT` family, the default hash and ref format, `GIT_ALLOW_PROTOCOL`, and the author and committer variables. On Windows the names are compared without case, as the system does. Two are added: `GIT_OPTIONAL_LOCKS=0`, so a status read never rewrites the index, and `GIT_TERMINAL_PROMPT=0`.

It is a denylist: everything it does not name passes, so `GIT_TEMPLATE_DIR`, `GIT_SSH_COMMAND` and the like reach our git, and no code-in path is affected by them. It is also the environment of the `docker exec` that git starts through `ext::`, so the Docker settings, `DOCKER_HOST`, `DOCKER_CONTEXT`, `DOCKER_CONFIG`, `DOCKER_TLS_VERIFY`, `DOCKER_CERT_PATH`, and `SSH_AUTH_SOCK` for a Docker over SSH, must pass, and a test holds them there. The one `init` on the host runs with `--template=`, which wins over `GIT_TEMPLATE_DIR`, and nothing uses ssh. `GIT_LITERAL_PATHSPECS`, `GIT_GLOB_PATHSPECS`, `GIT_NOGLOB_PATHSPECS` and `GIT_ICASE_PATHSPECS` are removed, because they change how every pathspec is read: with `GIT_LITERAL_PATHSPECS=1`, `:(exclude,literal)<folder>` would be a path, `add` would fail for any repository with an untracked nested repository, and `:(attr:filter=lfs)` would match nothing. `GIT_ATTR_SOURCE` is removed too: it makes git read every attribute from a tree instead of the working tree, and code out reads the working tree back with exactly the attributes it holds. The `GIT_TRACE` family, `GIT_TRACE2` included, is removed: it writes to stderr, which a push counts against its 1 MB output cap, so an inherited `GIT_TRACE_PACKET` would have turned every push into `command_output_too_large`.

Every host git call of code in also gets `-c core.hooksPath=<an empty folder of ours>` and `-c core.fsmonitor=` (empty), so it starts nothing of the user's: not in the snapshot, the ref writes, the sender's `init`, the pushes, the history, or the two lists. Measured with git 2.50.1 before these: the user's `post-index-change` hook ran three times per snapshot with `GIT_INDEX_FILE` pointing at our copy, their `reference-transaction` hook ran on every write of our ref, a `core.fsmonitor` program ran ten times per snapshot, and `core.fsmonitor=true` left a `git fsmonitor--daemon` running on the host and its files and socket in the user's `.git`. The value is empty and not `false`, because before git 2.36 `core.fsmonitor` is a command path, where `false` would name a program; empty means "none" in both readings, measured on 2.50.1 for a program and for `true`. The tests give the user a global `core.hooksPath` with thirteen hooks, a `core.fsmonitor` program and `core.fsmonitor=true` (not on Linux, whose git has no daemon), each with a control that shows it running on an ordinary command, and require nothing of it after a whole code in, the history, both lists and the ref removal: no marker, no running daemon, no new file.

Other config that starts a program was checked by reading git's documentation, not by trying each: auto gc and maintenance run after commit, merge, fetch, am and rebase, and none of our commands; the test gives the user `gc.auto=1`, `gc.autoPackLimit=1` and `maintenance.auto=true` on a repository with two packs, shows an ordinary commit consolidating them, and requires both packs untouched after code in. Clean filters, Git LFS among them, do run during `add`, on purpose, as the user's own `git add` would. Credential helpers, `core.sshCommand` and pagers are not reached: nothing authenticates, nothing uses ssh, and nothing runs on a terminal.

Before anything else, code in reads `git version` and refuses a git older than 2.29.0 with `git_too_old`, naming the version it needs, or with `git_version_unreadable`. Code out does the same with the same floor, which is the one floor of the feature. The floor comes from git's manual pages, not from a run: the suites ran with git 2.47.3 on Debian 13, 2.50.1 on macOS and 2.54.0 on Windows, so a git from 2.29.0 that is older than 2.47.3 is accepted but untested. It was 2.27.0 in 3a and rose to 2.29.0 in 3b: `fetch --no-write-fetch-head` first appears in the git-fetch manual of 2.29.0, and the 2.28.0 page does not have it; without it the promote would write `FETCH_HEAD` into the user's `.git`. Code out's other options are older, checked in the manuals of 2.29.0: `--update-shallow`, `--refmap`, `--no-recurse-submodules`, `--no-tags` and `--no-auto-gc` in git-fetch (the 2.28.0 page has all five); `--output`, `--binary`, `--full-index`, `--no-renames`, `--no-ext-diff`, `--no-textconv`, `--no-color`, `--src-prefix` and `--dst-prefix` in git-diff-tree; `--check`, `--binary` and `--whitespace=nowarn` in git-apply, whose configuration section names exactly two settings, `apply.ignoreWhitespace` and `apply.whitespace`; `--objects` and `--no-object-names` in git-rev-list. For code in the floor comes from git's own manual pages per version on git-scm.com: `git init --object-format` first appears in the git-init manual of 2.27.0 (the 2.26 page is the 2.21 text); `rev-parse --show-object-format` and `add --pathspec-from-file` with `--pathspec-file-nul` are in the 2.25.0 manuals. The rest is older than 2.27.0: `--is-shallow-repository`, `--git-path`, `--show-prefix` in the git-rev-parse manual of 2.27.0; `-C`, `GIT_OPTIONAL_LOCKS` and `GIT_TERMINAL_PROMPT` in git's own manual of 2.27.0; `core.hooksPath` and the `post-index-change` hook in githooks of 2.27.0; `--no-verify`, `--no-signed`, `--follow-tags`, `--no-recurse-submodules`, push options and `--quiet` in the git-push manual of 2.25.0; the `literal`, `exclude` and `attr` pathspec magic in gitglossary of 2.23.0. Two behaviours were measured on 2.50.1 only and not checked on older versions: that an empty `push.pushOption` clears the list, and that an empty `core.fsmonitor` turns it off. The parser reads the first three numbers and ignores a build's suffix, such as `2.50.1 (Apple Git-155)` or `2.54.0.windows.1`.

### The snapshot

Refusals come first, each a `SpaceError` with a message that says what to do: `not_a_git_work_tree` (a bare repository is one too), `repository_has_no_commit` for an unborn HEAD, `repository_has_unmerged_changes` for a merge or rebase with unresolved conflicts, and `space_ref_exists`. After the commits and before the ref, `project_folder_does_not_travel` refuses a project that is a subfolder of its repository when the start has no folder there, because it is ignored or empty: the project path inside would not exist. Nothing has reached the space at that point.

Then, with `-c core.splitIndex=false` on the three calls that use the copy:

```
cp <git-path index> <temp>/index
GIT_INDEX_FILE=<temp>/index git write-tree            staged tree
GIT_INDEX_FILE=<temp>/index git add --all --pathspec-from-file=- --pathspec-file-nul
                                                      stdin: . and :(exclude,literal)<each untracked repository>
GIT_INDEX_FILE=<temp>/index git write-tree            working tree
git commit-tree --no-gpg-sign <staged tree> -p HEAD   "openchamber: staged snapshot"
git commit-tree --no-gpg-sign <working tree> -p <staged>   "openchamber: working tree snapshot"
git update-ref refs/openchamber/spaces/<id>/start <start> <zero id>
```

- The commits carry a fixed identity through the environment, `OpenChamber <spaces@openchamber.invalid>`, so a user without `user.name` can still create a space.
- `core.splitIndex=false` matters. Measured with git 2.50.1: without it, `add` into the copy of a split index writes a new `sharedindex.*` file into the user's `.git`. The test proves it by removal.
- `--no-gpg-sign` guards less than it looks. Measured with git 2.50.1, `commit-tree` ignores `commit.gpgSign`, and only `git commit` signs: a user with `commit.gpgSign=true` and a broken `gpg.program` gets a working snapshot with or without the flag. It stays for a git that reads the setting. Removing it does not turn the test red, and that is why.
- The ref is created against the zero id, so it never moves a ref that appeared in between. 3b needs it as the base of the result patch, and it survives garbage collection.
- Untracked folders that are git repositories of their own stay out of the snapshot, excluded by pathspec with literal magic, so a name with `*` or `[` in it is not read as a pattern, and read from stdin, so their number meets no command line limit. `add` would record each as a gitlink; it would arrive as an empty folder the space's git cannot see, and `git diff refs/openchamber/start` inside would then show it deleted before the agent did anything, which is a trap for the 3b patch. A test checks that the start and the working tree inside agree for a repository with two of them. This also means an untracked repository with no commit no longer stops the snapshot.
- Paths in `listTravellingFiles` are relative to the repository's top level. For a project in a subfolder the whole repository travels, so the list holds files outside the subfolder too.
- `add` reads the same ignore rules as `ls-files --others --exclude-standard`, with the same config. That is why `listTravellingFiles` lists exactly what travels, and a test compares the two. Untracked repositories are listed under `notTravelling` as `{ path, kind: 'repository' }`.
- With `core.autocrlf` or other clean filters, the size `listTravellingFiles` gives is the size on disk, and the file arrives in the form `add` stores, line endings normalised.

### The transfer

`bringCodeIn` takes the snapshot, then:

1. `execArgv` of the place, asked before anything runs inside, so a space that is gone, stopped or not ours is refused with a code of its own.
2. Inside, a fixed script: refuse with `space_path_in_use` if the project path exists, else `git init --object-format=<the host's> --initial-branch=<the host's branch>` there and set `receive.shallowUpdate=true`, the only config set inside. With the host's branch the agent's reflog shows no stray `master`; `--initial-branch` needs git 2.28 inside, and the image has 2.39.5. A detached host HEAD keeps git's default initial branch, which never becomes a ref.
3. On the host, a temporary bare sender, made with `--template=` so no template hook lands in it, with `objects/info/alternates` pointing at `git rev-parse --git-path objects` of the project, which names the common object store in a linked worktree too, and a `shallow` file holding the base. It copies no object.
4. Two pushes from the sender: the base to `refs/openchamber/base`, then the staged commit and the start to `refs/openchamber/start-index` and `refs/openchamber/start`. One push of both failed on git 2.39.5, the image's git. The host pushes only to `refs/openchamber/*`.
5. Inside, the unfold: `user.name` and `user.email` from the host into the space repository's own config when they are one short line, and `identityCopied` says which of the two went in, then `checkout -B <branch> refs/openchamber/start`, `reset --soft refs/openchamber/base`, `read-tree refs/openchamber/start-index`. The branch is the host's, checked on the host with `git check-ref-format --branch`. A detached HEAD on the host gives a detached HEAD inside. Measured by the maintainer's probe: without the identity the space repository has none, and the agent cannot commit. Nothing is guessed for a user who has none: git's own guess would put the host's machine name into the space. Signing keys and every other host config stay on the host.

Every push runs as `git -c protocol.ext.allow=always -c push.pushOption= push --quiet --no-verify --no-signed --no-follow-tags --no-recurse-submodules <url> <refspecs>`. The ext switch is on the push and nowhere else. `--no-verify` and the empty hooks folder both stop a pre-push hook from a global `core.hooksPath`; with both in place, removing either one alone leaves the hook test green, and removing the hooks folder turns it red for the other hooks. Both pushes go from the sender and never from the user's repository, so the repository's own config and hooks are not involved.

The URL is `ext::` followed by an argv: `execArgv` of the place, then `/usr/bin/timeout -s KILL <seconds> /usr/bin/git receive-pack <path>`, written out, without `%S`. git-remote-ext splits the text on spaces itself, so `buildExtUrl` writes a space inside an argument as `% ` and a percent as `%%`, as its documentation says, and refuses an empty argument or a control character. The docker CLI on Windows can live under a path with spaces. The URL is one argv element, never through a shell. The unit tests run the transfer through a receiver kept in a folder whose name has a space and a percent sign, so removing the escaping breaks a real push.

`timeoutMs` is a whole number of milliseconds from 1,000 to 86,400,000, refused otherwise with `invalid_timeout`. It limits the pushes only: the init and unfold scripts inside, the shallow check and the side repository's init run with a fixed limit of five minutes each, whatever `timeoutMs` is, and the unshallow fetch inside with the inner limit. The inner limit is the host's timeout plus a margin, five seconds unless the caller gives `innerMarginSeconds`, a whole number from 1 to 3600 and nothing else, refused otherwise with `invalid_inner_margin`. So it never ends a push the host still waits for, and a push the host gave up on leaves nothing running inside once it passes. The live hanging tests size the margin from a measured `docker exec` round trip: over `DOCKER_HOST=ssh://` each docker call opens its own SSH connection, and a five-second window between the host's kill and the inner limit had no room for the checks that run inside it. The default host timeout is ten minutes for each push of `bringCodeIn` and thirty minutes for the push of `sendHistory`, so a receiving process the host gave up on can live inside for up to 10 min 5 s or 30 min 5 s.

`bringCodeIn` rejects with the codes in the table under the function table: its refusals and own codes as they are, and everything else, a failure of the place included, as `code_transfer_failed` with the place's or runner's code in `details.cause`. The temporary folder is removed with retries, and a removal that still fails never replaces the error: the error names the folder in `details.temporaryDirectoryLeft`. After a call that worked, a leftover folder is let go rather than turned into a failure. Measured on Windows before the kill was awaited: the removal failed with EBUSY, and that error hid the timeout.

On any failure after the snapshot, `bringCodeIn` deletes the host ref it wrote, by its value, and says in `details.refNotRemoved` if that failed. What reached the space stays there: the space is the unit to remove, and a retry into the same space is refused with `space_path_in_use`, whose message says to remove the space and create a new one.

### History

`sendHistory` runs later, while the agent may already work. A push cannot deepen a shallow receiver, so:

1. Inside, `rm -rf` and `git init --bare` the side repository `/spaces/<id>/.openchamber-history.git`. Its name starts with a dot, which no project folder name can.
2. A push of the base with its full history from a temporary sender with alternates and no `shallow` file, into the side repository, the same way as above.
3. Inside, in the project repository, `timeout -s KILL <seconds> git fetch --quiet --unshallow --no-tags <side> +refs/openchamber/base:refs/openchamber/base`, then `rm -rf` of the side repository whatever the fetch did.

It changes no working tree, index or HEAD inside, and commit ids stay the same. A host repository that is shallow itself has no history to send: `sendHistory` resolves `{ status: 'host_shallow' }` and asks the space nothing. Before it pushes, it asks the space whether its repository is still shallow, and resolves `{ status: 'already_complete' }` without sending anything when it is not: after an earlier call that worked, a second one used to fail on "--unshallow on a complete repository". That answer comes from inside and only decides whether to send, so a space that lies about it costs itself its history and nothing else. Calls for one space in this process share the run that is under way, because the side repository is one per space and its `rm -rf` would kill a run in progress. The runs are kept at module level, so two `createCodeIn` instances share them too. Each call's own `spaceId`, `spacePath` and `base` are checked first, and a call joins a run only when it asks for the same `spacePath` and `base` from the same checkout, the `repository` path resolved; one that asks for something else is refused with the cause `history_in_progress`. Calls from two processes are not coordinated. Every failure rejects with `history_transfer_failed`, a moved repository, an old git, an unreachable space, a temporary folder that cannot be made and a `spacePath` code in did not return among them, with the step in `details.step` and the root cause in `details.cause`: `command_timeout` or another code of the runner for a push, `inside_command_failed` with `details.exitCode` for a script inside, `not_a_commit` for a base the host does not have. The side repository is removed on the way out. The caller reports it; the space keeps working with a shallow history. It does not refuse a host that has started a merge since the snapshot, because that has nothing to do with the history behind a commit that already travelled. `sendHistory` takes the `spacePath` that `bringCodeIn` returned and checks that it is one folder in this space; it does not work it out again from the repository it is handed, which may have been renamed or checked out elsewhere since.

### A space that does not cooperate

The space is hostile during code in too. Its receiving side can refuse, hang, lie about what it has, or print a lot. By the time the history travels the agent has run, and it can have planted hooks or config: a global `~/.gitconfig` in the space's HOME with `core.hooksPath` reaches every git there, the receive-pack of the side repository among them. None of it may change the user's repository or leave a process on the host.

- A refusal is a `code_transfer_failed` whose message carries at most 2,000 characters of what the space printed. That text is shown and never parsed.
- A hang ends at the host timeout, which kills the whole process tree, see below.
- More than 1 MB of output ends the push the same way. A quiet push prints nothing. The receiving process inside is not ended by that: it runs until the inner limit, 10 min 5 s for code in and 30 min 5 s for the history with the defaults. That is a limit of the space, which is the agent's own machine, and not of the host.
- A lie about what it has can only shrink what the host sends, so the unfold or the fetch inside then fails and that failure is reported. This is reasoned from how a push negotiates, and not tested.
- The host only pushes into the space during code in. It fetches nothing from it.

### Killing a transfer

Measured by the maintainer's probe on 2026-09-22 on macOS with Colima, on Debian over `DOCKER_HOST=ssh://` and on Windows 11 with Docker Desktop: when a hanging push is ended by killing only the `git` process, the `docker exec` it started stays alive on the host until the limit inside the space ends it. When the whole tree is killed, nothing is left on the host.

So `runCommand` takes `killTree: true`, and the pushes use it. On POSIX the child is spawned with `detached: true`, so it leads a process group, and a kill signals that group. On Windows, `taskkill.exe /T /F /PID <pid>`, named by its absolute path under `SystemRoot`, spawned directly with `windowsHide: true` and no shell; `detached` stays off there, because it would give the child a console of its own. `killProcessTree` refuses to signal anything for a pid that is not an integer greater than 1. A fake process with pid 1 once became `kill(-1)` and closed every program on a developer's machine, and a pid of 0 would signal our own group. Every other docker call keeps the old behaviour. The rejection waits until the kill has done its work: on Windows until `taskkill` has exited, then until the child has closed, at most five seconds each. Without that wait, measured on Windows, the caller's removal of its temporary folder ran while git still held files there.

A `killTree` child leads a process group of its own on POSIX, so a Ctrl-C that reaches the server's group does not reach it: measured, the child of a wrapper that got SIGINT outlived it, with `git push` and its `docker exec` left until the inner limit. `run-command.js` therefore keeps the live `killTree` children in a set and registers once, lazily, an exit handler that kills each tree synchronously: a group kill with the pid guard on POSIX, and on Windows `taskkill.exe` by its absolute path through `spawnSync`, hidden. It is registered with `process.prependListener`, because an `exit` listener registered earlier that throws stops the ones after it, measured on Node 22.23.1, and the foreground serve command registers one earlier; the handler itself never throws. A child leaves the set when its own process exits, not when its pipes close: an escaped grandchild can hold the pipes for ever, and the exit handler would then kill a pid that exited long ago and may belong to somebody else by then.

Windows needs the handler too. libuv puts each child it starts into a job object that kills its members when the Node process goes, a hard kill included, because the job's last handle closes with the process. But the job is created with silent breakaway, and libuv's `src/win/process.c` says so in a comment: only the processes libuv adds are affected, not their subprocesses. git is a member and dies with the server; the `docker exec` git starts is not, and only `taskkill /T` reaches it. The test "kills the tree of a live child when the process leaves with process.exit" is the one that shows it on Windows: its grandchild is started outside the child's job, as `docker exec` is outside ours. The module installs no signal handler of its own; the server's handlers for SIGINT, SIGTERM, SIGQUIT and SIGHUP end in `process.exit`, and the exit handler runs then. The tests start a real wrapper that runs a `killTree` child and leaves with `process.exit`, one that leaves from its own SIGINT handler, and one with a throwing `exit` listener registered before ours, and require the child and its grandchild gone. Other tests count the `exit` listeners across several runs, feed the handler fakes with pids that must not be signalled, and check that the set is empty after a spawn failure, a timeout, a normal run, a kill at exit, and after a child exited while an escaped grandchild still holds its pipes. What remains: a server killed with SIGKILL runs no exit handler, so its transfer lives until the inner limit, 10 min 5 s or 30 min 5 s with the defaults.

The close wait has its bound for a reason the tests show: a grandchild that left the process group with `setsid` survives the group kill and holds the child's stdout, so the child's pipes never close. The bound, five seconds, injectable as `killWaitMs`, is what ends the wait then.

The unit control for the tree kill needs care on Windows. Every Node process puts the children it starts into a job object of its own that kills them when that Node process goes. In the control that Node process is the child, so a grandchild started the plain way dies with the child, and the control, "killing only the child leaves the grandchild", failed on the Windows host. git is not Node and uses no job object: the maintainer's probe showed `docker exec` alive after killing only git. The control's grandchild is therefore started with `detached` on Windows, which takes it out of the child's job and keeps the child as its parent, the shape `docker exec` has under git and what `taskkill /T` follows. The control held on the maintainer's Windows run of the second round.

The live test plants the hanging hook above and checks that no host process names the container two seconds after the host gave up, while the planted hook still runs inside, and that nothing runs inside once the inner limit has passed. The check must finish before that limit: the limit ends a leftover `docker exec` on its own, and a first version of the test that waited five seconds passed without the tree kill.

### What code in does to the user's repository

- `add -A` writes loose objects for the untracked and changed files into the user's `.git`, and runs the user's clean filters, Git LFS among them, as their own `git add` would.
- The ref `refs/openchamber/spaces/<id>/start` stays until `removeSpaceRefs`. Git makes the folders `refs/openchamber/spaces/<id>` for it, and may leave an empty `refs/openchamber` behind when the ref goes.
- Nothing else. The tests compare the whole repository folder, `.git` included, file by file with modes and folders, before and after, also after every failure and every refusal, and allow exactly those two kinds of difference: new loose objects in the repository's own `.git` with their fan-out folders, as additions only, and the start ref of the space the test names with its folders, added or removed but never moved. A socket, a FIFO or a device is described by its type and never read. A test of the comparison itself feeds it a changed `.git/config`, a new hook, a changed `info/exclude`, `MERGE_HEAD`, a `sharedindex.*` file, a new empty folder, packed refs, a reflog, a ref of ours outside the space, the ref of another space, a rewritten loose object, the start ref of this space moved to another commit, a new loose object in a nested repository's `.git`, a socket and a FIFO, and `chmod +x` of an untracked file, and requires it to see each.

### Known limits

- Submodules arrive empty, as gitlinks. `readTransferLimits` names their paths. An untracked nested repository does not travel at all, see "The snapshot".
- A user without `user.name` or `user.email` gets a space where the agent cannot commit: git there answers "Author identity unknown". Having one of the two is not enough; the agent needs both. `identityCopied` says which went in, so the create dialog can warn.
- Git LFS files arrive as pointers.
- A repository with no commit is refused.
- Intent-to-add entries lose their mark: their content arrives as an untracked file. Stashes, conflict state and skip-worktree flags do not travel either.
- A project that is a subfolder of its repository brings the whole repository. The folder inside is named after the repository's top level, and `projectPath` names the subfolder in it.
- The project folder name is the top level's base name with every run of characters outside letters, digits, `.`, `_` and `-` turned into one `-`, no leading dot or dash, at most 64 characters, and `project` when nothing readable is left or the name is `node_modules`, where the plugin link lives.
- Not verified: a partial clone, a sparse checkout, a repository over 1 GB, a slow link, and a newer git inside.

## Code out

The space's work travels back as git objects over the same exec channel, this time `git fetch` over `ext::`, and is applied to the user's repository in one of two ways. Everything that comes out of a space is untrusted data. The command sequences start from `docs/isolated-spaces/stage-0/e4-git-over-exec.md` and carry the changes below, most of them from the maintainer's probe of 2026-09-22 on macOS with Colima, on Debian 13 over `DOCKER_HOST=ssh://`, and on Windows 11 with Docker Desktop. Nothing calls this yet.

`createCodeOut({ git, place, temporaryDirectory, removeDirectory, platform, readBackTimeoutMs, files })` in `code-out.js` returns four functions. `platform` is `process.platform` unless a test says otherwise, and decides which file names this computer can hold. `readBackTimeoutMs`, five minutes unless a test says otherwise, bounds the time one apply, or one `describeApplyState`, spends reading the working tree back; see "Apply". `files` is how the working tree is read back, `node:fs/promises` unless a test counts what is read. It shares its host git session, the version floor and the `ext::` URL builder with code in, through `code-transfer.js`.

| Function | What it does |
|---|---|
| `bringCodeOut({ repository, spaceId, spacePath, timeoutMs, innerMarginSeconds, maxTransferBytes, maxChangedBytes, maxChangedEntries })` | The snapshot inside, the fetch into a quarantine repository, the checks there, and the promote into `refs/openchamber/spaces/<id>/result`. Resolves `{ result, changedPaths, changedBytes, nestedRepositories, unmerged }`: the result commit, how many paths it changes against the start, how many bytes those paths hold in the result, which is what applying writes, and the two things the caller has to warn about, each `{ count, paths }` with at most a hundred paths. `nestedRepositories` are repositories the agent made inside its project, which travel as a gitlink and nothing else; `unmerged` are the paths the agent left in a conflicted merge, whose markers are in the result as ordinary content. |
| `applyAsBranch({ repository, spaceId, branch })` | Creates `refs/heads/<branch>` at the result. Nothing is checked out. Resolves `{ branch, commit }`. |
| `describeApplyState({ repository, spaceId })` | Reads where the space stands for an apply as uncommitted changes and changes nothing: `{ changesRoute, result, lastApplied, newPaths, newPathsOverLimit, newPathsUndecided, interruptedApply }`. `changesRoute` is `open` or `closed`, `newPaths` how many paths an apply would write now, or null before any bring-out, and `interruptedApply` whether an earlier apply was cut off, which the next apply sorts out first; an intent beside a closed route is not reported, because nothing applies as changes there any more. When the working tree shows that the interrupted apply finished, `newPaths` counts from what it wrote, as the next apply will. When the user threw applies away, it counts from where the next apply will start, decided the same way, and when the user threw away part of the last apply, which the apply refuses, from the last apply. When reading the working tree back does not end in time, `newPaths` is null and `newPathsUndecided` true, and the apply answers `changes_undecided`. When the list of those paths is longer than the host reads, 64 MiB, `newPaths` is null and `newPathsOverLimit` true: an apply of that many is refused as too large, and the branch is the way. It takes its turn with the applies of the same space, see "Apply". It is the one way to learn whether the route is closed without applying. It reads the working tree back as an apply does and costs what that costs, see "Apply": ask it when the user is about to apply, and never poll it. On a closed route it reads nothing back. |
| `applyAsChanges({ repository, spaceId, maxChangedBytes, maxChangedEntries })` | Applies to the working tree, as uncommitted changes, what the space changed since the last apply of this space, or since its start when there was none. When the user threw the last apply away, the whole work since before the last apply, see "Apply". Once a round of this was refused, the route is closed for that space and every later call refuses at once. Resolves `{ status: 'applied', appliedPaths, remembered, nestedRepositories, conflicted }` or `{ status: 'nothing_to_apply' }`. |

What the caller must do:

- Hand `bringCodeOut` the `spacePath` that `bringCodeIn` returned. It is checked with `requireSpaceProjectPath`, as `sendHistory` checks it, and never worked out again from the repository.
- Call `bringCodeOut` before either apply, and again whenever the user wants the newer state of the space: a later call moves the result ref. One `bringCodeOut` per space at a time; two at once are not coordinated. Applies as changes and `describeApplyState` take turns by themselves, within one process.
- Keep `changedPaths` of `bringCodeOut` and `appliedPaths` of `applyAsChanges` apart in whatever the user reads. They count different things: the first the paths the result changes against the space's start, the second the paths one apply wrote, which after a first apply is only what is new.
- Ask `describeApplyState` before offering an apply as uncommitted changes, and show its `newPaths`: after rounds were applied, that is what the next apply writes, which `changedPaths` of `bringCodeOut` is not.
- Offer the branch, and only the branch, once `applyAsChanges` has refused once for a space: `changes_route_closed` says the same thing every later time, and its message is written for the user to read as it is.
- Warn about `nestedRepositories` and `unmerged` before the user applies. Both are real work of the agent that does not arrive whole, and nothing else says so.
- Show `changedPaths` and `changedBytes` in the apply dialog. When `applyAsChanges` rejects with `changes_do_not_apply`, offer the branch: its message says that nothing was changed.
- Call `removeSpaceRefs` of code in when the space is removed. It deletes every ref under `refs/openchamber/spaces/<id>/`, so the result ref, the applied ref and what it was built from, the intent and the closed ref go with the start ref, and a test holds that.

What each function rejects with, read from the code:

| Function | Codes |
|---|---|
| `bringCodeOut` | The refusals, before anything reaches the space: `invalid_space_id`, `invalid_space_path`, `invalid_timeout`, `invalid_inner_margin`, `invalid_limit`, `git_version_unreadable`, `git_too_old`, `project_folder_missing`, `not_a_git_work_tree`, `space_start_missing` for a repository that holds no start of this space. Then `result_ref_missing` when a fetch exited 0 and the ref is not there, `result_not_a_commit`, `result_transfer_too_large`, `result_too_large` and `result_too_many_changes`, each with `details.step`. Everything else is `code_out_failed` with `details.step` and `details.cause`, the cause being the place's or the runner's code, `inside_command_failed` for the snapshot inside, or null for a fetch git refused, whose message carries at most 2,000 characters of what git and the space printed. The steps are `reach the space`, `snapshot the space`, `fetch into the quarantine`, `measure the result` and `promote the result`. |
| `applyAsBranch` | `invalid_space_id`, `invalid_branch_name`, `result_missing`, `branch_exists` for a branch of that name and for one that git cannot keep beside it, such as `feature/x` beside `feature` or `topic` beside `topic/one`, with `details.branch` naming the one in the way, `git_command_failed`, the refusals of the repository and the git version above, a runner code, `code_out_failed`. |
| `applyAsChanges` | `invalid_space_id`, `invalid_limit`, `space_start_missing`, `result_missing`, `changes_route_closed`, `changes_too_large`, `name_not_allowed_here`, `case_only_rename`, `changes_blocked_by_link`, `patch_not_possible`, `changes_do_not_apply`, `changes_partly_applied`, `git_command_failed`, the same refusals, a runner code, `code_out_failed`. |

### The snapshot inside

A fixed script under the image's `timeout -s KILL`, every value a positional argument, with the image-only PATH: it copies the space repository's index, runs `add --all` into the copy with `core.splitIndex=false`, writes the tree, and when that tree differs from `HEAD^{tree}` makes one commit on top of HEAD with the fixed identity `OpenChamber <spaces@openchamber.invalid>` and the message `openchamber: uncommitted changes from the space`. When nothing is uncommitted, the result is HEAD itself. It writes `refs/openchamber/result` inside.

Then it reports two things on a channel of its own, file descriptor 3, while everything it runs writes to `/dev/null`: the id of the commit it made, and the unmerged entries of the space's index, read before `add --all` stages their conflicted content and cut at 256 KiB, so a space cannot fail a code out by printing without end. The id is what the fetch then requires. A space can move its own `refs/openchamber/result` between the snapshot and the fetch — a reviewer did it live with a loop inside, and the host delivered one commit while warning about another — so the fetch takes that one commit and nothing else. Everything the script runs writes to `/dev/null` and runs with descriptor 3 closed; only the two commands at the end write the report. That keeps what git and the agent's hooks print out of the report, and a hook from writing into it through what it inherits: its parent git's stdout through `/proc/$PPID/fd/1`, or descriptor 3 itself. A live test does the first from inside a real space, and a unit test the second in the stand-in, each red with its part removed. It does not make the channel private, and nothing here claims so. On Linux a process of the agent's can still reach it: while the block runs, the shell keeps a copy of descriptor 3 to restore afterwards, `dash` on descriptor 10, and `/proc/<pid>/fd/10` opens it for the same user. Checked on 2026-09-24 in the base image, where a child wrote a line through its parent shell's `/proc/$PPID/fd/10` and the line came out on the report's channel. So the report is data from the space, like everything else from it: the id only names the one object the fetch must bring, which is then checked like any result, and a report the agent wrote itself can name another commit of its own, which the agent could as well have made its result, and paths that do not belong to it. The paths are shown, never acted on. The agent's working tree, index and branch stay as they were, and a test compares the status inside before and after. A repository with no commit at HEAD is refused inside, as `code_out_failed` with the cause `inside_command_failed`. The limit inside is five minutes plus the margin.

The space is the agent's machine, so its own config and hooks run during this script: that is inside, and it changes nothing about what the host checks afterwards. The live test plants a `reference-transaction` hook there and sees it run inside, and nothing on the host.

### The quarantine

On the host, a throwaway bare repository in the temporary folder, made with `--template=`, with `objects/info/alternates` pointing at the user's object store, as the sender of code in. It reads the user's objects and copies none, so the space sends only what the user does not have. Then:

```
git -c protocol.ext.allow=always -c fetch.fsckObjects=true -c transfer.fsckObjects=true
    -c fetch.prune=false -c fetch.writeCommitGraph=false -c fetch.unpackLimit=1 -c transfer.unpackLimit=1
    fetch --quiet --no-tags --no-recurse-submodules --no-write-fetch-head --no-auto-gc --refmap= --update-shallow
    ext::<execArgv> /usr/bin/timeout -s KILL <seconds> /usr/bin/git upload-pack <spacePath>
    +refs/openchamber/result:refs/openchamber/result
```

- `--update-shallow` is on this fetch only. Measured by the maintainer's probe: while the space's repository is still shallow, before `sendHistory` finished or after it failed, which code in treats as a warning, a fetch without it printed `rejected ... because shallow roots are not allowed to be updated` and exited 0 without writing the ref.
- After the fetch the host reads `refs/openchamber/result` in the quarantine itself, and a missing ref is `result_ref_missing`. It checks that the ref names a commit. Nothing the space printed is read.
- The object checks refused every hostile tree name the probe tried: `.git`, `.GIT`, `.Git`, `git~1`, `.git ` with a trailing space, `.git.`, `..`, `.`, `a/../../x` and `.git/hooks/x`. The unit test and the live test try them again, built inside with `hash-object -t tree --literally`, and require the refusal at the quarantine, not later.
- The unpack limit of 1 keeps what arrives as a pack that grows while it arrives, so the size cap below can see it. Below the default limit of 100 objects git would write loose objects, each only once it is whole. Measured with git 2.50.1: `fetch.fsckObjects` alone already makes git keep a pack; the setting does not rely on that.
- `--no-tags`, the explicit refspec and `--refmap=` keep every other ref and tag of the space out. The tests plant tags, an annotated tag, a branch and a ref named like another space's start, and the control shows an ordinary fetch following the tags.

The quarantine is removed on every path, a killed fetch's partial pack with it; the tests check that no temporary folder of code out is left.

### Caps and the deadline

- **The deadline.** `timeoutMs`, ten minutes by default, the same rule and range as code in, ends the fetch's whole process tree with `killTree`. The inner limit is the deadline plus `innerMarginSeconds`, so the `upload-pack` inside and what it started are gone by then. Measured by the probe with a `uploadpack.packObjectsHook` that never ends, planted in the space's global config: the tree kill left no host process on any of the three machines.
- **The transfer.** `maxTransferBytes`, 256 MiB by default, twice the changed-bytes cap, because a pack of the new objects is not larger than what they inflate to plus commits and trees. The host polls the size of the quarantine folder every 100 ms while the fetch runs and stops the fetch through `runCommand`'s `signal` when it passes the cap; a fetch that finished between two polls is held to the same cap afterwards. A transfer that was already over the cap when it ended between two polls is refused the same way, and a test proves that check by a result that arrives in one piece, where the space's own sender ends normally. The rejection is `result_transfer_too_large`, whatever exit code the killed tree left: on Windows a tree ended by `taskkill /T /F` exits with code 1, not a signal, measured by the probe, so the code knows it stopped the fetch because it did, not from the exit code. The overshoot is what arrives in one poll: the probe measured about 13 MB at 130 MB/s locally and about 45 KB over SSH.
- **What the result writes.** A cap on the transfer is not enough. The probe sent 1.5 GB of zeros as a 1.4 MB pack, and building the patch then took 1.5 GB of memory on the host. So before anything is promoted, in the quarantine, the host lists the changed paths with `diff-tree -r -z --no-renames <start> <result>`, lists the new objects of the result's history with `rev-list --objects --no-object-names <result> --not <start>`, and sizes both with `cat-file --batch-check`, which inflates nothing: the probe measured 0.02 s and 4 MB of memory for the 1.5 GB blob. Two sums come out of it. `newBytes` is everything the result's history brings that the user does not have, which is what the repository keeps: it must stay within `maxChangedBytes`, 128 MiB by default, and no single object of it that is not a file may be larger than 4 MiB. `changedBytes` is what a patch between the start and the result would read and write, both sides of every changed path, deletions included, which is what an apply costs; `bringCodeOut` reports it and `applyAsChanges` is where it is capped, because that is where it is paid. So a space that deletes a large file, or fills the project with copies of a file the user already has, brings almost nothing into the repository and can still be applied as a branch, while applying it as changes is refused with the branch as the way. Every new object counts, whatever it is, and not only files: a reviewer brought a 64 MiB commit message out under both caps while only files counted, and the object was in the user's repository for good. A commit message and a tree weigh as much as a file, repetitive text travels in a pack of a few kilobytes, and a test refuses a megabyte of commit message and a tree with forty thousand entries, neither of which changes a file. On top of the sum, one object that is not a file may hold at most 4 MiB, which is a tree of about a hundred thousand entries and a commit message no one writes: such an object is the cheapest thing to send and the most expensive to hold, and a reviewer measured 1.03 GB of host memory for a 1 GiB message and 2.94 GB for a 4 GiB one, spent by the fetch before any cap can see them. The transfer cap counts compressed bytes and says nothing about that. `newBytes` catches a file of zeros, a large file the agent committed and deleted again, which the diff never shows and a later checkout of the branch would write, and anything the agent put in a commit message or a tree. `changedBytes` catches what only a patch pays for: a deletion brings no object at all, and measured with git 2.50.1, deleting one 431 MB file cost `diff-tree` 1.09 GB and `apply` 1.67 GB, while deleting a 1.36 GB one made `diff-tree` refuse to build a diff at all. Over a cap is `result_too_large` at the bring-out and `changes_too_large` at the apply.
- **How many.** `maxChangedEntries`, 100,000 by default, bounds both the changed paths and the new objects, and with them every list the host reads. Over the cap is `result_too_many_changes`. A list longer than 64 MiB is refused the same way, and the message says that it was the list's length, not a count: 64 MiB holds about 60,000 paths of a kilobyte each.
- **Folders.** The folders the changed paths pass through are a cost that neither the paths nor the bytes show. A reviewer brought out 20,000 files 500 folders deep, which shared one chain of trees, 503 objects and 663 KB: they passed every cap, and the checks before an apply then took 4.4 GB and ran the server out of memory, every time it was tried. So `applyAsChanges` counts two more things, in the same pass over the listing as the paths, before anything else is built: the folders, each once, against a cap of the same size as the paths, `maxChangedEntries`; and the steps, how deep each of those folders and each changed path lies, against 32 times that, 3.2 million by default. The folders bound the host's memory: the name check and the link check keep one entry per folder. The steps bound its time: every look at a folder and every file `git apply` writes goes down from the top of the work tree one folder at a time. The folders alone were not enough, measured on macOS: 95,520 files in 95,520 folders 480 deep, within the folder cap, took the checks 173 s and `git apply` 148 s for its dry run and 307 s for the apply, which is half the apply's deadline. diff-tree lists the paths of one folder together, so a folder is counted when a path enters it and not again for the paths inside it that follow, holding one path at a time; a listing in another order would count a folder again, which refuses sooner and never lets more through. Over either count is `changes_too_large`, touching nothing, and the branch holds the work. `bringCodeOut` refuses neither, because the folders are what a patch pays for, like `changedBytes`, and the refusal has to be able to point at the branch.

Measured for the folder counts on macOS 26 with git 2.54.0, on APFS, with the objects packed as a fetch leaves them, wall time and peak memory of the whole call from `/usr/bin/time -l`, before and after:

| Result | Before | After |
|---|---|---|
| 20,000 files `x<i>/a/.../a/f`, 500 folders deep, one shared chain | out of memory after 21 s, 4.4 GB | `changes_too_large` in 5.7 s, 117 MB; 5.4 s of it is git listing the change |
| 2,000 files 450 deep, their folders on disk, a repository that compares case | applied, 157 s, 549 MB | `changes_too_large` in 0.7 s, 53 MB |
| 95,520 files at every level of 199 chains 480 deep, folders on disk | applied, 595 s, 462 MB | `changes_too_large` in 0.4 s |
| 6,123 files at every level of 13 chains 470 deep, just under the step cap | | applied in 37 s, 107 MB: checks 10 s, dry run 8 s, apply 18 s |
| 20,000 files in shared folders, three deep | applied in 1.9 s, 104 MB | applied in 2.0 s, 105 MB |
| 20,000 files, each three deep in folders of its own | applied in 5.4 s, 142 MB | applied in 5.4 s, 145 MB |

Why 128 MiB, measured on macOS with git 2.50.1 on one machine, for the worst case this cap allows: a 64 MiB text file rewritten line by line, which charges 128 MiB because both sides count, made a 137 MB patch on disk, `diff-tree -p --binary` peak at 0.41 GB of memory and `apply` at 0.54 GB. The same shape at twice the cap took 1.6 GB and 2.2 GB, and a patch of 1 GiB is refused by `git apply` itself as too large. The caps are arguments, so a caller can give a larger one to a user who asks, and `applyAsChanges` takes its own pair.

What the host can still be made to spend before any of this refuses: the fetch inflates every object it indexes, so one object costs its own inflated size in memory, and a transfer costs the cap in disk. Both are paid inside the quarantine, which is thrown away, and git offers nothing to bound a fetch while it runs.

### The promote

From the quarantine into the user's repository, with the same config and flags except the ext switch, the unpack limit and `--update-shallow`: `+refs/openchamber/result:refs/openchamber/spaces/<id>/result`, killed with its tree at the deadline. The user's repository never becomes shallow. A space that forges its `shallow` file so that the promote would need to make it shallow gets the same warning and exit 0 from git, measured by the probe; the host then reads the ref, finds it missing or not holding the quarantine's commit, and rejects with `result_ref_missing`. The unit test forges one and checks that no `shallow` file appears in the user's `.git`.

### Apply

`applyAsBranch` checks the name with `git check-ref-format --branch` and requires the output to be the name as given, because that command also expands `@{-1}`. A name that starts with `-`, holds a control character, or is not a string is refused before git sees it. An existing branch is refused with `branch_exists` and never moved: the host looks first, and `update-ref` creates the branch against the zero id, so a branch that appeared in between is not moved either. Nothing is checked out and no configuration is written, so `branch.autoSetupMerge` has nothing to act on. The branch holds the agent's commits and, on top, the uncommitted rest as one commit when there was any.

`applyAsChanges` measures its own pair first, the result against where the last apply left the working tree, which is a pair `bringCodeOut` never saw: over its caps it refuses with `changes_too_large` and names the branch. Then it builds the patch in the user's repository, into a file in the temporary folder, never in the host process's memory:

```
git diff-tree -r -p --binary --full-index --no-renames --no-ext-diff --no-textconv --no-color --src-prefix=a/ --dst-prefix=b/ --output=<file> <start> <result>
git -c apply.whitespace=nowarn -c apply.ignoreWhitespace=no apply --binary --whitespace=nowarn --check <file>
git -c apply.whitespace=nowarn -c apply.ignoreWhitespace=no apply --binary --whitespace=nowarn <file>
```

A space is applied more than once: the user applies, the agent works on, the work comes out again, and the user applies again. The maintainer's decision of 2026-09-22 is that the second apply brings only what is new since the first one. So the host remembers what it applied, as `refs/openchamber/spaces/<id>/applied`, a service ref beside the start and the result, which also keeps that commit through a garbage collection. The patch goes from that commit when it exists and from the start when it does not, and the ref is written only once the apply has gone through, never after a refusal. `applyAsBranch` does not touch it, in either order. Applying the same result twice has nothing to apply, rather than a refusal, and a second apply that collides with the user's own work touches nothing and offers the branch, like the first. When writing the ref fails after the changes are already in the working tree, `remembered` is false and the changes are there all the same; the intent the apply wrote stays, and the next call finishes the record, see below.

The user may also throw an apply away: apply, dislike it, `git restore` what it changed and remove what it added, have the agent redo it, bring the work out again and apply. The patch from `applied` would then bring only what is new, or nothing, and the thrown-away round would be lost. The maintainer's decision of 2026-09-25, made exact in the seventh and eighth rounds: "thrown away" is decided path by path from what is authoritative, a part thrown away is never passed over in silence, and an edit of the user's, committed or not, is never a throw-away. So every apply also records what its patch was built from, `refs/openchamber/spaces/<id>/applied-from`, the start or the apply before it, and the commit the user's HEAD named then, `refs/openchamber/spaces/<id>/applied-head`, absent while HEAD has no commit. Both move with `applied` in one ref transaction. Each path of the last apply is then one of three things, in `whatBecameOf`:

- **kept**: the working tree holds the side the apply wrote;
- **edited**: it holds neither side, an edit of the user's; or it holds the side before the apply and a commit of the user's changed that path since: a commit reachable from this worktree's HEAD and not from `applied-head`, found in one `git log --name-only` over that range and every path, asked once per call and shared by every question of the call, bounded by the read-back time and the listing cap; see below for why and what it costs. A revert is such a commit. A move back, a reset or a checkout of an older commit, adds none, so the paths it put back count as thrown away;
- **thrown away**: it holds the side before, and no commit since changed it.

`decideBase` then chooses where the next patch goes from, in this order:

1. A route that is closed stays closed: the refusal comes first, before anything is read, whatever the working tree looks like.
2. Every path of every apply so far thrown away, by the same rule over the change from the start to `applied`: the whole work comes from the start. A reviewer threw away both of two applies, and the patch from the first apply's result applied without round one and said nothing.
3. Every path of the last apply thrown away: the patch goes from `applied-from`, even where the patch from `applied` would fit, and even when the same result is applied again with nothing new brought out.
4. Some paths of the last apply thrown away and others kept or edited: the apply is refused with `changes_do_not_apply` and the route closes. `details.thrownAway` and `details.stillThere` name the two parts, `{ count, paths }` each, and so does the message, which says that the branch holds everything. It says the files still there are "still there or changed since", not changed by the user: a colleague's change pulled into one of them lands there too. Where one of them differs from what HEAD holds, as `git status` compares, the message adds that the files of the space still in the project must be removed or committed before that branch can be merged or switched to: a reviewer took the branch after this refusal, and git refused both the merge and the switch over those files. A file the user committed as it is stands in the way of neither, and then the message says nothing of it; when the host cannot tell, it keeps the advice. The bot had `git restore .` put back what round one changed, leaving the file it added, and a redo that touched only another file: the patch from `applied` fit and the thrown-away part was gone without a word.
5. Nothing thrown away: from `applied`.

Only commits the last apply's HEAD does not have count, whatever HEAD is now: in another linked worktree, which shares the refs of a space and has a HEAD of its own, on another branch, or after history was rewritten, the commits of the user's since are the ones that count. A first version compared the two HEADs' trees instead, and that took a reset to before the apply for the user's change, so a user who committed a round, applied the next and reset hard to before both got the rounds no commit touched and lost the committed one without a word; and a revert, whose trees are the same as before, for nothing at all. A reviewer applied round one in one worktree and round two from a second, which held nothing of round one, and got round two alone; now round one is thrown away there and the whole work comes, and back in the first worktree, which holds round one and not round two, the part is refused. The seventh round took any move of HEAD for keeping the whole of the last apply, and reviewers showed that wrong both ways: a user who threw an apply away and then pulled, switched branch or reset got the next round without the first and no word, and one who then had the agent redo it got a refusal that closed the route.

The list of the user's commits is one `git --literal-pathspecs log --format= --name-only -z --no-renames --no-show-signature <HEAD> ^<applied-head> --`, with no path filter, read into a set once per call and range and asked in memory by every question of the call: the look at every apply and the look at the last one share it. The eighth round limited the log to the paths still undecided, and git then compared each commit against each of those paths. Measured on macOS 26 with git 2.54, one run each, a last apply of 100,000 files three folders deep, every one put back, then 10,000 commits of the user's, eighth round against this: commits of other files, the log 74 s against 2.9 s and `describeApplyState` 78 s against 6.8 s; commits that touch 10,000 of those files, the log past the five minutes, so every call answered `changes_undecided`, against 0.3 s and a `describeApplyState` of 4.0 s; the same 10,000 commits ahead in a second linked worktree, 79 s against 6.6 s. Peak memory 210 MB against 240 MB. No history is simplified, so a commit of a side branch that a merge brought counts even where the merge dropped its change: that path is then taken for the user's edit, the safe side, never for a throw-away. Merges list nothing of their own. The log is not asked when no path is back as before, and its list prints while git walks, so a host that dies stops it at its next write; see "A folder of ours after a host crash". `--no-show-signature` keeps a user's `log.showSignature` from checking a signature per commit.

Reading the working tree back writes nothing, anywhere, and runs no smudge filter. The host looks at each path itself: a folder on the way that is not a plain folder, a link to another place among them, makes the path read as gone, as `git status` reads it, and nothing is read through it. A file is hashed as a blob by the host, 64 KiB at a time and never held whole. It is opened without following a link and without waiting, `O_NOFOLLOW | O_NONBLOCK`, and must be a plain file once open: a verifier swapped a FIFO in between the host's `lstat` and its open, and the open waited for ever, where no deadline is checked. A file that changes size while it is read, as when the user's editor saves it at that moment, is read again, up to four reads 100 ms apart within the call's time; one that keeps changing, or that cannot be read as one plain file, because the open or a read fails or it is something else once opened, makes the call `changes_undecided` with `details.cause` `read_back_failed` and the file named in the message. Tests make a file change during two reads, then hold still, and during every read. It is never handed to git instead, which would follow a link or wait on the FIFO. A test swaps a FIFO in, and another a link to a file outside that holds exactly what the apply wrote. A file that is byte for byte one side's blob holds that side, the applied side first: that is what `git apply` wrote, or `git restore` put back, wherever no filter and no line-ending conversion changes a byte, and it needs no git at all. Only a file that is neither side's bytes is read the way `git status` reads it, through the user's clean filters and line endings, by one `git hash-object --stdin-paths` for all such files, whose answers stream back; a name with a line break or a leading quote, which that input cannot carry, is asked on its own. The attributes are exactly those the working tree holds now, with the user's global attributes file and `info/attributes`: git reads each folder's `.gitattributes` from the working tree and, where one is missing, would fall back to the index, so it is given an index that does not exist. A `.gitattributes`, `.lfsconfig` or `.gitmodules` from the space's side is never written anywhere and never read. The seventh round compared through a temporary index holding the sides of the change and checked `to` out into a folder of ours, and a verifier had the space's filter run twice: on the user's own file, when an interrupted apply had not written the `.gitattributes` it brought and git fell back to the temporary index, and in our folder, on a `.gitattributes` the user had thrown away. A `.gitattributes` the user kept from an apply is the user's now, and the apply itself honours it, so reading back does too; a test shows both. A path whose two sides no disk can tell apart, a gitlink on either side or a change of the exec bit alone where `core.filemode` is off, is left out. When nothing is left, that is no evidence that anything was thrown away, and counts as kept.

The check of step 2 stops at the first path that is not thrown away, looking at one path first and sixteen times more each time; a test counts the files it reads. Step 3 reads every path of the last apply, because a thrown-away one can be any of them: by bytes alone where the files are the applied blobs, which a test proves with a clean filter that would take seconds and never runs. Measured on macOS 26 with git 2.54, a last apply of 100,000 files three folders deep, then a round of one file, one run each, the seventh round's code against this: kept, `describeApplyState` 8.6 s against 3.7 s and the apply 7.9 s against 3.7 s; kept with a clean filter that starts a program per file, 273 s against 3.7 s, measured by a reviewer before; every file but one thrown away, the refusal 24 s and 425 MB of temporary files against 3.6 s and none; a clean filter that is neither side's bytes for every file still pays the filter, and at 3 ms a file ran into the five minutes and answered `changes_undecided`. Peak memory 230 MB either way. The refusals of the patch that follows are the ones any apply has: too large or a name this computer cannot hold leave the route open, a link on the way and a failed dry run close it. On success `applied`, `applied-from` and `applied-head` move as for any apply, `applied-from` staying where it was after a throw-away, so a second throw-away comes back to the same place. A repository whose last apply was recorded before `applied-from` existed has no earlier base and behaves as before.

Reading the working tree back is bounded in time, `readBackTimeoutMs`, five minutes, for the whole call: each git command gets what is left of it, and the host checks it while it hashes a file. The seventh round read every file of the last apply through the user's clean filters, and a reviewer measured 276 s for a hundred thousand kept files with a clean filter that starts a program per file; now such files are read by bytes, and only files that are neither side's bytes pay for the filter. Where that does not end in time, it is `changes_undecided`: the route closes, with an intent where there is one in the same transaction, `details.recorded` saying whether it went through, and the message says that it is not known how much of the last apply is still there. A clean filter of the user's that fails while reading back ends the same way, with `details.cause` `read_back_failed` and what git said, rather than failing every later call. A dry run that does not end within the deadline of the apply ends the same way. `describeApplyState` says so with `newPathsUndecided` and writes nothing. Every git command of the read-back, every dry run and the real apply end with their whole process tree, `killTree`, when they run out of time, and all but the real apply also when the server leaves, see "Killing a transfer": git starts the user's filters as children of its own, and a verifier found one hanging clean filter left behind by each of five calls that ran out of time, while only git was killed. Tests hold a clean filter that never ends against a read-back, a dry run and an apply that each run out of time, and require it gone.

Both apply commands run at the top level of the work tree, whatever folder of it the caller names: from a subfolder, `git apply` ignores the paths outside it. Plumbing ignores `diff.noprefix`, `diff.relative`, colours and mnemonic prefixes, measured by the probe; the porcelain `git diff` broke the patch with the first two. `apply.whitespace=fix` made apply change content without a word and `apply.whitespace=error` made it refuse; `apply.ignoreWhitespace=change` would let the patch land on lines whose whitespace differs. The tests show each setting breaking the obvious command first. Never `--3way`, which fails on a working tree with unstaged edits and changes the index; never `--index`; never `--unsafe-paths`, so `git apply` keeps refusing paths outside the work tree and writes beyond a symbolic link. When the work of a space and the user's project have gone apart, this route closes for that space. The maintainer's decision of 2026-09-23: the user is told that once, at the refusal, and from then on the space is applied as a branch, which holds its whole work, the rounds already applied included. Nothing is applied in part and nothing is lost. The closure is `refs/openchamber/spaces/<id>/changes-closed`, at the result that was refused, so it survives a restart, a new `bringCodeOut` and anything else, and `removeSpaceRefs` takes it with the rest. A refused dry run closes it, and so does an apply that stopped in the middle, because the project is then in a state this cannot reason about. After that `applyAsChanges` refuses at once with `changes_route_closed`, before it builds anything, whatever the project looks like by then. `applyAsBranch` works throughout and closes nothing, and nothing in this stage opens the route again.

Before it builds the patch, the host checks every name the patch creates, each folder and file on the way, against what this computer's file system can hold, and refuses with `name_not_allowed_here`, touching nothing, when one cannot exist here. The message names the file, redacted like every reported path, says the agent made a name this computer cannot hold, and asks for the agent to rename it and the work to be brought out again. That refusal leaves the route open, by the maintainer's decision of 2026-09-24: the project and the space did not go apart, and after a rename the next `bringCodeOut` and apply go through as usual. The rules are per platform on purpose, in `nameNotAllowedHere`, each with its source in a comment: on Windows, from Microsoft's naming documentation and git's own `is_valid_win32_path`, refused when either refuses it, the characters `< > : " / \ | ? *` and 1 to 31, a trailing space or period, and the device names CON, PRN, AUX, NUL, COM1 to COM9, LPT1 to LPT9, alone or followed by spaces and an extension or a colon, in any case, with the superscript digits Microsoft names and the LPT0, CONIN$ and CONOUT$ git adds. macOS and Linux refuse only NUL and `/`, which never come out of a git tree. On every platform a name longer than 255 bytes is refused, the longest name ext4, APFS and NTFS hold, and so is a path of 1024 bytes or more with the project's own path in front of it, macOS's PATH_MAX with its terminating null: a reviewer found that a path within the limit below the project still ended in `ENAMETOOLONG` and a generic `code_out_failed` once the project's path was added, and 1023 bytes opens on this Mac where 1024 does not. `git apply` itself would write such a path, because it works below the project, but the file is then out of reach of whatever opens it by its whole path: the test's own clean-up failed on it with `ENAMETOOLONG` when the check was removed. On Windows also, unless the repository has `core.longpaths`, a whole path past 259 characters and a folder whose whole path is past 247, the limit Microsoft gives for `CreateDirectory` (MAX_PATH - 12, room for an 8.3 name) and the one Git for Windows' `mingw_mkdir` applies. A path too long has a message of its own that says so.

Whether two names that differ only in case, or only in the Unicode form of an accent, are one file is the repository's own answer: `core.ignorecase` and `core.precomposeunicode`, which git sets by probing the disk when it makes the repository. So a case-sensitive macOS volume is not refused, and a case-blind disk on Linux is checked; the platform still decides the Windows character rules. How wide "only in case" is, is the disk's, and APFS is wider than lowering a name: it folds case fully, so `strasse` and `straße`, `σ` and `ς`, `fi` and `ﬁ`, `s` and `ſ`, `β` and `ϐ` are one name there, and a reviewer lost a file to the first pair. So names are folded lower, upper, lower again, which also joins `ẞ` with `ss`, and composed wherever case is ignored, because APFS ignores the form there too; the name check and the link check use this one fold. Checked on this Mac's APFS for every character of the Basic Multilingual Plane, each after an `x`: the disk took 1,678 names for one already there, and the fold joined every one of those pairs and every longer spelling the disk joined, where lowering alone missed 57; it joins one pair the disk keeps apart, `ı` and `I`. A test repeats the pairs above on the disk itself. NTFS compares through a table that upcases one UTF-16 unit at a time, which is narrower, so there the fold is wider than the disk. That only ever refuses more in the name check, and never lets a lost file through; in the link check it lets a real path pass as the same folder when it differs from git's spelling by more than the disk's own fold, and by reasoning that lets no redirection through, because a real path names a different folder only through a link, which `lstat` reports first, or through a volume mount point, whose real path is on another volume. A rename `strasse.txt` to `straße.txt` on such a disk is a `case_only_rename`, and the route stays open. Where the repository says so, a name the patch creates is compared with the other names of its folder in the tree the patch leads to. Another spelling that stays there is `name_not_allowed_here` with `details.other` naming it: without the refusal, `git apply` would take the second name for the first, refused as "already exists" with the wrong cause when the first is on disk, or written over it when both are new. Another spelling that the patch removes is a rename that changes only the case, `case_only_rename`: `git apply` cannot turn one into the other on such a disk, because the new name "already exists", so the message says that, that nothing was changed, and that the branch holds it; the route stays open, and a new name brings the work out normally. That was chosen over making the rename apply, which would take a patch that `git apply` does not write in one step here, and all or nothing matters more. A pair of spellings that both existed before the patch is the user's own and passes, so a project that already has `Docs/` and `docs/` can take new files in either.

The check keeps only the folders on the way of the paths the patch creates, one node per name, and passes over every other path of the tree without holding it. Measured on macOS: a tree of 203,000 files with one new file took 58 ms and 3 MB of memory, against 477 ms and 69 MB before; one path 2048 folders deep with 255-character names, which used to take 572 MB and ran Node out of memory at sixteen such paths, is now refused by the length rule in a millisecond.

Before it builds the patch, the host looks at every folder on the way to every path the patch writes or deletes, in the user's work tree, and refuses with `changes_blocked_by_link`, touching nothing, when one of them leads somewhere else. `git apply` is not trusted with this: measured on Windows 11 with Git for Windows 2.54, it wrote through a directory junction in the user's work tree into the folder the junction points at, where git on POSIX refuses with "beyond a symbolic link". A folder leads somewhere else when `lstat` reports it as a link, which Node does for a symbolic link everywhere and for a junction on Windows, because libuv reports both kinds of reparse point as a link; or when its real path is not that same folder inside the real work tree, which catches a redirection that is not reported as a link. Names are compared the way the repository says its disk compares them, `core.ignorecase` and `core.precomposeunicode`, the same folding the name check uses: a folder stored with its accent decomposed, as a macOS disk may hold it, is the folder git names in the composed form and not a link. The real path is asked once per path, of its deepest folder that exists, which covers every folder above it. Measured on macOS against asking every folder: 20,000 paths each in a chain three folders deep of its own took 1.1 s instead of 2.0 s, 5,000 chains 16 deep 1.1 s instead of 3.5 s, and 1,000 chains 64 deep 1.1 s instead of 6.3 s; where paths share their folders the two were the same, because every folder was already asked once. Folders that do not exist yet are fine, because apply makes them. A link that points inside the work tree is refused too: the space made a plain folder where the user has a link, so the two went apart. The one link that is not in the way is one the patch itself deletes as a link, mode 120000, where the space turned a tracked link into a folder: `git apply` removes the link before it makes the folder, and its dry run first requires the link on disk to be the one the patch deletes. Nothing else is passed over. The refusal closes the route like any other, and its message names the folder. How many folders it looks at, and how deep, is bounded before it runs; see "Folders" under the caps. A symbolic link trips both checks at once on POSIX, so a unit test gives the check a fake of each kind to prove each one alone; the junction itself is covered by the same tests on Windows, which build one there.

An apply can be cut off by the host itself: the server killed, the computer turned off, a Ctrl-C to the whole process group. `git apply` runs with `killTree` for its timeout and with `keepAtExit`, so a server that leaves, through its own handlers or killed outright, leaves it running to its end on POSIX: killed in the middle it would leave a part written and close the route, while left to run it usually finishes, and the next call records it. On Windows libuv's job object ends git with the server whatever is set, as it did before. Such a child has no pipe back to the host on its output either: a reviewer's apply, kept alive, still wrote its stderr into a pipe to the host that had gone, and a smudge filter that printed there got SIGPIPE and stopped the apply with files deleted, 15 and 17 of 20. Its stdout goes nowhere, and its stderr into a file the host opened in the system's temporary folder and removed at once, which the host and the child each keep open, so no clean-up can break the child's writes; the host reads the last 64 KiB of it at `close`, the window `keepTail` keeps. A test holds the options of both apply commands, and `run-command.test.js` a `keepAtExit` child alive after `process.exit`, and a real `git apply` with a smudge filter that prints to stderr, finishing every file after its host left, red with the pipes back. So just before it runs, the host writes the intent, `refs/openchamber/spaces/<id>/applying` at the result, with `applying-from` at what its patch was built from and `applying-head` at the user's HEAD, in one transaction, and replaces them with `applied`, `applied-from` and `applied-head` in one transaction once the apply is done. That is how a crash in the middle of an apply of the whole work again is read against the right base; an intent written before `applying-from` existed was built from `applied`, which has not moved since. A later call that finds the intent looks at the working tree first, for the paths of that patch only, read back as above: when every path holds the result, the apply finished and was not recorded, so the record is finished and the call goes on from there; when every path still holds what was there before, nothing was written, so the intent is dropped and the call goes on. The user may have edited something since, which leaves a path as neither, and a reviewer had an ordinary edit of another line in a touched file called a partial apply and the route closed. So then the dry run an apply makes decides: when no path holds the result alone and the patch still applies, nothing was written; when no path holds the old content alone and the patch applies backwards, all of it was. The first half of each condition matters: `git apply` writes a whole file at a time, so a file it wrote holds the result alone, and a test shows that without it a file of repeated lines would take the same insertion a second time. When that reads as a part, the working tree is read again against where the applies before it came from, `applied-from` and then the start, and the first answer that is not a part counts, with that base: the user may have thrown an earlier apply away after the host went away, and a reviewer had an attempt that wrote nothing, followed by such a throw-away, taken for a part and the route closed. Every dry run keeps only the last 64 KiB of what it prints, and so does the real apply, because the exit code is the answer: a reviewer had a check fail on tens of thousands of paths and print past the host's cap of four megabytes, which stopped it, and every later call failed the same way with the intent still there. A test makes a dry run fail on 8,000 long paths, about 5 MB of output, for an apply and for the recovery of an intent. Anything else is `changes_partly_applied`, with a message that says an earlier apply was interrupted and part of it is in the project, and it closes the route at the result that was being applied. The closure and the removal of the intent are one ref transaction, there and after an apply that stopped in the middle, so a host that dies in between leaves both or neither, and never a closed route beside an intent; `details.recorded` says whether the transaction went through, and when it did not, the next call finds the intent and sorts it out again. The same intent is what a record that failed after the apply leaves, where `remembered` is false, and the next call finishes that record the same way. `describeApplyState` reports the intent as `interruptedApply`, reads it the same way without writing anything, and counts `newPaths` from what the interrupted apply wrote when the working tree holds it.

Two applies of one space at once each passed the dry run against the same working tree; the second then wrote over the first, failed and closed the route, three times out of three for a reviewer. So applies as changes take turns, per repository and space: the repository's common git folder, where every worktree of it keeps these refs, by its real path. Two linked worktrees of one repository share the refs of a space, so a call from one waits for a call from the other; a test holds an apply in one worktree at its `git apply` and requires a read from the other to wait for it. A call waits for the one before it, however that one ended, and then answers from what it left, which is nothing to apply, or only what is new. `describeApplyState` takes the same turns, so it never reports an apply under way as interrupted. The turns are kept in the process, at module level like the history runs of code in, so two instances of code out share them; two processes are not coordinated, and neither is `bringCodeOut`.

Three ways it can end other than applied. A patch git cannot build at all, which happens for a single file too large for it, is `patch_not_possible` in our words with git's text after it, and points at the branch. A dry run that fails is `changes_do_not_apply`, nothing was touched, and the message says whether the two grew apart since the space was made or since the work was last applied here. When a file the patch adds is already on disk and git ignores it here, through the user's global ignore file or `info/exclude`, the message says that instead, because the file was there before the space, and names it in `details.ignoredInTheWay`; the route closes all the same. When a file the patch changes is kept by a filter such as Git LFS, and the working file is byte for byte the blob an earlier apply wrote while git reads it back through that filter as another file, the message says that files like that cannot be updated as uncommitted changes a second time, and names them in `details.filteredInTheWay`; the route closes. A reviewer had the agent change a file an LFS-like filter keeps: the first apply wrote the agent's bytes, which the filter reads back as another pointer, and the next round on that file was told that the project had changed. Both hints are asked after the route is closed, and a failure of either leaves the ordinary message. A real apply that fails after a clean dry run, for a folder that cannot be written or a disk that is full, is `changes_partly_applied`: `git apply` writes file by file, so part of the work may already be in the project, the message says so and asks the user to look before going on, and the remembered ref is not moved, because it no longer describes what is there. A test makes that happen with a folder the user cannot write, and holds the promise to what is true: the locked file kept its content, what else git had written by then is its own order, and nothing was remembered.

The dry run is the documented clean-apply decision: a user who edited a line the space changed, an untracked file of the user's where the space adds one, a file the user deleted, or a link of the user's where the space writes a folder each give `changes_do_not_apply`, and the repository folder is byte-identical afterwards, `.git` and the working tree included. An empty result resolves `nothing_to_apply` and touches nothing.

### What code out does to the user's repository

- `bringCodeOut` adds the space's new objects, as loose objects or as a pack, whichever git's own unpack limit picks, and writes `refs/openchamber/spaces/<id>/result`, which a later call moves. Nothing else: no `FETCH_HEAD`, no `shallow` file, no tag, no other ref, no commit graph, no gc or maintenance, and none of the user's hooks or fsmonitor. The tests compare the whole repository folder with `hostState` and allow exactly those differences through `unexpectedChanges(..., { codeOut: true })`. A test of the comparison itself feeds it a pack of the user's rewritten, `FETCH_HEAD`, a `shallow` file, a commit graph, a branch, the result ref of another space and a changed working tree file, and requires it to see each. After a refusal of the space's content, nothing of it is in the repository at all: the tests compare with no allowance.
- `applyAsBranch` adds the branch and its reflog.
- A refused `applyAsChanges` writes one thing, `refs/openchamber/spaces/<id>/changes-closed`, and nothing else: the tests compare the whole repository folder and allow exactly that one path.
- `applyAsChanges` reports what it wrote as well: `nestedRepositories` from the paths it applied, and `conflicted`, the paths whose applied content holds a conflict marker, found in one pass over the patch it built. That one is a text check and no more: a file that talks about merge markers counts too. It changes the working tree, and in `.git` only `refs/openchamber/spaces/<id>/applied`, `applied-from` and `applied-head`, which a later apply moves, and the intent while it runs. The index, HEAD and every other ref stay as they were, and a test compares `.git` with and without that one allowance.
- The user's global hooks and fsmonitor run in none of it, with the same empty hooks folder and empty `core.fsmonitor` as code in. The test gives the user fourteen global hooks and an fsmonitor program, shows them running on ordinary commands, and requires nothing of them after a whole code out and both applies. A second test gives the user automatic gc, maintenance and `fetch.writeCommitGraph=true`, shows an ordinary fetch consolidating two packs and writing a commit graph, and requires neither after code out.

### Known limits

- **Filters.** `applyAsChanges` runs the user's own configured filters, clean and smudge, Git LFS among them, for files that a `.gitattributes` already in the user's working tree covers, as a `git pull` from someone else would. A filter named by a `.gitattributes` that arrives in the same patch does not run: the test defines both filters globally, shows both running on an ordinary `add` where their attributes are, and then sees only the user's own run during apply. `applyAsBranch` runs none. This is the maintainer's decision of 2026-09-22, recorded in DESIGN.md. Reading the working tree back runs only clean filters, only for files that are neither side's bytes, and only with the attributes the working tree holds; it runs no smudge filter, so it never downloads an LFS object.
- **A file a filter such as Git LFS keeps, changed a second time.** Git LFS files arrive in a space as pointers, so the agent changes such a file by writing its own bytes, and the first apply writes those bytes into the user's file. The filter then reads that file back as a pointer to new content, which is not what the result holds, so a later round that changes the file again cannot fit: the apply is refused with a message that says so, names the files, and closes the route. The branch holds everything. The same holds for any clean filter that changes the bytes, such as an encrypting one.
- **The exec bit on Windows.** With `core.filemode=false`, measured by the probe, the uncommitted variant writes the content and loses the exec bit. The branch keeps it. On macOS and Debian the working tree after the apply equals the result tree, mode included.
- **Line endings.** The user's `core.autocrlf` and `eol` attributes convert what apply writes, as they would for any patch; the content is the same once git reads it back.
- **A repository the agent made inside its project** arrives as a gitlink, from the snapshot's `add --all`: its commits never travel, the uncommitted variant makes an empty folder for it, and the branch points at a commit whose gitlink the user's repository cannot resolve. `bringCodeOut` reports those paths in `nestedRepositories`, as code in reports untracked repositories in `notTravelling`, so the caller can warn. Nothing is refused.
- **A conflicted merge in the space** comes out as content: `add --all` stages the conflicted files as they are, so the conflict markers are in the result, the merge's second parent is not, and applying writes the markers into the user's files. `bringCodeOut` reports those paths in `unmerged`, from the snapshot's own report, and `applyAsChanges` reports the markers it wrote in `conflicted`.
- **A path a space names with a control character** is counted like any other and shown with those characters replaced by `?`. Counting comes before redacting, so a warning cannot be made to disappear by naming a file with a newline.
- **Work the user undid by hand after an apply** is not brought again: the next apply starts from what was applied, not from the working tree, and nothing in the result says that a path was skipped. The branch holds all of it.
- **A name the host cannot hold** is refused before an apply with `name_not_allowed_here`, and a rename that changes only the case with `case_only_rename`, see "Apply". The branch is not checked: it still holds such a file, and checking that branch out on Windows will fail on the same name. The case check covers names the patch creates, not a user's own files that differ in case from each other.
- **A folder of ours after a host crash.** A call that is killed with the host leaves its temporary folder, `openchamber-code-out-*` in the system's temporary folder: for `bringCodeOut` measured at 128 to 139 MB with a quarantine in it; for an apply or `describeApplyState` only the patches it built, which the caps bound, since reading the working tree back writes nothing. A `git hash-object` that is reading back when the host dies ends at its next answer, because its output goes nowhere: measured on macOS, a host killed while `hash-object` read 100,000 edited files through a slow clean filter left no process one second later, where the seventh round's `update-index --refresh` ran on under PID 1. A clean filter that never ends would still keep it waiting, as it would keep `git status`. The `git log` of the user's commits prints as it walks, so it ends at its next write once the host is gone: measured with the host killed 0.5 s into a log of 10,000 commits, nothing was left at the first look, where the eighth round's log limited to 100,000 paths ran on for 76 s, and past ten minutes, when it was killed by hand, for commits that touch those paths. Before its first name git walks the whole range once, so a long range keeps it that long: with 400,000 commits it was gone 1.3 s after a host killed 0.4 s in. The dry run, `git apply --check`, is not killed with a host killed outright; it only reads, and ends on its own; a filter of the user's that prints to stderr after the host is gone gets SIGPIPE there, which ends only the read. The apply is not killed at any exit of the host, and its output goes to a file rather than to a pipe to the host, so a filter printing after the host is gone does not stop it half written, see "Apply". It is not swept: a later call cannot tell a folder whose process died from one whose process is still running, because a process id can belong to somebody else by then, and removing a live call's quarantine would break that call. The system's own clean-up of its temporary folder removes it in time.
- **Work the user threw away after an apply** comes again only when the whole of the last apply is gone, path by path, see "Apply". After throwing away part of it the next apply is refused, and only the branch holds what was discarded. A part thrown away of an apply before the last one is not seen: only the last apply's paths are read for that.
- **A throw-away, then a colleague's change pulled into a file of it.** The user throws the whole last apply away and then pulls or rebases onto a colleague's commit that changes one of those files. That file now holds neither side of the apply, which is an edit, so the apply is refused as a part and the route closes, though the whole work would fit. Nothing is lost: the branch holds everything. The rule does not look at whose commit brought the change, so it cannot tell this from an edit of the user's; the message says "still there or changed since" for that reason. It stays that way on purpose: an edit is never taken for a throw-away.
- **An amend that drops a file of the last apply.** The user commits the apply and then amends that commit to drop one of its files, putting it back as before. The amended commit replaces the one that held the file, so no commit since the apply changed it, and it counts as thrown away beside the files that are kept: a part, refused. The same undo made by a new commit on top counts as the user's change, and the next apply brings only what is new.
- **A change a merge dropped.** The user's commits are listed with no history simplification: a commit on a side branch that a merge brought counts even where the merge dropped its change. The path is then the user's edit, never a throw-away, so at worst the whole work does not come again and a part is refused, with the branch holding everything.
- **A space made over the user's uncommitted edits** cannot be thrown away with git commands alone. Its start holds those edits, so "as before the apply" means with the user's own lines in; `git restore` takes the file back to the index and removes them as well, and the file then holds neither side, which counts as kept, or, beside files that are back, as a part thrown away. Putting back exactly what was there before, the user's lines included, is what counts.
- **A dotless ı beside a capital I.** The name check folds `ı` and `I` together, which this Mac's APFS keeps apart, so a new file whose name differs from another only by that pair is refused with `name_not_allowed_here` on a disk that ignores case, though the disk would hold both. The route stays open, and the branch holds it.
- **Reading back is bounded, and costs a read of every file of the last apply.** An apply after the first, and every `describeApplyState`, reads each file the last apply changed to know whether any of it was thrown away: by its bytes, and through the user's clean filters where the bytes are neither side's; see the measurements under "Apply". Where that does not end within `readBackTimeoutMs`, the route closes with `changes_undecided`.
- **A change the comparison cannot see counts as kept.** A gitlink, and a change of the exec bit alone where `core.filemode` is off, are left out of the question whether the apply was thrown away. A file that is byte for byte the blob the apply wrote counts as kept even where the user's attributes write both sides the same way, as `eol=crlf` does for a change from LF to CRLF, so throwing such a change away is not seen. A file whose bytes are neither side's and whose side before the apply does not read back as itself through the user's filters is taken for an edit when it is put back, which counts as kept.
- **A branch that was merged, then an apply as changes.** Taking the work as a branch and merging it, and then applying as uncommitted changes, finds the same changes already there, is refused, and closes the route. Before merging the branch after rounds were applied as changes, the user must drop those uncommitted copies, or the merge meets them.
- **Conflict markers the agent committed** are reported by `applyAsChanges` in `conflicted`, from the patch it writes, and not by `bringCodeOut`, whose `unmerged` comes from the space's index and sees only a merge the agent left unfinished.
- **A patch is not one write.** `git apply` writes file by file, so a failure in the middle can leave part of the work in the project; see `changes_partly_applied` above.
- **The byte caps count what the result would write into a working tree**, not what it takes on disk. Compressed, what a cap refuses can be a few kilobytes in a pack, which is why the transfer cap alone is not enough.
- **A transfer the cap stopped leaves the sending process inside the space** until the limit inside, which is the host's deadline plus the margin, as a transfer the deadline killed does. The host has nothing left within seconds; the live test waits for the sender inside and sees it go at that limit. That is the space's own machine, and it is a limit of the space.
- **One huge object is paid for in host memory before any check sees it.** The fetch itself inflates each object it indexes: measured on macOS with git 2.50.1, a 260 MiB commit message made the fetch peak at 279 MB while the quarantine held 804 KB on disk. The caps refuse the result right after, so nothing of it is promoted, but the memory was spent. Git offers nothing to bound that during a fetch.
- The result ref keeps what the last call that worked put there, also after a later call failed.
- Two `bringCodeOut` calls for one space at the same time are not coordinated, and applies in two processes are not either; see "Apply" for the turns within one.
- **An interrupted apply and a user's edit of the same file.** When the user edited a file that the interrupted apply had already written, and the patch's lines repeat there, the dry run may fit the patch a second time. The per-path comparison keeps that from happening for a file the apply wrote and nobody touched since; for one the user edited after it, it is what applying any patch twice does.
- **A route closed by the code before this round** can have an intent beside it, from a host that died between the two writes. It stays in the repository, is not reported, and goes with `removeSpaceRefs`.
- Not verified: a partial clone, a sparse checkout, a repository over 1 GB, and a slow link to a place. This stage's code did run on Windows and on Debian; see "Verified and not verified".

## New tools at the next start

DESIGN.md: spaces pick up a new host version at their next start, and a busy agent is never interrupted. The mounts of a container are fixed at creation. So `start` of a stopped space whose tools mount is not the current tools volume makes the container again:

1. Rebuild the spec from the labels of the old container and its inspected memory limit.
2. `docker rename` the old container to `openchamber-space-<id>-space-old`.
3. Create the new container on the current tools volume, verify it, start it, wait until its server is ready.
4. Remove the old container, then prune tools volumes.

Everything a space owns lives in its two volumes, so nothing is lost, and the token stays. On any failure in step 3 the new container is removed and the old one gets its name back. `details.rollbackFailures` lists what could not be put back, and the message says "keeps its old container" only when that is true.

The new container is removed by the id that `docker create` printed, never by name. When the create itself failed there is no id, and nothing is removed. A name proves nothing here: if anything gave the old container its name back in the meantime, a removal by name would delete the only container of the space. That was a real defect of the first draft. A failed create that still left a container behind therefore keeps the plain name, the old container stays aside, the error says so, and the next `start` repairs it.

A running space is never touched, whatever tools it runs on.

Only `start` repairs a half-done move, and calls of `start` for one space share one run inside a process. `verify`, `exec`, and `stop` rename nothing. When only the aside container exists they reject with `space_move_unfinished` and tell the caller to start the space. That is also their answer during the short window of a move in the same process. `list` shows such a space as `exited`, which matches: it can be started. When two containers exist, `list` prefers the one with the plain name. `remove` finds both by label.

The repair in `start`: if the aside container exists and the container with the plain name is stopped, that one is the unfinished new container. It is removed by the id that was just inspected and without `--force`, and the old container gets its name back. Then the start goes on as usual and moves the space again. Docker refuses to remove a running container without force. If it refuses, `start` looks again, and a running container with the plain name is a space that is already started. If the container is gone after the repair, `start` rejects with `space_not_found`.

`stop` waits for a `start` of the same space that is under way in this process, whatever its outcome, and stops then. A stop that slipped in between the create and the start of a move would otherwise resolve, and the space would run anyway.

Known limits:

- Two host processes with the same owner are not coordinated. One could repair a move that the other is in the middle of. Two rules keep a container alive in that case. The new container of a move is removed only by the id its create printed, so the old one survives and the move fails with a name conflict. The repair removes the unfinished container without force, so a new container that the other process has started meanwhile survives.
- A move that succeeded and died before it removed the aside container leaves both. While the space runs, nothing happens. The next start of the stopped space takes the container with the plain name for unfinished, goes back to the old one, and moves again. That costs time and loses nothing.
- `start` of a stopped space needs the current tools. If they cannot be prepared, for example with no internet right after a host update, `start` rejects with the fill error. It does not fall back to the old tools.
- A space whose token file the agent deleted cannot start: the command inside waits for the file until the readiness wait ends. Rewriting the token arrives with the dispatcher stage.

Not built: moving a space when the agent finishes its turn. That needs session activity, which only the dispatcher stage knows.

## Rollback

`create` checks that its seven names are free, then creates. On any failure after that check it removes everything labelled with the new id and this owner, containers first, then volumes, then the networks. That covers every step up to the readiness wait, the gatekeeper's steps among them: a space whose gatekeeper never answers, or whose server never becomes ready, is removed with everything it made. The tools volume stays, because other spaces use it. It rejects with the original error's code, and `details.rollbackFailures` lists what it could not remove. The pulled image stays, because spaces share it.

The name check sits outside the rollback on purpose. If the id is taken, the resources belong to an existing space and must survive the failed call.

After an interrupted step (`command_timeout`, `command_killed`, or `command_output_too_large`) the daemon may still finish the step whose CLI died. `create` then sweeps, waits two seconds, and sweeps again. The second sweep also inspects the seven names directly and removes the ones that exist, labelled or not. This covers a late `docker run` or `docker create`: it makes a missing `src=` volume again, and that volume has no labels. This by-name removal, and the two for a tools volume described in "Fill rollback", are the only places in the module where a resource without our labels can be deleted. It is safe because the same call confirmed all seven names absent moments earlier, and the names hold a random 48-bit id, so nobody else makes them in between. The error carries `details.uncertain: true`, so the caller can tell the user to look at the spaces list.

The manager verifies again after `create` and removes a space that reports violations or cannot be inspected. For the Docker place this repeats a check that already passed. It stays because the guarantee must hold for every place, including a place whose `create` forgets to verify.

`list` and `remove` tolerate a resource that vanishes between the listing and the inspect. Docker exits 1 for the missing name and still prints the entries it found, and those are used. Any other inspect failure rejects. `remove` also counts "removal of container ... is already in progress" as removed.

## Process rules

`run-command.js` spawns the executable directly with an argv array, `shell: false`, and `windowsHide: true`. No `cmd.exe`, no shell strings. Every call has a timeout that kills the child, and an output cap. The Docker place receives the runner as a dependency, so tests pass a fake and never mock a module.

A timeout kills the `docker` CLI only. A command started with `exec` keeps running inside the space or the gatekeeper.

`killTree: true` makes a kill end the child's whole process tree instead. Only the pushes of code in use it, see "Killing a transfer".

Since 3b the fetches of code out use `killTree` as well, and so does every git command of code out that can start one of the user's filters or run long on the user's working tree: reading it back, the dry runs and the apply; see "Apply". `keepAtExit: true` beside `killTree` keeps the kill for a timeout or an abort, leaves the child out of the exit handler's set, and gives it no pipe on its output: stdout goes nowhere and resolves empty, stderr goes to an already removed temporary file read back at `close`, its last `maxOutputBytes` with `keepTail`, and `command_output_too_large` after the end without it. Only the real `git apply` of code out uses it. The fetches also use one more option: `signal`, an `AbortSignal` that lets the caller stop the child from outside. The child is killed the way a timeout kills it, the tree with `killTree`, and the rejection waits for that kill the same way; its value is the signal's reason when that reason is a `SpaceError`, and `command_aborted` otherwise, so every rejection of `runCommand` is a `SpaceError` whatever the caller aborted with. The caller then knows that it stopped the child, which the exit code cannot tell on Windows. A signal that is already aborted starts nothing, the listener goes when the run settles in any way, and an abort after the run settled, or after the timeout has already begun its kill, does nothing: the kill happens once. Code out uses it for the transfer cap.

`stdin` is a string or a Buffer. A Buffer reaches the child byte for byte, which is how the tarballs of a development build reach the filler. `cwd` sets the working directory and is used by `tools-pack.js` only. `env` replaces the child's environment and is used by host git only.

`check()` says the CLI is missing only for `ENOENT`. Any other spawn error names its errno. `check()` also reads `docker info --format '{{json .SecurityOptions}}'` and reports the place unavailable when the engine has no builtin seccomp profile. Older engines name that profile `default`.

## Verified and not verified

Stage 5a ran on macOS 26 with Colima, linux/arm64, Docker Engine 29.2.1, host git 2.50.1, on 2026-09-26: the unit suite of the module 1414 passed and 116 skipped of 1530 on the final code, after the local review round, `packages/web` type-check and lint clean, `bunx oxlint` clean on the six new files. Live, against the real server with the switch on and a scratch repository registered as a project: a space made through `POST /api/openchamber/spaces` with an allowlist of one domain, the five progress events on the global stream and the list saying `preparing` with the step until `running`, 70 s in all with the tools volume filled on the way; inside, `git status` equal to the host's, the ignored `.env` absent, the history complete; the journal with a refused attempt and an allowed one; stop, then start with `networkRestored` true, the same refusal and allowance after it and a new `since`; a session made inside through the dispatcher and shown under the space in the sidebar; the apply preview, the apply as a branch with the space's commit and the uncommitted rest on top, and the apply as changes bringing three files into the working tree; the switch turned off with the space stopped, the setting written and every journey route 404, and on again with the space listed as `exited`, and on the final code a turn-off refused while a space was being made, two turn-offs at once stopping the space once, a creation refused while off, and two turn-ons at once building one host; the removal with no container, network, volume, record or ref left and no `docker exec` on the host. Not run for 5a: Debian and Windows, the packaged desktop app, a creation that fails after the containers exist against real Docker (covered by the unit tests only), and any UI of the journey, which does not exist yet.

Stage 4b ran on macOS 26 with Colima, linux/arm64, Docker Engine 29.2.1, on 2026-09-25, on the final code after the review fixes: the unit suite of the module 1373 passed and 116 skipped of 1489, the tests of the changed server files (event stream, proxy, SSE routes, realtime proxy, dispatcher, relay tunnel host, UI auth) 186 passed, and `places/sessions.docker.live.test.js` 4 passed in 85 s, with no container, network or volume left and no `docker exec` on the host. Each of the three protections of TESTING.md was removed once and its tests went red: the directory rule of the list (4 cases and the host's merge), the host-id rule of the list (its case and the host's merge), and the rules for events (5 cases, the connection's drop test and the host's follow test); restored, all green. Not run for 4b: Debian and Windows, any real client, and the relay path of the new sockets on a device.

Stage 4a ran on macOS 26 with Colima, linux/arm64, Docker Engine 29.2.1, on 2026-09-25: the unit suite of the module with the tests of the changed server files, 1460 passed and 113 skipped of 1573, and `places/dispatcher.docker.live.test.js` 9 passed in 70 s, with no container, network or volume left. Each guard, the parser predicate, the directory-gate hook, the response allowlist with the redirect refusal, and the body piping were removed once and their tests went red. Not run for 4a: Debian and Windows, and any real client, since nothing in the app makes a prefixed request yet.

Stage 3b ran on macOS 26 with Colima, linux/arm64, Docker Engine 29.2.1 and host git 2.50.1, on 2026-09-23, after three rounds of review fixes: unit 1094 passed and 99 skipped of 1193 in 154 s, and the whole folder with `OPENCHAMBER_TEST_DOCKER=1` 1189 passed and 4 skipped of 1193 in 275 s, with no container, network or volume left and no host process of a transfer. `code-out.test.js` has 92 tests and the live code-out file 8, which take 200 s alone. Every protection of 3b was removed once in a disposable copy and its test went red, all three rounds of fixes included; the one exception is the report channel of the snapshot, where git sends a hook's output to stderr on 2.50.1, so no mutation of ours could show the difference. Before the merge on 2026-09-25 it also ran, after the rebase onto v2.0.1 and with the copied files hashed against the worktree: on macOS 26 again, unit 1279 passed, and live 1372 passed, 3 failed and 4 skipped of 1379, the three being the server-inside tests that still expected OpenCode 1 routes and are fixed by the OpenCode 2 branch below; with the module on a Windows 11 host against Docker Desktop, Engine 29.6.2, Git for Windows 2.54.0, 1247 passed, 5 failed and 126 skipped of 1378, the same three plus two flaky ones, a read-back timing test at 2160 ms against an 1800 ms bound and the tunnels escape test's JSON read, both green on the previous Windows run of the same files; and the unit suite on a Debian 13 linux/amd64 host itself, git 2.47.3, the tests running on the Linux host for the first time, 1283 passed, 13 failed and 83 skipped of 1379, the 13 triaged as test assumptions and not product defects, git's localized text in one assertion, a Docker bridge address chosen for the gatekeeper's own-address test, and tests that do a real DNS lookup. The serial live suite on Debian with local Docker was still running when the pull request opened; the run before the rebase passed 250. No container, network, volume or host process was left after the macOS and Windows runs.

Stage 3a ran on three machines on 2026-09-22, the same files hashed against the worktree before each run:

| Machine | Engine, host git | Result |
|---|---|---|
| macOS 26, Colima, linux/arm64 | 29.2.1, git 2.50.1 | Unit 992 passed, 91 skipped. Live 1079 passed, 4 skipped of 1083, 128 s. No leftover container, network, volume or host process |
| Debian 13, linux/amd64, over `DOCKER_HOST=ssh://` | 29.8.1, the Mac's git 2.50.1 | 1023 passed of 1083 in one run, every file but the stage-2 escape file, whose setup ran out of time filling the tools volume while the npm registry took 30 s per request from that machine. That file alone then passed 56 of 56. So 1079 passed and 4 skipped, in two runs. No leftovers |
| Windows 11, Docker Desktop, the module on the Windows host | 29.6.2, Git for Windows 2.54.0 | 1042 passed, 40 skipped of 1082, 407 s, exit 0. No leftovers. The skipped ones are switched off on `win32`: the `tools-filler` group, the unit transfer tests, and the POSIX-only signal and file-name cases |

Over `DOCKER_HOST=ssh://` every docker call opens its own SSH connection, one to two seconds each, so the live code-in file measures one `docker exec` round trip first and sizes the hanging tests' host timeout and inner margin from it, never below the local values. Two earlier Debian runs failed on those tests before they did that, and twice a stage-2 escape test was inconclusive or timed out reading the token over the same slow link; a rerun of that file passed 56 of 56 each time.

An earlier Windows run found four things no other machine could: an `EBUSY` that hid a timeout, because the kill was not awaited before the temporary folder was removed; a unit control for the tree kill that proved nothing there, because Node puts its own children into a job object; forward slashes in returned paths; and a test folder name Windows does not allow. All four are fixed and the fixes ran there.

Measured on macOS with Colima: bringing the bait in takes 1.1 seconds and its history 0.6 seconds. A clone of this repository, 3,711 commits and about 5,200 files with mixed uncommitted work, comes in in 2.0 seconds, and its history, a 103 MiB pack, in 9.9 seconds.

Before any of this code existed, the transfer was run by hand into a real hardened space on all three machines, Windows included, where stage 0 had only reasoned about it. That probe is also where the process-tree kill comes from: on every machine, killing only `git` left its `docker exec` running on the host.

Not verified for stage 3a: a partial clone, a sparse checkout, a repository over 1 GB, and a slow link to a place; a host git older than the 2.27 floor, which comes from git's manual pages rather than a run; the unit transfer tests on Windows, where they are switched off because the stand-in for a space runs the fixed scripts with a POSIX `sh`, so the live file is what covers the transfer there; and whether a console window flashes for the hidden `taskkill` in an interactive Windows session.

Stage 2 ran on three machines on 2026-09-21, the staged files hashed against the worktree before each run:

| Machine | Engine | Result |
|---|---|---|
| macOS 26, Colima, linux/arm64 | 29.2.1 | Unit 695 passed, 83 skipped. Live 774 passed, 4 skipped of 778, 113 s. No leftover container, network or volume |
| Debian 13, linux/amd64, over `DOCKER_HOST=ssh://` | 29.8.1 | 774 passed, 4 skipped of 778, 783 s. No leftovers. The first amd64 run of the corridor |
| Windows 11, Docker Desktop, the module on the Windows host | 29.6.2 | 761 passed, 17 skipped of 778, 311 s, exit 0. No leftovers. Files ran in parallel, as they do by default, beside the three live Docker files |

The 13 tests between the Windows number and the other two are the `tools-filler` group that stage 1b switches off on `win32`: 774 − 761 = 13 and 17 − 4 = 13, so the smaller number is the same run and not a partial one.

The vitest worker that ran `gatekeeper-program.test.js` used to die on Windows. It does not any more: that file ran to completion there and every one of the seven tests that had failed on a clock reading passed on a record count.

Measured on macOS with Colima, Docker Engine 29.2.1, linux/arm64, with `web` 1.24.2 and OpenCode 1.18.31:

- Every unit, contract and escape test and the live tests of the server inside: 774 tests in 96 seconds. The escape suite runs 56 test cases, from 41 `it` blocks and 3 `it.each` tables, 22 of them about the gatekeeper. The contract suite runs 13 and has the three added assertions and no changed one.
- A create with the gatekeeper costs 2.9 seconds when the tools volume is filled, against the 2.3 seconds stage 1b measured without one, and 38.9 seconds when it pays for the fill. `stop` takes 0.2 seconds and `start` 2.5 seconds. The live contract file, one fill plus the whole scenario, took 43 to 49 seconds, and the whole live run of the module 96 seconds.
- The corridor carries a real CONNECT to `example.com:443` from inside a space, refuses a name that is not on the list, refuses the same name on port 80, 22 and 8443 in both modes, and answers a changed list on the next attempt without a restart.
- The address of `auth.openai.com`, resolved on the host at test time, is refused in four forms and in both modes, with the gatekeeper's own reach of that address as the control. Before this rule existed a reviewer reached the real service through the corridor by its address.
- The space holds no long-lived credential: no OpenCode auth record, and nothing shaped like an oauth refresh record, a private key, a GitHub token or a provider key in any file under its HOME or work directory or in any process environment. The search finds all three when the test plants them first. That was OpenCode 1.18.31, which kept logins in `auth.json`. The search skipped binary files and so could not see where OpenCode 2 keeps them; see "Verified with OpenCode 2.0.15" below.
- A flood of 400 sockets on each listener, and 80 tunnels at once, leave the gatekeeper running, its journal readable, the corridor serving and the space verifying clean.
- 140 refused clients that hold their sockets are all refused, and the corridor still answers with `tunnels: 0` at 5, 15, 30 and 45 seconds afterwards.
- A tunnel whose peer dies gives its place back at once rather than at the idle limit: 20 held became 0, while a genuinely half-closed tunnel kept its place. Through a real corridor, a four-megabyte answer with a concurrent upload arrives byte for byte, sha256 equal; the old teardown loses about 460 KB in the same shape. An upstream that half-closes now receives what the space sends afterwards.
- The window's deadline answers 504 with exactly one `failed:timeout` record, gives the slot back, and 64 hanging requests all recover. It measures silence, not length: an answer of 64 bytes every 700 ms for 8.4 seconds completed under a 1.5 second deadline.
- A reset in the middle of a transfer is journalled and puts no 502 into the stream: all 65,536 bytes arrived and the place came back.
- A gatekeeper that was stopped under a running space is started again by `start`, which changes nothing about the space container.
- The gatekeeper itself reaches the Docker host, and the space reaches none of the host's addresses through the corridor, in allowlist mode and in open mode, by address and in the IPv4-mapped IPv6 form.
- A name of our own that resolves into a refused range — a helper container on the space's outer network, answered by Docker's embedded resolver with an address on that bridge — is refused with `deny:blocked-address` in both modes, including with the name on the allowlist.
- A real model key held by the gatekeeper reached a stand-in upstream on the space's outer network, in the `x-api-key` header and as a bearer token, while the space's own `authorization` and `x-api-key` were dropped. The key is in neither container's `docker inspect`, in no answer, in no error text, in no journal record, and nowhere in the space's environment, process list or files.
- A grant whose upstream is `https://example.com/` answers 200 through the window, so TLS to an upstream works end to end. Stage 0 had only proved a plain-HTTP upstream.
- 12 rounds of malformed CONNECT lines, huge header blocks, resets in the middle of the handshake and parallel connections left the gatekeeper running, the corridor serving, and the space healthy.
- The gatekeeper, seen from inside it: uid 1000, `NoNewPrivs: 1`, empty `CapEff`, `Seccomp: 2`, a read-only root filesystem, no runtime socket, no tools volume, and a writable `/tmp`.
- The filler runs as root with every capability dropped. One fill took 36 seconds and left 438 MB in the volume. The marker check takes 0.1 to 0.2 seconds.
- An idle space with the server and OpenCode holds 357 to 374 MiB and 25 processes. The contract suite's 512 MiB limit is enough to start.
- OpenCode 1.18.31 reported the directory of a session byte for byte as the space path, and the real path for a path that went through a symlink. OpenCode 2 changed the second half; see "Verified with OpenCode 2.0.15" below.
- The terminal works through its WebSocket with `node-pty`, although install scripts were ignored.
- `bun pm pack` of bun 1.4.2 runs `prepack`, rewrites `workspace:*` to the version, writes `<name>-<version>.tgz` into `--destination`, and prints the path as the last line of stdout. Two packs of unchanged sources give the same bytes, so the key of a development build changes only when a source file changes. Every file under `packages/web/server` is in the tarball, so any edit there means a new key and a new fill.

Verified on 2026-09-19, for stage 1a, on a Windows 11 host with Docker Desktop, engine 29.6.2:

- Every contract and escape test of 1a.
- Arguments reach the container unchanged: `=`, `,`, spaces, quotes, a backslash, `%PATH%`, `^&`.
- Stdin piping works.
- A timeout leaves no orphan process on the Windows side.
- The isolated network has no gateway. The injected `*.docker.internal` names do not resolve from a space. Neither the WSL VM nor the Windows loopback is reachable from a space.
- A plain internal network there does reach services in the WSL VM through its gateway, the same gap as on Colima.

Verified on 2026-09-19, for stage 1a, on Debian 13, linux/amd64, over `DOCKER_HOST=ssh://`:

- Engine 29.8.1 passed every contract and escape test of 1a.
- Engine 26.1.5 accepted the isolated gateway option silently and still gave the bridge a gateway address. The checker reported it, and `create` failed closed with a clean rollback.

Verified on 2026-09-20, for stage 1b, on Debian 13, linux/amd64, Engine 29.8.1, over `DOCKER_HOST=ssh://`:

- Every contract and escape test, the live tests of the server inside, and the live test of a development build: 197 tests. This covers the x64 binaries of `node-pty` and OpenCode.
- Every `exec` opens its own SSH connection there. The run took 18 minutes against 3 locally, and the readiness wait still ended in time.

Verified on 2026-09-20, for stage 1b, with the module running on a Windows 11 host against Docker Desktop, Engine 29.6.2:

- Every contract and escape test and the live tests of the server inside. This covers the 2,641 character `node -e` argument of the filler and a Buffer on the stdin of `docker.exe`.
- Not run there: the development build, and the 13 unit tests that run the filler program on the host with a stand-in `npm`. They are switched off on `win32` in `tools-filler.test.js`. The filler program itself ran there in the live fill.

Not verified for stage 2:

- Any place but local Docker. The three hosts above are all local Docker: Colima, Docker Desktop, and a Debian daemon driven over `DOCKER_HOST=ssh://`, which is the local place pointed at another machine rather than the SSH place.
- A real model provider. The window was proved against a stand-in upstream on the outer network and against `example.com` over TLS, both without a real key.
- A real OpenCode turn through the window or the corridor. Nothing in this stage configures OpenCode to use either.
- Whether a space with a busy agent stays under the 64 tunnel cap, the socket caps and the 300 second idle limit in real work. The numbers are chosen, not measured against a real workload.
- Memory of a gatekeeper under load. 24 MiB is the idle figure.
- IPv6 to a real upstream. The rules are tested against literals, and the outer network has IPv6 off.
- DNS rebinding as a behaviour. The defence is that the corridor resolves once and connects to what it checked, and it is covered by the rules tests, by a check on the program's own source, and live by a name of ours that resolves into a refused range. Showing a resolver that changes its answer between two lookups would need the gatekeeper pointed at a DNS server this module cannot configure, so nothing here demonstrates the race itself.
- Whether a space can reach a refused host through a third-party intermediary in open mode. It can, by the shape of the thing: the corridor sees only the intermediary's name. Nothing was run to demonstrate it, and nothing here claims otherwise.

Not verified for stage 1b:

- A real agent turn. No model key was used, so OpenCode only reached "ready".
- A fill through a slow or filtered link, and a private npm registry. Measured on 2026-09-20: the registry served metadata for the `@peculiar` scope that no range could satisfy for about four minutes, and every fill in that window failed with `ETARGET`. It reproduced in a bare container with no code of ours involved, and it cleared by itself. That is the "only the three named packages are pinned" limit above, seen live.
- Whether the filler's `sync` and flush order survives a real crash of the Docker machine.
- Two host processes with the same owner. They are not coordinated, and "Known limits" says what can go wrong.

Not verified since 1a:

- Whether a console window flashes in an interactive Windows desktop session. `docker.exe` gets a `conhost.exe` child even with `windowsHide`. The `desktop-shell` skill requires an inspection of the whole process tree before release.
- Docker Desktop's credential helper in a real desktop session. Over SSH a pull failed with "A specified logon session does not exist".

Verified with OpenCode 2.0.15, on 2026-09-24, on macOS with Colima, Docker Engine 29.2.1, linux/arm64. OpenChamber moved to OpenCode 2 after stage 3a, and three live tests and the credential search still described OpenCode 1:

- A session takes its directory from `location.directory` in the body of `POST /api/session`, and reports it back byte for byte there and in the list for that directory. `?directory=` on that route is ignored, and such a session lands in `/home/space`.
- Through a symlink OpenCode 2 reports the link path itself, `/spaces/<id>/link-to-repo`, with `subpath: "../link-to-repo"`, and leaves that session out of the list for the real path. OpenCode 1.18.31 reported the real path. OpenCode normalises nothing any more, so handing it real paths only is the whole defence, and it is the dispatcher stage's job.
- OpenCode 2 has neither `/api/instance/dispose` nor `/api/experimental/tool/ids`, and it no longer loads `.opencode/tool/*.ts` at all. A project plugin in `.opencode/plugin/` or `.opencode/plugins/` that imports `@opencode/plugin` loads from the link after `POST /api/location/reload`, and `/api/plugin` reports it `active`. Without the link it reports `failed` at once, with no download attempt.
- OpenCode 2 keeps logins as rows of the `credential` table in `~/.local/share/opencode/opencode.db`, a SQLite database in write-ahead-log mode, so a new row lands in `opencode.db-wal` first. In a fresh space the table has no rows. A row written there is listed by `GET /api/integration` as a connection of that integration, but only when OpenCode's catalog knows the integration: a key stored for a provider id of the user's own, such as `acme-llm`, is listed nowhere, so the test reads the table itself. `grep -I` does not see it, `grep -a` does.
- A provider key in OpenCode's environment, `OPENAI_API_KEY` for example, is listed as a connection of type `env`. The catalog of 2.0.15 names 218 such variables for 226 integrations. `OPENCODE_AUTH_CONTENT` appears nowhere in the 2.0.15 binary. `OPENCODE_CONFIG_CONTENT` does, and a provider key inside it is a login too; `OPENCODE_CLI_CONFIG_CONTENT` also exists and was not tried. The hardening checker therefore allows only the variables the space is given and the base image's own.
- No route of 2.0.15 stores an oauth record handed to it from outside. `/api/credential/<id>` takes a new label, and has `activate` and `DELETE`; a login is stored only by the flows under `/api/integration/<id>/connect/`. How the host will put a short OpenAI token into a space is therefore open again, see DESIGN.md.

Not verified with OpenCode 2.0.15: the rewritten credential test and the rewritten tests of the server inside have run on macOS with Colima only, not on Debian over `DOCKER_HOST=ssh://` and not with the module on a Windows host.

## Base image

`SPACE_BASE_IMAGE` in `places/docker.js` is `node:22-bookworm` pinned as `node@sha256:dd5847a04b0deee391fa145f1f4c6d214196668b6bcc7988ebed67249f226844`.

It must be the multi-arch index digest, or spaces break on the other CPU architecture. Verified on 2026-09-19:

```
docker manifest inspect node@sha256:dd5847a04b0deee391fa145f1f4c6d214196668b6bcc7988ebed67249f226844
```

The answer has `mediaType: application/vnd.oci.image.index.v1+json` and lists manifests for `linux/amd64`, `linux/arm`, `linux/arm64`, and `linux/ppc64le`. An arch-specific digest answers with `application/vnd.oci.image.manifest.v1+json` and no platform list. `docker images --digests node` showed the same digest for the `22-bookworm` tag. Repeat both commands when you bump the digest. `docker buildx imagetools inspect` gives the same facts where buildx is installed.

## Tests

```
bun run --cwd packages/web test -- server/lib/spaces
OPENCHAMBER_TEST_DOCKER=1 bun run --cwd packages/web test -- server/lib/spaces
OPENCHAMBER_TEST_DOCKER_PACKED=1 bun run --cwd packages/web test -- server/lib/spaces/places/packed
```

The second command also runs the live files against the local Docker daemon: `places/contract.docker.live.test.js`, `places/escape.docker.live.test.js`, `places/server.docker.live.test.js`, and since 3a `places/code-in.docker.live.test.js`. Since 3b it also runs `places/code-out.docker.live.test.js`, since 4a `places/dispatcher.docker.live.test.js`, and since 4b `places/sessions.docker.live.test.js`. The third runs `places/packed.docker.live.test.js`. It compiles the sdk and packs the local `web` and `sdk`, so it needs the workspace dependencies installed.

Each live file uses an owner id of its own, removes its spaces, helper containers, and tools volumes in `afterAll`, and then fails if any container, network, or volume with that owner label remains. An owner of its own also means a tools volume of its own, so every live file pays for one fill of about 40 seconds. The first run pulls the image, about 1.6 GB. The whole live run is timed in "Verified and not verified" above.

`places/contract-suite.js` is a fixed contract. It passes unchanged against the memory place, the fake Docker, and live Docker.

`places/fake-docker.js` answers like the docker CLI for the subcommands the place uses. It knows tools volumes and their fill marker, the three one-shots, container ids, `rename`, `network connect`, a container name that is in use, a volume that is in use, a CLI that cannot reach the daemon and exits with 1, the server inside a space, which answers `/health` once its token is there, and a gatekeeper, whose control channel answers once its program has been written and whose tmpfs is empty again after a stop. Its `wait` moves its own clock, so no test sleeps. `docker.test.js` uses it for tools reuse, fill, fill failure, interrupted fill, a stranger's volume, two creates that share one fill, the readiness timeout, the token that never shows in an argument, a marker check that Docker could not run, a marker for other content, a tools volume that came back without labels, and the move to new tools with its rollbacks, including the old container that got its name back before the create landed.

`dispatcher.test.js` runs the dispatcher in front of a stand-in for the server inside: an Express app behind the host's own `createUiAuth`, so the login, the cookie name with the port in it and the refusal of a wrong token are the real ones, reached over a plain socket. It covers the prefix and the credentials coming off and the session going on, one login and one connection pool for many requests, a three-megabyte body streamed in, a form-encoded body kept whole by the parser predicate, an event stream read event by event with the request inside ended when the client leaves, a four-megabyte event stream whole, both guards in every spelling they take with their controls, the host-only and refused routes with their neighbours forwarded, an unknown id and one that appears, the cookie stripped and the page and raw-file content types, a redirect and `Clear-Site-Data` and `Refresh` never leaving the dispatcher for any method, an answer without a content type served as text, a stopped space answered with its code while a three-megabyte body is still on its way in, the renewed session and the repeated request after a 401, the body request that is not repeated, the renewal before expiry, the codes for a stopped space and an unreachable server, and the token absent from every answer and log line when the login fails. Each guard's test was run once with its guard removed and went red; so did the parser predicate's test, where the request hangs without it. `command-stream.test.js` runs `openCommandStream` and the bridge on the host's Node: bytes both ways, the error with the command's stderr, the kill on destroy, the idle timeout, the spawn failure, HTTP over a keep-alive agent with one process for many requests, the four-megabyte full-duplex exchange compared by sha256, `ECONNREFUSED` reported, and the command gone when the host lets go and when the server closes. `host.test.js` covers the owner file and the wiring over the memory place, with no process started, and since 4b, over a stand-in place whose spaces are Express apps behind the real UI auth, the merged list with its drops and the stale mark of a stopped space, the event connections into the real hub with the host's ids learned from it, and a socket forwarded through the mounted upgrade handler. `websocket.test.js` runs the forwarder against such a stand-in with an echo socket: the handshake built without the user's credentials and with the loopback origin, a header with a line break refused, the answer parsed with the bytes after it, the close frame's bytes, the terminal socket forwarded with the query token gone and the space session on, a 4 MiB frame both ways, every refusal before the upgrade with its code, the place's and the space's own refusals, the host's auth and origin rules per socket, the session renewed once after a 401, the close frame when the stream inside dies and the stream killed when the client leaves, and every socket ended at close. `space-sessions.test.js` is the table of what a space may claim: each directory spelling kept or dropped, a host id never taken, an id another space listed dropped, the field allowlist, each event rule, the merge order and marks, and the index's stale, partial, unknown and forgotten states. `space-events.test.js` runs the connection against a stand-in stream and the real hub: the announcement, the status seed, an event entered with the space's id and directory, the three drops with the directory absent from the log, the backoff after the stream ends with the gap announced, the connection dropped for a space that is gone, and a refused connection retried quietly. `places/sessions.docker.live.test.js` is the live file of 4b, see "Verified and not verified".

`space-server.test.js` feeds the channel every hostile answer listed under "The exec channel", and `gatekeeper-channel.test.js` does the same for the gatekeeper's channel, including a journal that is not JSON, one without records, records of another shape, one larger than the host reads, and since 5a the `since` field taken as text only.

`journey.test.js` runs the journey over the memory place with recording stand-ins for the gatekeeper channel, code in and code out: the order of the steps and the events of a create, what the record holds after it, a history that fails alone, a failure at the network or at code in that removes the space and lists the failure until it is dismissed, an unavailable place, an allowlist refused without host isolation, each bad field of the request refused before anything runs, stop and start with the network said again, a start without a record, the history sent again at start, remove with the refs and the record and with refs that would not go, every action refused while a creation is held at code in, stop all with one space that would not stop, no creation after the turn-off began until it is undone, a failed creation whose clean-up failed removed at its dismissal, the journal of a running and of a stopped space, the apply preview, the apply as a branch with the removal after it, as changes with the space kept, no removal when there was nothing to apply or the apply refused, and a remove, a stop and a journal read refused as busy while an apply holds the space. `routes.test.js` runs the routes over a stand-in journey: every route 404 while the feature is off with the switch still answering, the switch's preview and its turn-off, each route translated into its journey call, an id that is not a space id and a body that is not an object refused before the journey is asked, and the status of every code; and the switch controller: the unknown list when the place cannot be asked, one host built for two turn-ons at once and closed once for two turn-offs, the order of stop, write and close, and a setting that cannot be written leaving the feature on and open. `space-records.test.js` covers the file, its mode, the update, an unreadable file never taken for a fresh record, the id check and the domain rule.

`gatekeeper-program.test.js` covers the program two ways. It requires the file and calls the rules directly: every blocked range with its edges, every IPv6 embedded form, the public addresses that must stay allowed, `refuseTarget` for both modes and both ports, thirty target forms that must be refused as addresses or non-names, including the integer, hex, short and octal spellings, ten ordinary names that must still pass, the address-family choice, and what `splitTarget` refuses. The program runs its listeners only as the container's main module, so requiring it binds nothing and there is no switch that changes a decision. Then it spawns the program the way `tools-filler.test.js` runs the filler and reads decisions back from its own journal: a name that does not resolve, `localhost` as the name that proves the check is on the resolved address, exact-match domains, the port rules of both modes, `auth.openai.com` by name and in fifteen address and non-name forms, the three Unicode full stops, which Node's own parser refuses before any rule of ours sees them and which are therefore checked against the rules directly, a plain proxy request, malformed CONNECT lines, a reset in the middle of the handshake, a 200,000 character header block, a flood of 600 sockets on each listener, the window's header rules against a local upstream, a grant that points back at the gatekeeper, journal redaction, the ring buffer's overflow, and what the control channel refuses. A tunnel that carries bytes needs a name that resolves to a public address, so the tunnel cap and every tunnel that carries something live in the escape suite, inside real containers.

`tools-filler.test.js` runs the real filler program with Node against temporary directories, with a stand-in `npm` on PATH.

`places/dispatcher.docker.live.test.js` mounts the dispatcher over a real space, the way `server/index.js` mounts it, and covers a prefixed request answered by the server inside, a session created inside through a streamed body and listed back, the event stream of the server inside, the guard against a directory outside the space and a host-only route, the escape check of TESTING.md, a request without the prefix but with the space's directory refused before a host route that counts its runs, the bridge processes inside counted from `/proc` and gone after the host closes its connections, and a stopped space answered with 503 and served again after a start. Its second part starts the real server headless, `index.js --api-only`, with `DOCKER_HOST` pointed at a stand-in daemon on a Unix socket that counts its connections: with the switch off the prefixed request falls through to the host, the unprefixed one runs, the daemon sees no connection and no owner file is written; with the switch on the prefixed request is the feature's 502, the guard refuses, the host-only route is refused, and the daemon was asked.

`places/server.docker.live.test.js` covers `/health`, the token file, the session directory question of stage 0 through OpenCode 2's `location` body, a symlinked directory, a project plugin that imports `@opencode/plugin` and must load with no download, the terminal WebSocket, stop and start, the move to a second tools key at the next start, and a failed fill that leaves nothing. The terminal probe is a small Node script that runs inside the space and takes `ws` out of the tools volume.

`code-in.test.js` runs real host git in temporary directories, with no Docker, against the bait repository of `code-in-bait.js`: a committed file with the executable bit, an ignored `.env` with a fake secret, `node_modules/`, a file that only a global ignore file names, set through `GIT_CONFIG_GLOBAL`, a staged new file, a file with staged and unstaged edits, a deleted tracked file, an untracked file with a space in its name, a Unicode name, and a symlink except on Windows. The bait is built once per object format and copied into each test, because building it was most of the cost. The tests cover the comparison of host states itself, the two snapshot commits, the host ref and its removal, the clean mode, a user who signs every commit with a broken `gpg.program`, a user with no identity, every refusal with the host state compared, an ignored and an empty project subfolder, untracked repositories with odd names and without a commit, SHA-256, a linked worktree, a split index, intent-to-add, a detached HEAD, a project in a subfolder, the list of travelling files against what the snapshot adds, the limits, the URL builder, a shallow host, the git version floor with build suffixes, a temporary folder that cannot be made or removed, every failure of the history keeping its code, step and cause, a failure of the place or of the runner during `bringCodeIn` becoming `code_transfer_failed` with its code as the cause, a folder that does not exist, and a git that cannot be started. The transfer tests run against a stand-in for a space in a local folder: its `execArgv` points at a small Node receiver that checks the fixed command and runs the host's `git receive-pack`, and its `exec` runs the fixed scripts with the host's `sh`, so they are switched off on Windows. They cover the whole transfer and the history, the identity a user has in full, in part or not at all, the start agreeing with the working tree inside when untracked repositories stay behind, thirteen global hooks of the user's that must not run, SHA-256, a linked worktree, a detached HEAD, a space that refuses, one that prints without end, one that never answers, where a grandchild of the receiver must be dead afterwards, a project path that is taken, a history whose unshallow fails inside on a held `shallow.lock`, a user's fsmonitor program and daemon, a user who asks for automatic gc and maintenance at every chance, two concurrent history calls sharing one push, a repeated call answering `already_complete` without a push, and no stray `master` in the space's reflog. The refusing and flooding tests also check that the receiver really ran the command the host wrote out, so a receiver that never started cannot pass for one that refused.

`run-command.test.js` shows the process-tree kill with a real grandchild, which survives without `killTree` and dies with it, the child gone by the time the rejection comes, a grandchild that escaped the group ended by the close wait's bound, and a wrapper process whose `killTree` child and grandchild die with it when it leaves by `process.exit` or from its own SIGINT handler, and checks the pid guard against fakes for pid `null`, `undefined`, 0, 1, negative, fractional and string pids on every platform.

The eighth round, the rule path by path and the read-back by bytes, added a block of its own, `applyAsChanges after round seven`: the ordinary reviewer's matrix done with git, a pull, a switch of branch, a stash popped, applied over or dropped, an amend, a hard reset over a commit that brings the whole work, a revert that counts as the user's, and a commit followed by a deletion or a move, each with what the next apply brings; the verifier's apply from a second linked worktree, whole there and refused as a part in the first; a recorded interrupted apply keeping the HEAD of its intent; an attempt that wrote nothing after the second of two applies was thrown away; an invisible change beside a visible one, kept; `describeApplyState` reading nothing back on a closed route; a listing too long and a read-back time shared by the whole call, each `changes_undecided`; a clean filter that prints 200 KB a file on stderr; a kept last apply read by bytes while a slow clean filter never runs, counted through `files`; a path below a link read as gone; a name with a line break; a change of the exec bit alone where `core.filemode` is off; the time checked while one slow file is read; an LFS-like filter's second round refused with its own message; and three tests of the attributes read back with: a thrown-away `.gitattributes`, `.lfsconfig` and `.gitmodules` that name or configure a filter, a `.gitattributes` an interrupted apply never wrote, and one the user kept, which is honoured. Eleven of them are red on the seventh round's code.

The ninth round added `applyAsChanges after round eight`: a file of the space named like pathspec magic and a name with a line break, each undone by a commit of the user's and taken for the user's edit; a committed undo beside one without a commit, refused as a part, with no advice about clearing the way for a file committed as it is; a clean filter that fails while reading back, `read_back_failed`; the list of the user's commits read whole past four megabytes, and one past the cap, `changes_undecided`; the log asked once per call over every path and not at all when nothing is back as before, counted; a HEAD with no commit; a FIFO and a link swapped in between the host's `lstat` and its open, each `changes_undecided` at once; and a clean filter that never ends, required gone after a read-back, a dry run and an apply that each ran out of time. The LFS-like test gained a second file of the filter that fits, which the refusal must not name. Seven of them, and the two older tests of that refusal's message, are red on the eighth round's code; the others pin behaviour that was already right, and each is red under a mutation of the line it guards.

The seventh round, "thrown away" decided from authoritative state, added a block of its own: `git restore .` of the last apply with its added file left and a redo that touches only another file, refused and closed with both parts named; an edit on a file of the last apply, kept; every apply thrown away, the whole work from the start; the last apply committed and then put back by hand, kept, and committed with one file deleted, an edit: applied beside it, and refused by the dry run once the redo touches that file; a last apply of a gitlink only, kept; a CRLF-only change under the agent's `text eol=lf`, kept, alone and beside a change that can be seen; an attempt that wrote nothing followed by a throw-away of the apply before it, taken for not started; `describeApplyState` beside an interrupted apply read as a part, counting from the last apply; the verifier's closed route that stays closed after a throw-away, its read-only read beside an intent, its failed intent write and failed recovery record, and its file of the patch that is a link on the user's disk; the folder cap at its exact number; two linked worktrees taking turns; a dry run that fails on 8,000 long paths, for an apply and for a recovery; a read-back and a dry run that do not end in time; a slow clean filter; and an operation count that shows the check of all applies stopping at its first path. `firstRedirectedFolder` gained the exact spelling of a deleted link, and `run-command.test.js` the output window of `keepTail`.

`places/code-in.docker.live.test.js` runs each of its tests alone as well as in order: the shared code in happens in `beforeAll`, and each hanging test makes a space of its own. It brings the bait into a real space: `git status --porcelain=v1 --untracked-files=all` inside equals the host's line for line, `run.sh` is 755, the name and email are set, and `.env`, `node_modules` and the globally ignored file are absent. The history arrives: the commit count matches, the repository is not shallow, and the side repository is gone. The secret test collects the space's work directory and HOME as a tar, and every git object uncompressed, gzipped on the way out, and searches them on the host; its controls find a travelling file's text in the tar, and the text of a tracked file the bait deleted in the objects and not in the tar. Two tests plant a pre-receive hook that never ends through a global `core.hooksPath` in the space's HOME, one for the history and one for code in, each in a space of its own, with the host timeout and the inner margin sized from the slowest of three `docker exec` round trips measured at the start, and never below 6 s and 5 s, and check the timeout, the host ref and host state, the process tree on the host, and what runs inside before and after the inner limit.

`run-command.test.js` also covers the abort option of 3b: a signal that is already aborted starts nothing, where the control run of the same command does; an abort during a run kills the whole tree, the child gone by the time the rejection comes and the grandchild after it, with the rejection being the reason itself and the live set empty; without `killTree` the child alone goes; an abort after the run closed changes nothing; an abort after the timeout keeps the timeout as the error; and no listener stays on the signal after a close, a timeout, an abort or a spawn failure. A reason that is not a `SpaceError`, a string, null or nothing at all, rejects with `command_aborted`, before the start and during the run. On Windows the tree test runs the real `taskkill /T`.

`code-out.test.js` runs real host git in temporary directories, with no Docker. The apply tests run on every platform against a result made on the host, fetched into the result ref of a space whose start the snapshot of code in wrote: a patch that writes exactly the result tree, with a changed, an added, a deleted, a binary and an executable file and a link to outside, while the index, HEAD and `.git` stay as they were; nothing to apply; three unclean cases that touch nothing; the three `apply.*` settings and `diff.noprefix` and `diff.relative`, each first breaking the obvious command; a link of the user's where the space writes a folder; SHA-256; the branch with its exact changes, an existing branch, twelve bad names, a missing result, and a linked worktree. The filter test is POSIX only. The transfer tests run against the stand-in for a space of `code-in-bait.js`, extended with an `upload-pack` side that can refuse, flood, hang with a grandchild, or send slowly: the whole path with commits, uncommitted and untracked changes in the space and both applies; no extra commit when nothing is uncommitted; a shallow space; a forged `shallow` file; ten hostile tree names and the control with an ordinary name built the same way; tags and refs outside the refspec, with an ordinary fetch as the control; a refusal and a flood; a space that never answers, whose grandchild must be dead afterwards; a transfer stopped by the size cap in the middle, which the stand-in's `upload-pack` shows by never reporting its end, with the same space coming out with room as the control; a file of zeros, a large file committed and deleted, many copies of a user's file, a megabyte of commit message and a tree with forty thousand entries over the changed-bytes cap, each with the control of a larger cap; a transfer over the cap that arrived in one piece, which the cap after the fetch is what refuses; a repository the agent made inside its project and the paths of a conflicted merge, both reported and both applied as they are; the name check against a 200,000-file tree with one new file, the length rules on every platform and the Windows path rule with and without `core.longpaths`, a new file in a folder the user already has in two spellings, a rename that changes only the case of a file and of a folder refused with the route open and applied after a new name, a folder stored decomposed on macOS, tracked and untracked, applied rather than taken for a link, an ignored file in the way named as such, an interrupted apply that wrote nothing, one that stopped halfway and one that finished unrecorded, `describeApplyState` before and after an apply and with an intent and a closure, and a hook that writes to descriptor 3 with the planted commit refused; every Windows name rule through the check with the platform given, each also let through on macOS and Linux, ordinary names with spaces, Unicode, leading dots and brackets let through everywhere, names that differ only in case and the two spellings of an accent, a case collision and a name with a line break refused in a result made with git's plumbing, the route left open and the renamed file applied after it, and the same through a real bring-out; a link of the user's on the way to a path the patch adds, deletes, or reaches several folders down, refused before any patch and closing the route, a link that points inside the work tree, a link beside the patch's paths that is none of its business, and each of the two ways the check sees a redirection proven alone against fakes; a refused round closing the route, the next call refusing at once with no patch built and nothing touched, the branch working afterwards and holding the same commit, the closure surviving a new bring-out, and a space that was never refused keeping its route; a second apply that brings only the new work while the user's own file beside it stays, the same result applied twice, a second apply that collides, and the applied ref written only on success, moved by no branch and removed with the other two; a result the space swaps for another between the snapshot and the fetch, with the unswapped run as the control; one object that is not a file over its own cap; what a deletion costs and the apply refusing it; many copies of a file the user already has, refused by the apply and not by the bring-out; a hook that prints while the snapshot runs, with a control that it really ran; a repository and a conflicted path the space names with a line break, counted and shown redacted; what an apply reports about nested repositories and conflict markers; an apply that stops in the middle in a folder that cannot be written, with the writable run as the control; and a host git that fails where code out has to answer for it, for the patch it could not build and for the ref it could not write; too many paths and too many objects; bad requests, which never reach the space; an unreachable space and a failed snapshot inside; a temporary folder that cannot be removed; SHA-256; a project in a subfolder of a linked worktree; the user's hooks and fsmonitor; and gc, maintenance and commit graphs.

The fifth round of review added, in the same file: a result through more folders than the cap, and one whose folders lie too deep in all, each refused before anything is built with the branch holding it and each applying with room for it; the measured shape that ran the host out of memory, refused at the default caps in seconds; a bring-out that refuses neither; each APFS pair of the fold, as a name clash and as a case-only rename, and the same pairs on this Mac's own disk with the control that the disk ignores case; the link check across several paths, past a file at the top, with a link the patch deletes and the controls where it does not; a tracked link the space turned into a folder, applied; two applies and a read at once, one applied and one with nothing left; an interrupted apply with an edit of the user's since, taken for never started and for finished, a file of repeated lines it wrote that the dry run alone would take again, the closure at the interrupted result and not a newer one, and the intent gone after an apply that stopped; an intent beside a closed route; the list of changes longer than the host reads, for the apply's message and for `describeApplyState`; the path length with the project's folder in front, Windows' 259 and 247 at their boundaries, and `core.longpaths` read by the apply; and a branch name that a branch folder is in the way of.

The sixth round, the maintainer's decision on a thrown-away apply, added a block of its own: the whole last apply thrown away and the work applied again whole, with the route open and `describeApplyState` counting the same before and after; part of it thrown away, where the whole work would fit and leave the user's kept file behind, refused and closed; a throw-away beside an edit on another file, applied; one beside an edit on a path of the last apply, where the whole work would fit, refused; two applies with only the second thrown away, which brings the second round again from the first; no throw-away, the ordinary way, with each apply's base recorded; the whole work after a throw-away even where the patch from the last apply would fit; the same result applied again after a throw-away; a repository with no recorded base, as before; the caps held against the whole work, refused with the route open; and a host that dies before or right after `git apply` writes the whole work, read against the recorded base: finished and recorded, never started and applied again, or halfway and closed with both halves of the intent gone.

`places/code-out.docker.live.test.js` gives every test a space of its own and a bait repository of its own, and removes the space at the end of the test. It brings the agent's work out of a real space and applies it both ways, with the status inside unchanged, the branch holding mode 100755, and the host's `git status` showing what came out as uncommitted changes; brings work out of a space whose history has not arrived; plants a hook, a filter, a hooks folder and config in the space, plus tags and a branch, and sees the hook run inside and nothing on the host, and no ref but the result; refuses seven hostile tree names built inside past git's path checks, with the ordinary name as the control; refuses a link of the user's where the space writes a folder, with the result holding the file and a link to `/etc/passwd` as the control; ends a fetch that a planted `uploadpack.packObjectsHook` holds forever, sized from measured `docker exec` round trips like code in, and checks the host processes and what runs inside before and after the inner limit; refuses a moved result when a planted hook writes a commit id of its own to its parent git's stdout through `/proc`, with the hook's own marker as the control; stops a space whose planted hook sends a pack without end at a 4 MiB cap in seconds, against a 90 s deadline, which only the polling cap can do; and stops a 16 MiB transfer at a 4 MiB cap, with the default cap as the control.

Escape tests run inside a real space and pass only when the attempt fails. A positive control backs the attempts that could pass for the wrong reason:

- Network attempts first need a plain container on the default bridge to reach `example.com:443`. If it cannot, they fail as inconclusive, so an offline machine never passes them. The space then tries the address that the baseline reached.
- The host-listener attempt has two controls. The listener runs with `--network host` and reports every IPv4 address of the Docker host from the kernel's local route table. Those addresses plus `host.docker.internal` and `gateway.docker.internal` are the candidates. First a plain container must reach the listener. Then a container on a throwaway internal network without the isolated gateway mode must reach it too, which shows that the hole is real on this engine and that the probe can see it. If either control connects nowhere, the test fails as inconclusive. The space must then fail on every candidate and on every address a control reached through a name. On an engine that ignores the isolated mode, the candidates contain the gateway of the space's own bridge, and the test fails. Measured on Colima: the plain network connected through its gateway `172.19.0.1`, and the space got `ENETUNREACH` for every address and `EAI_AGAIN` for both names.
- `unshare`, `mount`, the hostname write, and `test -e` first prove that the tool or the file exists.
- The log and memory attempts check that the write and the kill really happened. The memory attempt also checks that the server inside is still healthy afterwards.
- The tools attempts first run `openchamber --version` from the mount, and show that the running server and OpenCode were started from it. Ten attempts to create, overwrite, replace, delete, rename, or chmod must fail with `Read-only file system` and nothing else, and must leave a fingerprint of the mount unchanged. Root owns the files, so `Permission denied` would stop the space user on a writable mount too and is not accepted. Measured: on a writable mount of the same files every one of these attempts answers `Permission denied` or `Operation not permitted`, so the ten would fail there. One more attempt, appending to the launcher, is answered by the file's ownership even on the read-only mount, because the kernel checks permissions first when it opens an existing file without truncating it. That test accepts both answers and says that it proves the ownership, not the mount. A remount must fail too, and `/proc/mounts` must show the mount as `ro`.
- The `~/.curlrc` test first shows that a plain `curl` inside does carry the injected text.
- The token test first logs in with the token it read over `exec`, and gets refused with another one. Then the full `docker inspect` output must not contain it.
- The listener test first finds the server's own loopback listener in `/proc/net/tcp`.
- The gatekeeper tests open with a positive control that the corridor carries a real CONNECT to an allowed domain, so every refusal that follows is a refusal and not a broken corridor. A 502 there ends that test as inconclusive, naming the host and the answer, because it means the corridor allowed the target and the Docker machine could not reach it. Measured on a Debian machine over `DOCKER_HOST=ssh://`: exactly that happened once, for the product service that control used to carry to, and the run reported a failed restriction when the restriction had worked. Both names it carries to now are IANA's reserved documentation domains, so the live suite depends on one operator rather than two.
- The refused name `auth.openai.com` is tried by its real address as well, resolved on the host at test time, in four forms and in both modes, with the gatekeeper's own reach of that address as the control. The test fails as inconclusive if the name does not resolve here. This is the escape a reviewer found in the first draft: the name was refused and the address was not. The allowlist-mode test says the name is unreachable; the open-mode test says only that these two forms are refused, because in that mode nothing can say more.
- The long-lived credential test searches the space's HOME, `/tmp`, its work directory and every process environment for the shape of an oauth refresh record, a private key, a GitHub token or a provider key, and for OpenCode 1's `auth.json` by path, which OpenCode 2 imports once if it finds one. The file search reads binary files too, because OpenCode 2 keeps logins in the `credential` table of its SQLite database `~/.local/share/opencode/opencode.db`, and `grep -I` skipped that file. It also asks OpenCode for the connections of every integration, which covers a stored login and a provider key OpenCode found in its own environment, and reads every row of the `credential` table, integration and kind only, with Node's built-in SQLite, because OpenCode lists only integrations its catalog knows. It requires none of either. The clean search runs first. Then the control writes two rows into the database OpenCode is using, with a five-second busy timeout, because no OpenCode 2 route stores an oauth record without a real login: an OpenAI oauth row with a decoy refresh token, and a key for the unknown provider `acme-llm`. OpenCode must then list the first as a connection, which shows it is the store OpenCode reads, the table read must return both, and the search must find the refresh shape in `opencode.db` or its write-ahead log, next to a planted private key and a decoy key in a process environment. The order matters: SQLite leaves a deleted row's text readable in the write-ahead log, so a clean search after the control would find the decoy. That is also why the search reads those files at all, since a login a user removed can still be there. Stage 2 puts no such credential inside, so the test guards the one guarantee that holds in both modes. The clean-up must report both rows removed. Measured: the old text-only search misses the planted row, a provider key added to the space environment turns the connection check red, and without the table read the custom provider's key goes unseen.
- The tunnel cap is tested by opening 80 tunnels at once to an allowed domain from inside the space. It passes only when some were established, none beyond the cap, the journal holds `deny:too-many-tunnels`, and the corridor carries a tunnel again once the space lets go.
- The cross-space tests make a second space of the same owner, about three seconds here because both share this owner's tools volume, and look from the first at everything the second has: its space address, both of its gatekeeper's addresses, on the server, corridor, window and control ports, directly and through the first space's own corridor. The controls are that the other space's server answers, that each space reaches the corridor of its own gatekeeper at that same port, and that the shared name `gatekeeper` resolves to the space's own gatekeeper and never the other's.
- The tunnel's teardown is covered by `joinSockets` in the unit tests, with socket pairs built the way the program builds them: the space-facing one half-open as an `http.Server` accepts it, the one out half-open as the program now asks `net.connect` for. Five cases: four megabytes carried whole while the space is still uploading, which is the only shape that shows the truncation; a half-close passed on in each direction with what follows still arriving; the place given back when one side dies under load; and the place kept while a half-closed tunnel is still carrying. It is tested there because no address this machine has is one the corridor would tunnel to, so a tunnel cannot be established any other way. Put the old teardown back and four of them fail; take away the part that tells a peer to go and one does.
- There is no live large-download test, on purpose. The suite used to pull a ten-megabyte published tarball through a real corridor and compare its sha1 with the registry's, which made every run depend on somebody else's service: a bad minute there reported an inconclusive run, and an inconclusive run is a failed one with no evidence in it. It also never caught the defect it was written for, because one direction alone does not fill the corridor's buffer.

  The large-transfer guarantee comes from the unit test above: four megabytes carried full duplex while the space is still uploading, compared by sha256, deterministic, and failing when the teardown goes back. That is byte-exact evidence and the live download was not. What the live suite still proves about carrying is that the corridor carries to a real host through a real container, which is the positive control every gatekeeper test opens with. Adding a large download back would buy size, and size is what the unit test pins.
- The live socket flood opens 400 connections to each of the three listeners from inside the space and holds them. What it proves is survival, not the cap: the gatekeeper is alive afterwards, its journal is still readable and holds at most a few notes about the flood, the corridor serves again, and the space still verifies clean. It does not assert that a connection was refused, because inside a busy container that is no more guaranteed than it was on the host.
- The unit tests do not flood a listener at all. They climb to its cap, and every rung is proven accepted before the next one opens: a listener answers only a connection it has accepted, so a rung that answered is a slot taken. Once the cap is full of proven connections the next one can only be dropped, whatever else the machine is doing, and the client sees its own socket closed with nothing on it. The note is written before that close, so the assertion waits for nothing.

  This replaced three tests that opened 300, 200 and 100 sockets and read the journal. A cap is not exceeded by opening sockets quickly: a connection is dropped when it is *accepted* past the cap, and accepting is the server's turn on the CPU, while the kernel's backlog keeps completing handshakes whether the process runs or not. Measured with the program stopped while 300 clients connected: every one of them connected, 128 sat in the backlog, nothing was accepted past the cap, and no note was ever written — not after three seconds of waiting. That is what failed in CI, which runs 245 files at once, and then on a Windows host beside the live Docker files. Lowering the cap did not fix it, because the number of sockets the client opens was never the problem.

  The rungs on the corridor are refusals, and their clients are opened with `allowHalfOpen`. Without it a client ends its own side when the refusal ends the gatekeeper's, the connection goes away, and the cap never fills: measured, two refusals left nothing behind and nothing was dropped.

  A second gatekeeper runs for these three tests with a cap of 2 on every listener, so each one costs five sockets instead of forty, and the first gatekeeper keeps a corridor cap of 20 — above the 16 refused clients the program waits on at once, which is what the test beside it needs.

  Sockets are the scarce thing here. On a Windows host a closed port waits two minutes in TIME_WAIT before it comes back, and beside the three live Docker files, which hold the daemon for five to six minutes each, this file's worker died twice: once at its original counts and again at 24, 8 and 8. Keep it cheap. Measured on Windows 11 with Docker Desktop 29.6.2 at those counts: the file alone passed 206, the whole folder with `OPENCHAMBER_TEST_DOCKER=1` one file at a time passed 761 of 778, and only the parallel folder run lost the worker — which is the ordinary way it runs.

- The "cannot reach the Docker host through the corridor" test is controlled by the gatekeeper itself reaching that host first, from its own container over `exec`. If it cannot, the test fails as inconclusive. Without that control a refusal could just as well mean that nothing had a route.
- The address block is proved live by a name, because an address in a CONNECT target is refused by the name rule before the block is consulted. The name is ours: a helper container on the space's outer network with a network alias of its own, which Docker's embedded resolver answers with an address on that bridge, inside `172.16.0.0/12`. The test checks that the address really is in a refused range before it asserts anything, and a name that does not answer is a failure rather than an inconclusive, because nothing outside this machine is involved.

  It used to be five names from third-party wildcard DNS services. Twice that turned a run on somebody's machine into an inconclusive one — a failed run with no evidence in it — when the service was having a bad minute, and a test that cries wolf gets ignored. What those names covered, that `10.0.0.0/8`, `192.168.0.0/16`, `169.254.0.0/16` and a Docker bridge are all refused, is arithmetic the unit tests check thirty ways without a network; what they were really for is the path, resolve then refuse, and one name proves that. Measured with the block removed: two live tests fail, this one and `host.docker.internal`, which answered 502 instead of 403.
- The key test first shows that the key is real: the stand-in upstream answers with the sha256 of what it received, and that matches the sha256 of the key the host gave the gatekeeper. Only then does the test look for the key in the answers, the error texts, the journal, the space, and both containers' metadata. The upstream never sends the key back, so proving it travelled does not hand it to the space.
- The host-listener test also asserts that the gatekeeper's own address is not among the candidates, because since stage 2 the `.1` of the space's subnet is the gatekeeper.

The escape suite's space has 1 GiB of memory since 1b, up from 256 MiB, because the server and OpenCode hold about 370 MiB at rest. The memory attempt stays meaningful: the allocating process grows to about 650 MiB, far more than any other process in the space, so it is the one the kernel kills. The test still demands exit code 137 and a running space.

The read-only filesystem, `su`, `sudo`, and `/proc/self/status` checks have no control of their own. They rely on the write control at the top of the suite.

To look for leftovers by hand:

```
docker ps -a --filter label=openchamber.space
docker network ls --filter label=openchamber.space
docker volume ls --filter label=openchamber.space
```
